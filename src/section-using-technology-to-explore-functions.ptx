<?xml version="1.0" encoding="UTF-8" ?>

<!-- *********************************************************************-->
<!-- Copyright 2016-2018                                                  -->
<!-- Portland Community College                                           -->
<!--                                                                      -->
<!-- This file is part of Open Resources for Community College Algebra    -->
<!-- (ORCCA).                                                             -->
<!-- *********************************************************************-->

<section xml:id="section-using-technology-to-explore-functions" permid="jnC">

    <title>Using Technology to Explore Functions</title>

    <objectives permid="rgw">
        <title>PCC Course Content and Outcome Guide</title>
        <ul permid="WSg">
            <li permid="kVL"><xref ref="mth95-calculator-skills" /></li>
        </ul>
    </objectives>

    <introduction permid="XnF">
         <p permid="XXM">Graphing technology allows us to explore the properties of functions more deeply than we can with only pencil and paper. It can quickly create a table of values, and quickly plot the graph of a function. Such technology can also evaluate functions, solve equations with functions, find maximum and minimum values, and explore other key features.</p>

         <p permid="EeV">There are many graphing technologies currently available, including (but not limited to) physical (hand-held) graphing calculators, <url href="https://www.desmos.com/"><pubtitle>Desmos</pubtitle></url>, <url href="https://www.geogebra.org/"><pubtitle>GeoGebra</pubtitle></url>, <url href="https://www.sagemath.org/"><pubtitle>Sage</pubtitle></url>, and <url href="http://www.wolframalpha.com/"><pubtitle>WolframAlpha</pubtitle></url>.</p>

         <p permid="kme">This section will focus on <em>how</em> technology can be used to explore functions and their key features. Although the choice of particular graphing technology varies by each school and curriculum, the main ways in which technology is used to explore functions is the same and can be done with each of the technologies above.</p>

        <figure permid="Qtn">
            <caption>Alternative Video Lesson</caption>
            <video youtube=""/>
        </figure>
    </introduction>

    <subsection permid="PuL">
        <title>Finding an Appropriate Window</title>

        <p permid="HnW">With a simple linear equation like <m>y=2x+5</m>, most graphing technologies will show this graph in a good window by default. A common default window goes from <m>x=-10</m> to <m>x=10</m> and <m>y=-10</m> to <m>y=10</m>.</p>

        <p permid="nvf">What if we wanted to graph something with a much larger magnitude though, such as <m>y=2000x+5000</m>? If we tried to view this for <m>x=-10</m> to <m>x=10</m> and <m>y=-10</m> to <m>y=10</m>, the function would appear as an almost vertical line since it has such a steep slope.</p>

        <p permid="TCo">Using technology, we will create a table of values for this function as shown in <xref ref="table-values-for-an-appropriate-window">Figure</xref>. Then we will to set the <m>x</m>-values for which we view the function to go from <m>x=-5</m> to <m>x=5</m> and the <m>y</m>-values from <m>y=-20{,}000</m> to <m>y=20{,}000</m>. The graph is shown in <xref ref="figure-graph-with-appropriate-window">Figure</xref>.</p>

    <figure permid="fQG">
        <caption>Creating a table of values to determine an appropriate graphing window</caption>
        <sidebyside widths="47% 47%" margins="0%" permid="BFB">
        <table xml:id="table-values-for-an-appropriate-window" permid="Atr">
            <caption>A table of values</caption>
            <tabular permid="MHJ">
                <col halign="center" right="minor"/>
                <col halign="center" />
                <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m>y=2000x+5000</m></cell>
                </row>
                <row>
                    <cell><m>-5</m></cell>
                    <cell><m>-5000</m></cell>
                </row>
                <row>
                    <cell><m>-4</m></cell>
                    <cell><m>-3000</m></cell>
                </row>
                <row>
                    <cell><m>-3</m></cell>
                    <cell><m>-1000</m></cell>
                </row>
                <row>
                    <cell><m>-2</m></cell>
                    <cell><m>1000</m></cell>
                </row>
                <row>
                    <cell><m>-1</m></cell>
                    <cell><m>3000</m></cell>
                </row>
                <row>
                    <cell><m>0</m></cell>
                    <cell><m>5000</m></cell>
                </row>
                <row>
                    <cell><m>1</m></cell>
                    <cell><m>7000</m></cell>
                </row>
                <row>
                    <cell><m>2</m></cell>
                    <cell><m>9000</m></cell>
                </row>
                <row>
                    <cell><m>3</m></cell>
                    <cell><m>11000</m></cell>
                </row>
                <row>
                    <cell><m>4</m></cell>
                    <cell><m>13000</m></cell>
                </row>
                <row>
                    <cell><m>5</m></cell>
                    <cell><m>15000</m></cell>
                </row>
            </tabular>
        </table>
        <figure xml:id="figure-graph-with-appropriate-window"  permid="ngK">
                <caption>Graphed with an appropriate window</caption>
                <image valign="middle" permid="KSw">
                    <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-5,xmax=5,
                    ymin=-20,ymax=20,
                    xtick={-4,-3,...,4},
                    ytick={-15,-10,...,15},
                    yticklabels ={-15000,-10000,-5000,0,5000,10000,15000},
                    minor ytick={-20,-15,...,20},
                        ]
                        \addplot+[domain=-5:5,-] {2*x+5};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                </image>
            </figure>
        </sidebyside>
    </figure>

    <p permid="zJx">Now let's practice finding an appropriate viewing window with a less familiar function.</p>
    <example permid="Vys">
        <p permid="RyG">Find an appropriate window for <m>q(x)=\frac{x^3}{100}-2x+1</m>.</p>
        <sidebyside widths="47% 47%" margins="0%" permid="hMK">
            <p permid="xFP">Entering this function into graphing technology, we input <c>q(x)=(x^3)/100-2x+1</c>. A default window will generally give us something like this:</p>

            <figure xml:id="figure-function-in-default-window" permid="JUh">
                <caption>Function <m>q</m> graphed in the default window.</caption>
                <image permid="qZF">
                    <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-10,xmax=10,
                    ymin=-10,ymax=10,
                    xtick={-8,-6,...,8},
                    minor xtick={-9,-7,...,9},
                    ytick={-8,-6,...,8},
                    minor ytick={-10,-9,...,10},
                    minor xtick={-10,-9,...,10},
                        ]
                        \addplot+[domain=-10:10,-,samples=100] {(x^3)/100-2*x+1};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                </image>
            </figure>
        </sidebyside>

        <sidebyside widths="47% 47%" margins="0%" permid="NTT">
            <p permid="dMY">We can tell from the lower right corner of <xref ref="figure-function-in-default-window">Figure</xref> that we're not quite viewing all of the important details of this function. To determine a better window, we could use technology to make a table of values. Another more rudimentary option is to double the viewing constraints for <m>x</m> and <m>y</m>, as shown in <xref ref="figure-function-in-doubled-window">Figure</xref>. Many graphing technologies have the ability to zoom in and out quickly.</p>

            <figure xml:id="figure-function-in-doubled-window" permid="qbq">
                <caption>Function <m>q</m> graphed in an expanded window.</caption>
                <image permid="XgO">
                    <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-20,xmax=20,
                    ymin=-20,ymax=20,
                    xtick={-16,-12,...,16},
                    minor xtick={-20,-19,...,20},
                    ytick={-16,-12,...,16},
                    minor ytick={-20,-19,...,20}
                        ]
                        \addplot+[domain=-20:20,-,samples=100] {(x^3)/100-2*x+1};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                </image>
            </figure>
        </sidebyside>
    </example>
</subsection>

<subsection permid="vBU">
    <title>Using Technology to Determine Key Features of a Graph</title>

        <p permid="LXP">The key features of a graph can be determined using graphing technology. Here, we'll show how to determine the <m>x</m>-intercepts, <m>y</m>-intercepts, maximum/minimum values, and the domain and range using technology.</p>

        <example permid="ubc">
            <statement>
            <p permid="Wiz">Graph the function given by <m>p(x)=-1000x^2-100x+40</m>. Determine an appropriate viewing window, and then use graphing technology to determine the following:
                <ol label="a." permid="CZp">
                    <li permid="RcU"><p permid="TgC">Determine the <m>x</m>-intercepts of the function.</p></li>
                    <li permid="xkd"><p permid="znL">Determine the <m>y</m>-intercept of the function.</p></li>
                    <li permid="drm"><p permid="fuU">Determine the maximum function value and where it occurs.</p></li>
                    <li permid="Jyv"><p permid="LCd">State the domain and range of this function.</p></li>
                </ol>
            </p>
            </statement>
            <solution permid="DuO">
                <sidebyside widths="47% 47%" margins="0%" permid="Gpu">
                    <stack>
                        <p permid="IAt">To start, we'll take a quick view of this function in a default window. We can see that we need to zoom in on the <m>x</m>-values, but we need to zoom out on the <m>y</m>-values.</p>

                        <p permid="oHC">From the graph we see that the <m>x</m>-values might as well run from about <m>-0.5</m> to <m>0.5</m>, so we will look at <m>x</m>-values in that window in increments of <m>0.1</m>, as shown in <xref ref="table-values-for-an-appropriate-window-small-increments">Table</xref>. This table allows us to determine an appropriate viewing window for <m>y=p(x)</m> which is shown in <xref ref="figure-graph-with-appropriate-window-small-increments">Figure</xref>. The table suggests we should go a little higher than <m>40</m> on the <m>y</m>-axis, and it would be OK to go the same distance in the negative direction to keep the <m>x</m>-axis centered.</p>
                    </stack>

                    <figure xml:id="figure-graph-with-default-windwo-needs-small-increments" permid="hdd">
                        <caption>Graph of <m>y=p(x)</m> in an inappropriate window</caption>
                            <image permid="DnX">
                                <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-10,xmax=10,
                    ymin=-10,ymax=10,
                    xtick={-8,-6,...,8},
                    minor xtick={-10,-9.5,...,10},
                    ytick={-8,-6,...,8},
                    minor ytick={-10,-9.5,...,10}
                        ]
                        \addplot+[domain=-0.3:0.2,-] {-1000*x^2-100*x+40};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                            </image>
                    </figure>
                </sidebyside>

                <figure permid="Nkm">
                    <caption>Creating a table of values to determine an appropriate graphing window</caption>
                    <sidebyside widths="47% 47%" margins="0%" permid="mwD">
                    <table xml:id="table-values-for-an-appropriate-window-small-increments" permid="gAA">
                        <caption>Function values for <m>y=p(x)</m></caption>
                        <tabular permid="sOS">
                            <col halign="center" right="minor"/>
                            <col halign="center" />
                            <row bottom="medium">
                                <cell><m>x</m></cell>
                                <cell><m>p(x)</m></cell>
                            </row>
                            <row>
                                <cell><m>-0.5</m></cell>
                                <cell><m>-160</m></cell>
                            </row>
                            <row>
                                <cell><m>-0.4</m></cell>
                                <cell><m>-80</m></cell>
                            </row>
                            <row>
                                <cell><m>-0.3</m></cell>
                                <cell><m>-20</m></cell>
                            </row>
                            <row>
                                <cell><m>-0.2</m></cell>
                                <cell><m>20</m></cell>
                            </row>
                            <row>
                                <cell><m>-0.1</m></cell>
                                <cell><m>40</m></cell>
                            </row>
                            <row>
                                <cell><m>0</m></cell>
                                <cell><m>40</m></cell>
                            </row>
                            <row>
                                <cell><m>0.1</m></cell>
                                <cell><m>20</m></cell>
                            </row>
                            <row>
                                <cell><m>0.2</m></cell>
                                <cell><m>-20</m></cell>
                            </row>
                            <row>
                                <cell><m>0.3</m></cell>
                                <cell><m>-80</m></cell>
                            </row>
                            <row>
                                <cell><m>0.4</m></cell>
                                <cell><m>-160</m></cell>
                            </row>
                            <row>
                                <cell><m>0.5</m></cell>
                                <cell><m>-260</m></cell>
                            </row>
                        </tabular>
                    </table>
                    <figure xml:id="figure-graph-with-appropriate-window-small-increments" permid="TnT">
                            <caption>Graph of <m>y=p(x)</m> in an appropriate window showing key features</caption>
                            <image permid="jvg">
                                <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-0.5,xmax=0.5,
                    ymin=-60,ymax=60,
                    xtick={-0.4,-0.3,...,0.4},
                    minor xtick={-0.5,-0.4,...,0.6},
                    ytick={-40,-20,...,40},
                    minor ytick={-60,-50,...,60},
                    clip=false
                        ]
                        \addplot+[domain=-0.37:0.27,-] {-1000*x^2-100*x+40};
                        \addplot[soliddot] coordinates {(-0.2562,0)} node[above left]{$(-0.2562,0)$};
                        \addplot[soliddot] coordinates {(0.1562,0)} node[above right]{$(0.1562,0)$};
                        \addplot[soliddot] coordinates {(0,40)} node[above right]{$(0,40)$};
                        \addplot[soliddot] coordinates {(-0.05,42.5)} node[above left]{$(-0.05,42.5)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                            </image>
                        </figure>
                    </sidebyside>
                </figure>

                <p permid="UOL">We can now use <xref ref="figure-graph-with-appropriate-window-small-increments">Figure</xref> to determine the <m>x</m>-intercepts, the <m>y</m>-intercept, the maximum function value, and the domain and range.</p>

                <p permid="AVU"><ol label="a." permid="oVK">
                    <li permid="pFE"><p permid="rJm">To determine the <m>x</m>-intercepts, we will find the points where <m>y</m> is zero. These are about <m>(-0.2562,0)</m> and <m>(0.1562,0)</m>.</p></li>
                    <li permid="VMN"><p permid="XQv">To determine the <m>y</m>-intercept, we need the point where <m>x</m> is zero. This point is <m>(0,40)</m>.</p></li>
                    <li permid="BTW"><p permid="DXE">The highest point on the graph is the vertex, which is about <m>(-0.05,42.5)</m>. So the maximum function value is <m>42.5</m> and occurs at <m>-0.05</m>.</p></li>
                    <li permid="ibf"><p permid="keN">We can see that the function is defined for all <m>x</m>-values, so the domain is <m>(-\infty,\infty)</m>. The maximum function value is <m>42.5</m>, and there is no minimum function value. Thus the range is <m>(-\infty,42.5]</m>.</p></li>
                </ol></p>
            </solution>
        </example>

        <!--https://www.desmos.com/calculator/3doxbb3l89-->
        <example permid="ail">
            <sidebyside widths="47% 47%" margins="0%" permid="SDM">
                <p permid="CpI">If we use graphing technology to graph the function <m>g</m> where <m>g(x) = 0.0002x^2 + 0.00146x + 0.00266</m>, we may be mislead by the way values are rounded. Without technology, we know that this function is a quadratic function and therefore has at most two <m>x</m>-intercepts and has a vertex that will determine the minimum function value. However, using technology we could obtain a graph with the following key points:</p>
                <figure xml:id="figure-graph-with-rounding-issue" permid="uLj">
                    <caption>Misleading graph</caption>
                    <image permid="PCp">
                        <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-8,xmax=4,
                    ymin=-4, ymax=4,
                    xtick={1,2,3},
                    minor xtick={-8,-7,...,4},
                    ytick={-4,-2,...,4},
                    yticklabels={$-0.04$,$-0.02$,0,0.02,0.04},
                    minor ytick={-4,-3,...,4},
                    clip=false,
                        ]
                       \addplot+[domain=-8:4] {(0.0002*x^2 + 0.00146*x + 0.00266)*100};
                        \addplot[soliddot] coordinates {(-3.8,0)} node[below left]{$(-3.8,0)$};
                        \addplot[soliddot] coordinates {(-3.65,0)} node[above]{$(-3.65,0)$};
                        \addplot[soliddot] coordinates {(-3.5,0)} node[below right]{$(-3.5,0)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                    </image>
                </figure>
            </sidebyside>

            <p permid="iwR">This <em>looks</em> like there are three <m>x</m>-intercepts, which we know is not possible for a quadratic function. We can evaluate <m>g</m> at <m>x=-3.65</m> and determine that <m>g(-3.65)= -0.0000045</m>, which is <em>approximately</em> zero when rounded. So the true vertex of this function is <m>(-3.65,-0.0000045)</m>, and the minimum value of this function is <m>-0.0000045</m> (not zero).</p>

            <p permid="OEa">Every graphing tool generally has some type of limitation like this one, and it's good to be aware that these limitations exist.</p>
        </example>
    </subsection>

    <subsection permid="bJd">
        <title>Solving Equations and Inequalities Graphically Using Technology</title>
        <p permid="seY">To <em>algebraically</em> solve an equation like <m>h(x)=v(x)</m> for <md permid="NNc">
            <mrow>h(x)\amp=-0.01(x-90)(x+20)\amp\amp\text{and}\amp v(x)\amp=-0.04(x-10)(x-80)</mrow>
        </md>, we'd start by setting up <me permid="kAz">-0.01(x-90)(x+20)=-0.04(x-10)(x-80)</me>
        To solve this, we'd then simplify each side of the equation, set it equal to zero, and finally use the quadratic formula.</p>

        <p permid="Ymh">An alternative is to <em>graphically</em> solve this equation, which is done by graphing <md permid="tUl">
            <mrow>y\amp=-0.01(x-90)(x+20)\amp\amp\text{and}\amp y\amp=-0.04(x-10)(x-80)</mrow>
        </md>.</p>

        <sidebyside widths="47% 47%" margins="0%" permid="eSe">
            <figure xml:id="figure-graph-with-points-of-intersection-two-functions" permid="fCl">
                <caption>Points of intersection for <m>h(x)=v(x)</m></caption>
                <image permid="vJy">
                    <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-60,xmax=100,
                    ymin=-40,ymax=60,
                    xtick={-40,-20,...,80},
                    minor xtick={-50,-40,...,90},
                    ytick={-30,-20,...,50},
                    minor ytick={-30,-20,...,50},
                    clip=false,
                        ]
                        \addplot+[domain=-48.815:100,samples=100] {-0.01*(x-90)*(x+20)};
                        \addplot+[domain=-2.17:92.17,samples=100] {-0.04*(x-10)*(x-80)};
                        \addplot[soliddot, color=fourthcolor] coordinates {(22.46,28.677)} node[above right]{$(22.46,28.677)$};
                        \addplot[soliddot, color=fifthcolor] coordinates {(74.207,14.878)} node[below left]{$(74.207,14.878)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                </image>
            </figure>

            <p permid="zvc">The points of intersection, <m>(22.46,28.677)</m> and <m>(74.207,14.878)</m>, show where these functions are equal.  This means that the <m>x</m>-values give the solutions to the equation <m>-0.01(x-90)(x+20)=-0.04(x-10)(x-80)</m>. So the solutions are approximately <m>22.46</m> and <m>74.207</m>, and the solution set is approximately <m>\{22.46, 74.207\}</m>.</p>
        </sidebyside>

        <p permid="Etq">Similarly, to <em>graphically</em> solve an equation like <m>h(x)=25</m> for <me permid="QHI">h(x)=-0.01(x-90)(x+20)</me>, we can graph <me permid="wOR">y=-0.01(x-90)(x+20)\qquad\text{and}\qquad y=25</me></p>

        <sidebyside widths="47% 47%" margins="0%" permid="KZn">
            <figure xml:id="figure-graph-with-points-of-intersection-constant-function" permid="rQD">
                <caption>Points of intersection for <m>h(x)=25</m></caption>
                <image width="47%" permid="bQH">
                    <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-60,xmax=100,
                    ymin=-40,ymax=60,
                    xtick={-40,-20,...,80},
                    minor xtick={-50,-40,...,90},
                    ytick={-30,-20,...,50},
                    minor ytick={-30,-20,...,50},
                    clip=false,
                        ]
                        \addplot+[domain=-48.815:100,samples=100] {-0.01*(x-90)*(x+20)};
                        \addplot+[domain=-60:100] {25};
                        \addplot[soliddot, color=fourthcolor] coordinates {(12.807,25)} node[above right, rotate=20]{$(12.807,25)$};
                        \addplot[soliddot, color=fifthcolor] coordinates {(57.913,25)} node[above right, rotate=20]{$(57.913,25)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                </image>
            </figure>

            <p permid="LJu">The points of intersection are <m>(12.807,25)</m> and <m>(57.913,25)</m>, which tells us that the solutions to <m>h(x)=25</m> are approximately <m>12.807</m> and <m>57.913</m>. The solution set is approximately <m>\{12.807, 57.913\}</m>.</p>
        </sidebyside>

        <example permid="yKV">
            <statement>
                <p permid="GZB">Use graphing technology to solve the following inequalities:
                    <ol label="a." cols="2" permid="jgy">
                        <li permid="Oio"><p permid="QlW"><m>-20t^2-70t+300 \geq -5t+300</m></p></li>
                        <li permid="upx"><p permid="cAo"><m>-20t^2-70t+300 \lt -5t+300</m></p></li>
                    </ol>
                </p>
            </statement>
            <solution permid="jBX">
                <p permid="trv">To solve these inequalities graphically, we will start by graphing the equations <m>y=-20t^2-70t+300</m> and <m>y=-5t+300</m> and determining the points of intersection:</p>

                <figure xml:id="figure-solve-inequality-graphically-setup" permid="FFN">
                    <caption>Points of intersection for <m>y=-20t^2-70t+300</m> and <m>y=-5t+300</m></caption>
                    <image width="47%" permid="HXQ">
                        <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-8,xmax=8,
                    ymin=-100,ymax=400,
                    xtick={-6,-4,...,6},
                    minor xtick={-7,-6,...,7},
                    ytick={-100,0,...,300},
                    minor ytick={-50,0,...,350},
                    clip=false,
                        ]
                        \addplot+[domain=-6.55:3.05] {-20*x^2-70*x+300};
                        \addplot+[domain=-8:8] {-5*x+300};
                        \addplot[soliddot, color=fourthcolor] coordinates {(-3.25,316.25)} node[above right]{$(-3.25,316.25)$};
                        \addplot[soliddot, color=fifthcolor] coordinates {(0,300)} node[below left]{$(0,300)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                    </image>
                </figure>
                <p permid="ZyE"><ol label="a." permid="VcT">
                    <li permid="awG">
                        <p permid="IHx">To solve <m>-20t^2-70t+300 \geq -5t+300</m>, we need to determine where the <m>y</m>-values of the graph of <m>y=-20t^2-70t+300</m> are <em>greater</em> than the <m>y</m>-values of the graph of <m>y=-5t+300</m> in addition to the values where the <m>y</m>-values are equal. This region is highlighted in <xref ref="figure-solve-inequality-graphically-geq">Figure</xref>.</p>

                        <figure xml:id="figure-solve-inequality-graphically-geq" permid="UVP">
                            <caption></caption>
                            <image width="47%" permid="oeZ">
                                <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-8,xmax=8,
                    ymin=-100,ymax=400,
                    xtick={-6,-4,...,6},
                    minor xtick={-7,-6,...,7},
                    ytick={-100,0,...,300},
                    minor ytick={-50,0,...,350},
                    xlabel={$t$},
                    clip=false,
                        ]
                        \addplot+[domain=-6.55:3.05] {-20*x^2-70*x+300};
                        \addplot+[domain=-8:8] {-5*x+300};
                        \addplot[highlightcurve,domain=-3.25:0] {-20*x^2-70*x+300};
                        \addplot[closedinterval,color=fourthcolor] coordinates {(-3.25,0) (0,0)};
                        \addplot[soliddot, color=fourthcolor] coordinates {(-3.25,316.25)} node[above right]{$(-3.25,316.25)$};
                        \addplot[soliddot, color=fifthcolor] coordinates {(0,300)} node[below left]{$(0,300)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                            </image>
                        </figure>

                        <p permid="oOG">We can see that this region includes all values of <m>t</m> between, and including, <m>t=-3.25</m> and <m>t=0</m>. So the solutions to this inequality include all values of <m>t</m> for which <m>-3.25\le t \le 0</m>. We can write this solution set in interval notation as <m>[-3.25,0]</m> or in set-builder notation as <m>\{t\mid -3.25 \leq t \leq 0\}</m>.</p>
                    </li>
                    <li permid="GDP">
                        <p permid="BcY">To now solve <m>-20t^2-70t+300 \lt -5t+300</m>, we will need to determine where the <m>y</m>-values of the graph of <m>y=-20t^2-70t+300</m> are <em>less than</em> the <m>y</m>-values of the graph of <m>y=-5t+300</m>. This region is highlighted in <xref ref="figure-solve-inequality-graphically-lt">Figure</xref>.</p>

                        <figure xml:id="figure-solve-inequality-graphically-lt" permid="Nrq">
                            <caption></caption>
                            <image width="47%" permid="Umi">
                                <description></description>
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-8,xmax=8,
                    ymin=-100,ymax=400,
                    xtick={-6,-4,...,6},
                    minor xtick={-7,-6,...,7},
                    ytick={-100,0,...,300},
                    minor ytick={-50,0,...,350},
                    xlabel={$t$},
                    clip=false,
                        ]
                        \addplot+[domain=-6.55:3.05] {-20*x^2-70*x+300};
                        \addplot+[domain=-8:8] {-5*x+300};
                        \addplot[highlightcurve,domain=-6.55:-3.25] {-20*x^2-70*x+300};
                        \addplot[highlightcurve,domain=0:3.05] {-20*x^2-70*x+300};
                        \addplot[infiniteopeninterval,color=fourthcolor] coordinates {(-8,0) (-3.25,0)};
                        \addplot[openinfiniteinterval,color=fourthcolor] coordinates {(0,0) (8,0)};
                        \addplot[hollowdot, color=fourthcolor, fill=yellow!30] coordinates {(-3.25,316.25)} node[above right]{$(-3.25,316.25)$};
                        \addplot[hollowdot, color=fifthcolor, fill=yellow!30] coordinates {(0,300)} node[below left]{$(0,300)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                            </image>
                        </figure>
                        <p permid="hkh">We can see that <m>-20t^2-70t+300 \lt -5t+300</m> for all values of <m>t</m> where <m>t\lt -3.25</m> or <m>t\gt 0</m>. We can write this solution set in interval notation as <m>(-\infty,-3.25)\cup(0,\infty)</m> or in set-builder notation as <m>\{t\mid t\lt -3.25 \text{ or } t\gt 0\}</m>.</p>
                    </li>
                </ol></p>
            </solution>
        </example>

    </subsection>


    <exercises permid="HQm">
<!--TODO: add review and warmup, if appropriate ones exist-->
<!--         <exercisegroup cols="2">
            <title>Review and Warmup</title>
        </exercisegroup>
 --><exercisegroup cols="2" permid="nXv">
            <title>Using Technology to Create a Table of Function Values</title>
            <exercise permid="Ops">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   $a = non_zero_random(-4,4,1);
                   $b = non_zero_random(-19,19,1);
                   $c = non_zero_random(-5,5,1);
                } until (0 &lt; -$b/(2*$a)  and  -$b/(2*$a) &lt; 5);
                $formula = Compute("$a x^2 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="wAw">Use technology to make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.</p>

                        <sidebyside permid="sHo">
                            <tabular halign="center" permid="YWb">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </statement>
                    <solution permid="PJg">
                        <p permid="lMW">This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>, <m><var name="$x[1]" /></m>, <m><var name="$x[2]" /></m>, <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>, <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>. The choice to use these <m>x</m>-values is arbitrary. Since they are small numbers, they might make calculations easier. It's important to include some negative numbers.</p>

                        <p permid="RUf">One at a time, we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.</p>

                        <p permid="ybo"><md permid="abu">
                            <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                            <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                            <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                            <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                            <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                            <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                            <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                        </md></p>

                        <p permid="eix">So the table may be completed as:</p>

                        <sidebyside permid="YOx">
                            <tabular halign="center" permid="Fdk">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[0]" /></m></cell>
                                    <cell><m><var name="$y[0]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[1]" /></m></cell>
                                    <cell><m><var name="$y[1]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[2]" /></m></cell>
                                    <cell><m><var name="$y[2]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[3]" /></m></cell>
                                    <cell><m><var name="$y[3]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[4]" /></m></cell>
                                    <cell><m><var name="$y[4]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[5]" /></m></cell>
                                    <cell><m><var name="$y[5]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[6]" /></m></cell>
                                    <cell><m><var name="$y[6]" /></m></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </solution>
                </webwork>
            </exercise>
            <exercise permid="uwB">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   $a = non_zero_random(-4,4,1);
                   $b = non_zero_random(-19,19,1);
                   $c = non_zero_random(-5,5,1);
                } until (0 &lt; -$b/(2*$a)  and  -$b/(2*$a) &lt; 5);
                $formula = Compute("$a x^2 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="cHF">Use technology to make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.</p>

                        <sidebyside permid="EVG">
                            <tabular halign="center" permid="lkt">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </statement>
                    <solution permid="vQp">
                        <p permid="KpG">This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>, <m><var name="$x[1]" /></m>, <m><var name="$x[2]" /></m>, <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>, <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>. The choice to use these <m>x</m>-values is arbitrary. Since they are small numbers, they might make calculations easier. It's important to include some negative numbers.</p>

                        <p permid="qwP">One at a time, we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.</p>

                        <p permid="WDY"><md permid="GiD">
                            <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                            <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                            <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                            <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                            <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                            <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                            <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                        </md></p>

                        <p permid="CLh">So the table may be completed as:</p>

                        <sidebyside permid="lcP">
                            <tabular halign="center" permid="RrC">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[0]" /></m></cell>
                                    <cell><m><var name="$y[0]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[1]" /></m></cell>
                                    <cell><m><var name="$y[1]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[2]" /></m></cell>
                                    <cell><m><var name="$y[2]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[3]" /></m></cell>
                                    <cell><m><var name="$y[3]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[4]" /></m></cell>
                                    <cell><m><var name="$y[4]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[5]" /></m></cell>
                                    <cell><m><var name="$y[5]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[6]" /></m></cell>
                                    <cell><m><var name="$y[6]" /></m></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="aDK">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                   $a = non_zero_random(-4,4,0.05);
                   $b = non_zero_random(-200,200,10);
                   $c = non_zero_random(-100,100,1);
                $formula = Compute("$a x^2 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="IOO">Use technology to make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.</p>

                        <sidebyside permid="RjY">
                            <tabular halign="center" permid="xyL">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </statement>
                    <solution permid="bXy">
                        <p permid="iSq">This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>, <m><var name="$x[1]" /></m>, <m><var name="$x[2]" /></m>, <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>, <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>. The choice to use these <m>x</m>-values is arbitrary. Since they are small numbers, they might make calculations easier. It's important to include some negative numbers.</p>

                        <p permid="OZz">One at a time, we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.</p>

                        <p permid="vgI"><md permid="mpM">
                            <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                            <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                            <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                            <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                            <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                            <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                            <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                        </md></p>

                        <p permid="bnR">So the table may be completed as:</p>

                        <sidebyside permid="xrh">
                            <tabular halign="center" permid="dFU">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[0]" /></m></cell>
                                    <cell><m><var name="$y[0]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[1]" /></m></cell>
                                    <cell><m><var name="$y[1]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[2]" /></m></cell>
                                    <cell><m><var name="$y[2]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[3]" /></m></cell>
                                    <cell><m><var name="$y[3]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[4]" /></m></cell>
                                    <cell><m><var name="$y[4]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[5]" /></m></cell>
                                    <cell><m><var name="$y[5]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[6]" /></m></cell>
                                    <cell><m><var name="$y[6]" /></m></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </solution>
                </webwork>
            </exercise>
            <exercise permid="GKT">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                   $a = non_zero_random(-4,4,0.05);
                   $b = non_zero_random(-200,200,10);
                   $c = non_zero_random(-100,100,1);
                $formula = Compute("$a x^2 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="oVX">Use technology to make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.</p>

                        <sidebyside permid="dyq">
                            <tabular halign="center" permid="JNd">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </statement>
                    <solution permid="IeH">
                        <p permid="Hva">This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>, <m><var name="$x[1]" /></m>, <m><var name="$x[2]" /></m>, <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>, <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>. The choice to use these <m>x</m>-values is arbitrary. Since they are small numbers, they might make calculations easier. It's important to include some negative numbers.</p>

                        <p permid="nCj">One at a time, we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.</p>

                        <p permid="TJs"><md permid="SwV">
                            <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                            <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                            <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                            <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                            <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                            <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                            <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                        </md></p>

                        <p permid="zQB">So the table may be completed as:</p>

                        <sidebyside permid="JFz">
                            <tabular halign="center" permid="pUm">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[0]" /></m></cell>
                                    <cell><m><var name="$y[0]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[1]" /></m></cell>
                                    <cell><m><var name="$y[1]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[2]" /></m></cell>
                                    <cell><m><var name="$y[2]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[3]" /></m></cell>
                                    <cell><m><var name="$y[3]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[4]" /></m></cell>
                                    <cell><m><var name="$y[4]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[5]" /></m></cell>
                                    <cell><m><var name="$y[5]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[6]" /></m></cell>
                                    <cell><m><var name="$y[6]" /></m></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="mSc">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                   $a = non_zero_random(-12,12,1);
                   $b = non_zero_random(-200,200,10);
                   $c = non_zero_random(-100,100,1);
                $formula = Compute("$a x^3 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="Vdg">Use technology to make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.</p>

                        <sidebyside permid="pMI">
                            <tabular halign="center" permid="Wbv">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </statement>
                    <solution permid="olQ">
                        <p permid="fXK">This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>, <m><var name="$x[1]" /></m>, <m><var name="$x[2]" /></m>, <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>, <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>. The choice to use these <m>x</m>-values is arbitrary. Since they are small numbers, they might make calculations easier. It's important to include some negative numbers.</p>

                        <p permid="MeT">One at a time, we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.</p>

                        <p permid="smc"><md permid="yEe">
                            <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                            <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                            <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                            <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                            <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                            <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                            <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                        </md></p>

                        <p permid="Ytl">So the table may be completed as:</p>

                        <sidebyside permid="VTR">
                            <tabular halign="center" permid="CiE">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[0]" /></m></cell>
                                    <cell><m><var name="$y[0]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[1]" /></m></cell>
                                    <cell><m><var name="$y[1]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[2]" /></m></cell>
                                    <cell><m><var name="$y[2]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[3]" /></m></cell>
                                    <cell><m><var name="$y[3]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[4]" /></m></cell>
                                    <cell><m><var name="$y[4]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[5]" /></m></cell>
                                    <cell><m><var name="$y[5]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[6]" /></m></cell>
                                    <cell><m><var name="$y[6]" /></m></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </solution>
                </webwork>
            </exercise>
            <exercise permid="SZl">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                   $a = non_zero_random(-12,12,1);
                   $b = non_zero_random(-200,200,10);
                   $c = non_zero_random(-100,100,1);
                $formula = Compute("$a x^3 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="Bkp">Use technology to make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.</p>

                        <sidebyside permid="Cba">
                            <tabular halign="center" permid="ipN">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                                <row>
                                    <cell><var name="$multians" width="8" /></cell>
                                    <cell><var name="$multians" width="8" /></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </statement>
                    <solution permid="UsZ">
                        <p permid="EAu">This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>, <m><var name="$x[1]" /></m>, <m><var name="$x[2]" /></m>, <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>, <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>. The choice to use these <m>x</m>-values is arbitrary. Since they are small numbers, they might make calculations easier. It's important to include some negative numbers.</p>

                        <p permid="kHD">One at a time, we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.</p>

                        <p permid="QOM"><md permid="eLn">
                            <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                            <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                            <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                            <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                            <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                            <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                            <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                        </md></p>

                        <p permid="wVV">So the table may be completed as:</p>

                        <sidebyside permid="iij">
                            <tabular halign="center" permid="OwW">
                                <row bottom="medium">
                                    <cell><m>x</m></cell>
                                    <cell><m><var name="$f" />(x)</m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[0]" /></m></cell>
                                    <cell><m><var name="$y[0]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[1]" /></m></cell>
                                    <cell><m><var name="$y[1]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[2]" /></m></cell>
                                    <cell><m><var name="$y[2]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[3]" /></m></cell>
                                    <cell><m><var name="$y[3]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[4]" /></m></cell>
                                    <cell><m><var name="$y[4]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[5]" /></m></cell>
                                    <cell><m><var name="$y[5]" /></m></cell>
                                </row>
                                <row>
                                    <cell><m><var name="$x[6]" /></m></cell>
                                    <cell><m><var name="$y[6]" /></m></cell>
                                </row>
                            </tabular>
                        </sidebyside>
                    </solution>
                </webwork>
            </exercise>
        </exercisegroup>

        <exercisegroup cols="2" permid="UeE">
            <title>Determining Appropriate Windows</title>
            <exercise permid="zgu">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $m = random(1001,9999,1)*random(-1,1,2);
                $b = random(1001,9999,1)*random(-1,1,2);
                $func = Formula("$m x + $b")->reduce;
                #important values to appear in plot
                @x = (0,-$b/$m);
                @y = (0,$b);
                #one good answer
                $xbound = 10**ceil(log(abs(-$b/$m))/log(10));
                $ybound = 10**ceil(log(abs($b))/log(10));
                $x = Interval("[-$xbound,$xbound]");
                $y = Interval("[-$ybound,$ybound]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #Check important values are all present;
                    for my $xval (@x) {
                      if (!$xstu->contains($xval)) {
                        Value::Error('Your x-range is not large enough');
                      }
                    }
                    for my $yval (@y) {
                      if (!$ystu->contains($yval)) {
                        Value::Error('Your y-range is not large enough');
                      }
                    }
                    #Check important values are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $xval (@x) {
                      if (exists($xbounds{$xval})) {
                        Value::Error('An important x-value is right on the edge of your window');
                      }
                    }
                    for my $yval (@y) {
                      if (exists($ybounds{$yval})) {
                        Value::Error('An important y-value is right on the edge of your window');
                      }
                    }
                    #Check important x-values are not visually too close together;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    for my $xval1 (@x) {
                      for my $xval2 (@x) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your x-range makes important x-values appear too close together');
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    @xentry=(($ymin-$b)/$m, ($ymax-$b)/$m);
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="hry">Choose an appropriate window for graphing the function <m>f</m> defined by <m>f(x)=<var name="$func"/></m> that shows its key features.</p>
                        <instruction>Use interval notation. Many answers are possible.</instruction>
                        <p permid="NyH">The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.</p>
                    </statement>
                </webwork>
            </exercise>
            <exercise permid="fnD">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $m = random(10,999,1)*random(-1,1,2);
                $b = random(10,999,1)*random(-1,1,2);
                $func = Formula("$m x + $b")->reduce;
                #important values to appear in plot
                @x = (0,-$b/$m);
                @y = (0,$b);
                #one good answer
                $xbound = 10**ceil(log(abs(-$b/$m))/log(10));
                $ybound = 10**ceil(log(abs($b))/log(10));
                $x = Interval("[-$xbound,$xbound]");
                $y = Interval("[-$ybound,$ybound]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #Check important values are all present;
                    for my $xval (@x) {
                      if (!$xstu->contains($xval)) {
                        Value::Error('Your x-range is not large enough');
                      }
                    }
                    for my $yval (@y) {
                      if (!$ystu->contains($yval)) {
                        Value::Error('Your y-range is not large enough');
                      }
                    }
                    #Check important values are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $xval (@x) {
                      if (exists($xbounds{$xval})) {
                        Value::Error('An important x-value is right on the edge of your window');
                      }
                    }
                    for my $yval (@y) {
                      if (exists($ybounds{$yval})) {
                        Value::Error('An important y-value is right on the edge of your window');
                      }
                    }
                    #Check important x-values are not visually too close together;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    for my $xval1 (@x) {
                      for my $xval2 (@x) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your x-range makes important x-values appear too close together');
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    @xentry=(($ymin-$b)/$m, ($ymax-$b)/$m);
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="tFQ">Choose an appropriate window for graphing the function <m>f</m> defined by <m>f(x)=<var name="$func"/></m> that shows its key features.</p>
                        <instruction>Use interval notation. Many answers are possible.</instruction>
                        <p permid="ZMZ">The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.</p>
                    </statement>
                </webwork>
            </exercise>

            <exercise permid="LuM">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = random(10,999,1)*random(-1,1,2);
                $b = random(10,999,1)*random(-1,1,2);
                $c = random(100,9999,1)*random(-1,1,2);
                $func = Formula("$a x^2 + $b x + $c")->reduce;
                #important values to appear in plot
                @x = (0,-$b/(2*$a));
                if ($b**2 - 4*$a*$c >= 0) {
                  push(@x,(-$b-sqrt($b**2 - 4*$a*$c))/(2*$a));
                  push(@x,(-$b+sqrt($b**2 - 4*$a*$c))/(2*$a));
                }
                @y = ();
                for my $xval (@x) {
                  push(@y,$func->eval(x=>$xval));
                }
                #one good answer
                $xbound = 10**ceil(log( max(map {abs($_)} (@x)) )/log(10));
                $ybound = 10**ceil(log( max(map {abs($_)} (@y)) )/log(10));
                $x = Interval("[-$xbound,$xbound]");
                $y = Interval("[-$ybound,$ybound]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #Check important values are all present;
                    for my $xval (@x) {
                      if (!$xstu->contains($xval)) {
                        Value::Error('Your x-range is not large enough');
                      }
                    }
                    for my $yval (@y) {
                      if (!$ystu->contains($yval)) {
                        Value::Error('Your y-range is not large enough');
                      }
                    }
                    #Check important values are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $xval (@x) {
                      if (exists($xbounds{$xval})) {
                        Value::Error('An important x-value is right on the edge of your window');
                      }
                    }
                    for my $yval (@y) {
                      if (exists($ybounds{$yval})) {
                        Value::Error('An important y-value is right on the edge of your window');
                      }
                    }
                    #Check important x-values are not visually too close together;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    for my $xval1 (@x) {
                      for my $xval2 (@x) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your x-range makes important x-values appear too close together');
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation

                    if ($a > 0) {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a));
                    }
                    else {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a));
                    }
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="FUi">Choose an appropriate window for graphing the function <m>f</m> defined by <m>f(x)=<var name="$func"/></m> that shows its key features.</p>
                        <instruction>Use interval notation. Many answers are possible.</instruction>
                        <p permid="mbr">The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.</p>
                    </statement>
                </webwork>
            </exercise>
            <exercise permid="rBV">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = random(10,999,1)*random(-1,1,2);
                $b = random(10,999,1)*random(-1,1,2);
                $c = random(100,9999,1)*random(-1,1,2);
                $func = Formula("$a x^2 + $b x + $c")->reduce;
                #important values to appear in plot
                @x = (0,-$b/(2*$a));
                if ($b**2 - 4*$a*$c >= 0) {
                  push(@x,(-$b-sqrt($b**2 - 4*$a*$c))/(2*$a));
                  push(@x,(-$b+sqrt($b**2 - 4*$a*$c))/(2*$a));
                }
                @y = ();
                for my $xval (@x) {
                  push(@y,$func->eval(x=>$xval));
                }
                #one good answer
                $xbound = 10**ceil(log( max(map {abs($_)} (@x)) )/log(10));
                $ybound = 10**ceil(log( max(map {abs($_)} (@y)) )/log(10));
                $x = Interval("[-$xbound,$xbound]");
                $y = Interval("[-$ybound,$ybound]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #Check important values are all present;
                    for my $xval (@x) {
                      if (!$xstu->contains($xval)) {
                        Value::Error('Your x-range is not large enough');
                      }
                    }
                    for my $yval (@y) {
                      if (!$ystu->contains($yval)) {
                        Value::Error('Your y-range is not large enough');
                      }
                    }
                    #Check important values are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $xval (@x) {
                      if (exists($xbounds{$xval})) {
                        Value::Error('An important x-value is right on the edge of your window');
                      }
                    }
                    for my $yval (@y) {
                      if (exists($ybounds{$yval})) {
                        Value::Error('An important y-value is right on the edge of your window');
                      }
                    }
                    #Check important x-values are not visually too close together;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    for my $xval1 (@x) {
                      for my $xval2 (@x) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your x-range makes important x-values appear too close together');
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation

                    if ($a > 0) {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a));
                    }
                    else {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a));
                    }
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="SiA">Choose an appropriate window for graphing the function <m>f</m> defined by <m>f(x)=<var name="$func"/></m> that shows its key features.</p>
                        <instruction>Use interval notation. Many answers are possible.</instruction>
                        <p permid="ypJ">The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.</p>
                    </statement>
                </webwork>
            </exercise>

            <exercise permid="XJe">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = random(0.0001,0.0005,0.00001)*random(-1,1,2);
                $b = random(0.001,0.005,0.0001)*random(-1,1,2);
                $c = random(0.1,0.9,0.01)*random(-1,1,2);
                $func = Formula("$a x^2 + $b x + $c")->reduce;
                #important values to appear in plot
                @x = (0,-$b/(2*$a));
                if ($b**2 - 4*$a*$c >= 0) {
                  push(@x,(-$b-sqrt($b**2 - 4*$a*$c))/(2*$a));
                  push(@x,(-$b+sqrt($b**2 - 4*$a*$c))/(2*$a));
                }
                @y = ();
                for my $xval (@x) {
                  push(@y,$func->eval(x=>$xval));
                }
                #one good answer
                $xbound = 10**ceil(log( max(map {abs($_)} (@x)) )/log(10));
                $ybound = 10**ceil(log( max(map {abs($_)} (@y)) )/log(10));
                $x = Interval("[-$xbound,$xbound]");
                $y = Interval("[-$ybound,$ybound]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #Check important values are all present;
                    for my $xval (@x) {
                      if (!$xstu->contains($xval)) {
                        Value::Error('Your x-range is not large enough');
                      }
                    }
                    for my $yval (@y) {
                      if (!$ystu->contains($yval)) {
                        Value::Error('Your y-range is not large enough');
                      }
                    }
                    #Check important values are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $xval (@x) {
                      if (exists($xbounds{$xval})) {
                        Value::Error('An important x-value is right on the edge of your window');
                      }
                    }
                    for my $yval (@y) {
                      if (exists($ybounds{$yval})) {
                        Value::Error('An important y-value is right on the edge of your window');
                      }
                    }
                    #Check important x-values are not visually too close together;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    for my $xval1 (@x) {
                      for my $xval2 (@x) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your x-range makes important x-values appear too close together');
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation

                    if ($a > 0) {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a));
                    }
                    else {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a));
                    }
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="ewS">Choose an appropriate window for graphing the function <m>f</m> defined by <m>f(x)=<var name="$func"/></m> that shows its key features.</p>
                        <instruction>Use interval notation. Many answers are possible.</instruction>
                        <p permid="KEb">The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.</p>
                    </statement>
                </webwork>
            </exercise>
            <exercise permid="DQn">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = random(0.0001,0.0005,0.00001)*random(-1,1,2);
                $b = random(0.001,0.005,0.0001)*random(-1,1,2);
                $c = random(0.1,0.9,0.01)*random(-1,1,2);
                $func = Formula("$a x^2 + $b x + $c")->reduce;
                #important values to appear in plot
                @x = (0,-$b/(2*$a));
                if ($b**2 - 4*$a*$c >= 0) {
                  push(@x,(-$b-sqrt($b**2 - 4*$a*$c))/(2*$a));
                  push(@x,(-$b+sqrt($b**2 - 4*$a*$c))/(2*$a));
                }
                @y = ();
                for my $xval (@x) {
                  push(@y,$func->eval(x=>$xval));
                }
                #one good answer
                $xbound = 10**ceil(log( max(map {abs($_)} (@x)) )/log(10));
                $ybound = 10**ceil(log( max(map {abs($_)} (@y)) )/log(10));
                $x = Interval("[-$xbound,$xbound]");
                $y = Interval("[-$ybound,$ybound]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #Check important values are all present;
                    for my $xval (@x) {
                      if (!$xstu->contains($xval)) {
                        Value::Error('Your x-range is not large enough');
                      }
                    }
                    for my $yval (@y) {
                      if (!$ystu->contains($yval)) {
                        Value::Error('Your y-range is not large enough');
                      }
                    }
                    #Check important values are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $xval (@x) {
                      if (exists($xbounds{$xval})) {
                        Value::Error('An important x-value is right on the edge of your window');
                      }
                    }
                    for my $yval (@y) {
                      if (exists($ybounds{$yval})) {
                        Value::Error('An important y-value is right on the edge of your window');
                      }
                    }
                    #Check important x-values are not visually too close together;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    for my $xval1 (@x) {
                      for my $xval2 (@x) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your x-range makes important x-values appear too close together');
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation

                    if ($a > 0) {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a));
                    }
                    else {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a));
                    }
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="qLk">Choose an appropriate window for graphing the function <m>f</m> defined by <m>f(x)=<var name="$func"/></m> that shows its key features.</p>
                        <instruction>Use interval notation. Many answers are possible.</instruction>
                        <p permid="WSt">The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.</p>
                    </statement>
                </webwork>
            </exercise>
        </exercisegroup>

        <exercisegroup cols="2" permid="AlN">
            <title>Finding Points of Intersection</title>
            <exercise permid="jXw">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @m = (non_zero_random(-20,20,1), non_zero_random(-20,20,1));
                @b = (non_zero_random(-500,500,1), non_zero_random(-500,500,1));
                $c = non_zero_random(-20000,20000,1000);
                $f = Formula("($b[0]+$m[0]x)($b[1]+$m[1]x)")->reduce;
                $g = Formula("$c");
                $disc = ($m[0]*$b[1]+$m[1]*$b[0])**2 - 4*$m[0]*$m[1]*($b[0]*$b[1]-$c);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="CZC">Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect. They intersect <var name="$popup" form="popup" />.</p>
                    </statement>
                </webwork>
            </exercise>
            <exercise permid="QeF">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @m = (non_zero_random(-20,20,1), non_zero_random(-20,20,1));
                @b = (non_zero_random(-500,500,1), non_zero_random(-500,500,1));
                $c = non_zero_random(-20000,20000,1000);
                $f = Formula("($b[0]+$m[0]x)($b[1]+$m[1]x)")->reduce;
                $g = Formula("$c");
                $disc = ($m[0]*$b[1]+$m[1]*$b[0])**2 - 4*$m[0]*$m[1]*($b[0]*$b[1]-$c);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="jgL">Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect. They intersect <var name="$popup" form="popup" />.</p>
                    </statement>
                </webwork>
            </exercise>


            <exercise permid="wlO">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @a = (non_zero_random(-9,9,1), 0);
                @b = (random(-3,3,1), 0);
                @c = (non_zero_random(-9,9,1), non_zero_random(-9,9,1));
                @d = (0, non_zero_random(-9,9,1));
                $f = Formula("$a[0]x^3 + $b[0]x^2 + $c[0]x + $d[0]")->reduce;
                $g = Formula("$a[1]x^3 + $b[1]x^2 + $c[1]x + $d[1]")->reduce;

                $disc = ($b[0]-$b[1])**2*($c[0]-$c[1])**2 - 4*($a[0]-$a[1])*($c[0]-$c[1])**3 - 4*($b[0]-$b[1])**3*($d[0]-$d[1]) -27*($a[0]-$a[1])**2*($d[0]-$d[1])**2 + 18*($a[0]-$a[1])*($b[0]-$b[1])*($c[0]-$c[1])*($d[0]-$d[1]);
                if ($disc > 0) {$ans = 3;}
                elsif ($disc == 0) {
                    if (($g/$c[1])^3 == $f/$a[0]) {$ans = 1;}
                    else {$ans = 2;}
                }
                else {$ans = 1;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="PnU">Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect. They intersect <var name="$popup" form="popup" />.</p>
                    </statement>
                </webwork>
            </exercise>
            <exercise permid="csX">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @a = (non_zero_random(-9,9,1), 0);
                @b = (random(-3,3,1), 0);
                @c = (non_zero_random(-9,9,1), non_zero_random(-9,9,1));
                @d = (0, non_zero_random(-9,9,1));
                $f = Formula("$a[0]x^3 + $b[0]x^2 + $c[0]x + $d[0]")->reduce;
                $g = Formula("$a[1]x^3 + $b[1]x^2 + $c[1]x + $d[1]")->reduce;

                $disc = ($b[0]-$b[1])**2*($c[0]-$c[1])**2 - 4*($a[0]-$a[1])*($c[0]-$c[1])**3 - 4*($b[0]-$b[1])**3*($d[0]-$d[1]) -27*($a[0]-$a[1])**2*($d[0]-$d[1])**2 + 18*($a[0]-$a[1])*($b[0]-$b[1])*($c[0]-$c[1])*($d[0]-$d[1]);
                if ($disc > 0) {$ans = 3;}
                elsif ($disc == 0) {
                    if (($g/$c[1])^3 == $f/$a[0]) {$ans = 1;}
                    else {$ans = 2;}
                }
                else {$ans = 1;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="vvd">Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect. They intersect <var name="$popup" form="popup" />.</p>
                    </statement>
                </webwork>
            </exercise>

            <exercise permid="IAg">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @a = (random(1,9,1), 0);
                @b = (0, random(1,9,1));
                @c = (non_zero_random(-9,9,1), non_zero_random(-9,9,1));
                @s = (non_zero_random(-0.9,0.9,0.1), non_zero_random(-0.9,0.9,0.01));

                $f = Formula("$s[0]($a[0] x^2 + $b[0] x + $c[0])")->reduce;
                $g = Formula("$s[1]($a[1] x^2 + $b[1] x + $c[1])")->reduce;

                $disc = ($s[0]*$b[0]-$s[1]*$b[1])**2 - 4*($s[0]*$a[0]-$s[1]*$a[1])*($s[0]*$c[0]-$s[1]*$c[1]);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="bCm">Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect. They intersect <var name="$popup" form="popup" />.</p>
                    </statement>
                </webwork>
            </exercise>
            <exercise permid="oHp">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @a = (random(1,9,1), 0);
                @b = (0, random(1,9,1));
                @c = (non_zero_random(-9,9,1), non_zero_random(-9,9,1));
                @s = (non_zero_random(-0.9,0.9,0.1), non_zero_random(-0.9,0.9,0.01));

                $f = Formula("$s[0]($a[0] x^2 + $b[0] x + $c[0])")->reduce;
                $g = Formula("$s[1]($a[1] x^2 + $b[1] x + $c[1])")->reduce;

                $disc = ($s[0]*$b[0]-$s[1]*$b[1])**2 - 4*($s[0]*$a[0]-$s[1]*$a[1])*($s[0]*$c[0]-$s[1]*$c[1]);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="HJv">Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect. They intersect <var name="$popup" form="popup" />.</p>
                    </statement>
                </webwork>
            </exercise>

            <exercise permid="UOy">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = non_zero_random(-2,2,0.05);
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,0.05);
                $m = non_zero_random(-2,2,0.05);
                $b = random(-2,2,1);

                $f = Formula("$a(x - $h)^2+$k")->reduce;
                $g = Formula("$m x + $b")->reduce;

                $disc = (-2*$h*$a - $m)^2 - 4*$a*($a*$h**2+$k-$b);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="nQE">Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect. They intersect <var name="$popup" form="popup" />.</p>
                    </statement>
                </webwork>
            </exercise>
            <exercise permid="AVH">
                <webwork>
                    <setup>
            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = non_zero_random(-2,2,0.05);
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,0.05);
                $m = non_zero_random(-2,2,0.05);
                $b = random(-2,2,1);

                $f = Formula("$a(x - $h)^2+$k")->reduce;
                $g = Formula("$m x + $b")->reduce;

                $disc = (-2*$h*$a - $m)^2 - 4*$a*($a*$h**2+$k-$b);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="TXN">Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect. They intersect <var name="$popup" form="popup" />.</p>
                    </statement>
                </webwork>
            </exercise>
        </exercisegroup>

<!--TODO: All subsequent problems here do not use randomization. Make them use randomization. Update the problems in the chapter review when randomized. -->
        <exercisegroup cols="2" permid="gsW">
            <title>Using Technology to Find Key Features of a Graph</title>
            <exercise permid="hcQ">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intercepts = List("(-0.873,0), (6.873,0), (0,2.4)");
                $vertex = Point("(3,6)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("(-inf,6]");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="AeW">For the function <m>j</m> defined by <me permid="cWa">j(x)=-\frac{2}{5}(x-3)^2+6</me>, use technology to determine the following. Round answers as necessary.
                        <ol label="a.'" permid="PnH">
                            <li permid="mKY">
                                <p permid="tyz">Any intercepts.</p>
                                <p permid="ZFI"><var name="$intercepts" width="30" /></p>
                            </li>
                            <li permid="SSh">
                                <p permid="FMR">The vertex.</p>
                                <p permid="lUa"><var name="$vertex" width="30" /></p>
                            </li>
                            <li permid="yZq">
                                <p permid="Sbj">The domain.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="yis"><var name="$domain" width="30" /></p>
                            </li>
                            <li permid="fgz">
                                <p permid="epB">The range.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="KwK"><var name="$range" width="30" /></p>
                            </li>
                        </ol></p>
                    </statement>
                    <solution permid="AAi">
                        <p permid="dde"><ol label="a." permid="Bkc">
                            <li permid="LnI"><p permid="qDT">The horizontal intercepts are about <m>(-0.873,0)</m> and <m>(6.873,0)</m>. The vertical intercept is <m>(0,2.4)</m>.</p></li>
                            <li permid="ruR"><p permid="WLc">The vertex is <m>(3,6)</m>.</p></li>
                            <li permid="XCa"><p permid="CSl">The domain is <m>(-\infty,\infty)</m>. The range is <m>(-\infty,6]</m>.</p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="NjZ">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intercepts = List("(0,12)");
                $vertex = Point("(-1,10)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("[-10,inf)");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="gmf">For the function <m>k</m> defined by <me permid="Jdj">k(x)=2(x+1)^2+10</me>, use technology to determine the following. Round answers as necessary.
                        <ol label="a.'" permid="vuQ">
                            <li permid="DJj">
                                <p permid="iZu">Any intercepts.</p>
                                <p permid="PgD"><var name="$intercepts" width="30" /></p>
                            </li>
                            <li permid="jQs">
                                <p permid="vnM">The vertex.</p>
                                <p permid="buV"><var name="$vertex" width="30" /></p>
                            </li>
                            <li permid="PXB">
                                <p permid="HCe">The domain.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="nJn"><var name="$domain" width="30" /></p>
                            </li>
                            <li permid="weK">
                                <p permid="TQw">The range.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="zXF"><var name="$range" width="30" /></p>
                            </li>
                        </ol></p>
                    </statement>
                    <solution permid="gHr">
                        <p permid="Jkn"><ol label="a." permid="hrl">
                            <li permid="clT"><p permid="geO">There are no horizontal intercepts. The vertical intercept is <m>(0,12)</m>.</p></li>
                            <li permid="Itc"><p permid="MlX">The vertex is <m>(-1,10)</m>.</p></li>
                            <li permid="oAl"><p permid="stg">The domain is <m>(-\infty,\infty)</m>. The range is <m>[10,\infty)</m>.</p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="tri">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intercepts = List("(0,4)");
                $vertex = Point("(-0.002,3.992)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("[3.992,inf)");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="Mto">For the function <m>L</m> defined by <me permid="pks">L(x)=3000x^2+10x+4</me>, use technology to determine the following. Round answers as necessary.
                        <ol label="a.'" permid="bBZ">
                            <li permid="UHu">
                                <p permid="YAp">Any intercepts.</p>
                                <p permid="EHy"><var name="$intercepts" width="30" /></p>
                            </li>
                            <li permid="AOD">
                                <p permid="kOH">The vertex.</p>
                                <p permid="QVQ"><var name="$vertex" width="30" /></p>
                            </li>
                            <li permid="gVM">
                                <p permid="xcZ">The domain.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="dki"><var name="$domain" width="30" /></p>
                            </li>
                            <li permid="NcV">
                                <p permid="Jrr">The range.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="pyA"><var name="$range" width="30" /></p>
                            </li>
                        </ol></p>
                    </statement>
                    <solution permid="MOA">
                        <p permid="prw"><ol label="a." permid="Nyu">
                            <li permid="tke"><p permid="VFJ">There are no horizontal intercepts. The vertical intercept is <m>(0,4)</m>.</p></li>
                            <li permid="Zrn"><p permid="BMS">The vertex is about <m>(-0.002,3.992)</m>.</p></li>
                            <li permid="Fyw"><p permid="hUb">The domain is <m>(-\infty,\infty)</m>. The range is <m>[3.992,\infty)</m>.</p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="Zyr">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intercepts = List("(9.883,0), (0,-8702500)");
                $vertex = Point("(9.883,0)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("(-inf,0]");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="sAx">For the function <m>M</m> defined by <me permid="VrB">M(x)=-(300x-2950)^2</me>, use technology to determine the following. Round answers as necessary.
                        <ol label="a.'" permid="HJi">
                            <li permid="lFF">
                                <p permid="Obk">Any intercepts.</p>
                                <p permid="uit"><var name="$intercepts" width="30" /></p>
                            </li>
                            <li permid="RMO">
                                <p permid="apC">The vertex.</p>
                                <p permid="GwL"><var name="$vertex" width="30" /></p>
                            </li>
                            <li permid="xTX">
                                <p permid="mDU">The domain.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="SLd"><var name="$domain" width="30" /></p>
                            </li>
                            <li permid="ebg">
                                <p permid="ySm">The range.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="eZv"><var name="$range" width="30" /></p>
                            </li>
                        </ol></p>
                    </statement>
                    <solution permid="sVJ">
                        <p permid="VyF"><ol label="a." permid="tFD">
                            <li permid="Kip"><p permid="LgE">The horizontal intercept is about <m>(9.833,0)</m>. The vertical intercept is <m>(0,-8702500)</m>.</p></li>
                            <li permid="qpy"><p permid="rnN">The vertex is about <m>(9.833,0)</m>.</p></li>
                            <li permid="WwH"><p permid="XuW">The domain is <m>(-\infty,\infty)</m>. The range is <m>(-\infty,0]</m>.</p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="FFA">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intercepts = List("(0.0035,0), (0,1.1025)");
                $vertex = Point("(0.0035,0)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("[0,inf)");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="YHG">For the function <m>N</m> defined by <me permid="ByK">N(x)=(300x-1.05)^2</me>, use technology to determine the following. Round answers as necessary.
                        <ol label="a.'" permid="nQr">
                            <li permid="CDQ">
                                <p permid="DCf">Any intercepts.</p>
                                <p permid="jJo"><var name="$intercepts" width="30" /></p>
                            </li>
                            <li permid="iKZ">
                                <p permid="PQx">The vertex.</p>
                                <p permid="vXG"><var name="$vertex" width="30" /></p>
                            </li>
                            <li permid="OSi">
                                <p permid="ceP">The domain.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="IlY"><var name="$domain" width="30" /></p>
                            </li>
                            <li permid="uZr">
                                <p permid="oth">The range.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="UAq"><var name="$range" width="30" /></p>
                            </li>
                        </ol></p>
                    </statement>
                    <solution permid="ZcS">
                        <p permid="BFO"><ol label="a." permid="ZMM">
                            <li permid="bgA"><p permid="AHz">The horizontal intercept is <m>(0.0035,0)</m>. The vertical intercept is <m>(0,1.1025)</m>.</p></li>
                            <li permid="HnJ"><p permid="gOI">The vertex is <m>(0.0035,0)</m>.</p></li>
                            <li permid="nuS"><p permid="MVR">The domain is <m>(-\infty,\infty)</m>. The range is <m>[0,\infty)</m>.</p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="lMJ">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intercepts = List("(0.002,0), (0.003,0), (0.0006)");
                $vertex = Point("(0.025,-0.000025)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("[-0.000025,inf)");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="EOP">For the function <m>B</m> defined by <me permid="hFT">B(x) = x^2 - 0.05 x + 0.0006</me>, use technology to determine the following. Round answers as necessary.
                        <ol label="a.'" permid="TXA">
                            <li permid="TCb">
                                <p permid="tda">Any intercepts.</p>
                                <p permid="Zkj"><var name="$intercepts" width="30" /></p>
                            </li>
                            <li permid="zJk">
                                <p permid="Frs">The vertex.</p>
                                <p permid="lyB"><var name="$vertex" width="30" /></p>
                            </li>
                            <li permid="fQt">
                                <p permid="RFK">The domain.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="xMT"><var name="$domain" width="30" /></p>
                            </li>
                            <li permid="LXC">
                                <p permid="dUc">The range.</p>
                                <instruction>Use interval notation.</instruction>
                                <p permid="Kbl"><var name="$range" width="30" /></p>
                            </li>
                        </ol></p>
                    </statement>
                    <solution permid="Fkb">
                        <p permid="hMX"><ol label="a." permid="FTV">
                            <li permid="seL"><p permid="qiu">The horizontal intercepts are <m>(0.002,0)</m> and <m>(0.003,0)</m>. The vertical intercept is <m>(0,0.0006)</m>.</p></li>
                            <li permid="YlU"><p permid="WpD">The vertex is <m>(0.025,-0.000025)</m>.</p></li>
                            <li permid="Etd"><p permid="CwM">The domain is <m>(-\infty,\infty)</m>. The range is <m>[-0.000025,\infty)</m>.</p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>
        </exercisegroup>

        <exercisegroup cols="2" permid="MAf">
            <title>Solving Equations and Inequalities Graphically Using Technology</title>
            <exercise permid="RTS">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intersections = List("(-10,50), (15,25)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-10,15}");
                Context("Interval");
                $ineq[0] = Interval("(-inf,-10)U(15,inf)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="kVY">Let <m>s(x)=\frac{1}{5}x^2-2x+10</m> and <m>t(x)=-x+40</m>. Use graphing technology to determine the following.
                        <ol label="a." permid="AeJ">
                            <li permid="kAm">
                                <p permid="iDV">What are the points of intersection for these two functions?</p>
                                <p permid="OLe"><var name="$intersections" width="30" /></p>
                            </li>
                            <li permid="QHv">
                                <p permid="uSn">Solve <m>s(x)=t(x)</m>.</p>
                                <p permid="aZw"><var name="$solutions" width="30" /></p>
                            </li>
                            <li permid="wOE">
                                <p permid="HgF">Solve <m>s(x)\gt t(x)</m>.</p>
                                <p permid="nnO"><var name="$ineq[0]" width="30" /></p>
                            </li>
                            <li permid="cVN">
                                <p permid="TuX">Solve <m>s(x)\leq t(x)</m>.</p>
                                <p permid="zCg"><var name="$ineq[1]" width="30" /></p>
                            </li>
                        </ol>
                        </p>
                    </statement>
                    <solution permid="lrk">
                        <p permid="NUg"><ol label="a." permid="mbe">
                            <li permid="JcW"><p permid="fJp"><m>(-10,-50)</m> and <m>(15,25)</m></p></li>
                            <li permid="pkf"><p permid="LQy"><m>\{-10,15\}</m></p></li>
                            <li permid="Vro"><p permid="rXH"><m>(-\infty,-10)\cup(15,\infty)</m></p></li>
                            <li permid="Byx"><p permid="YeQ"><m>[-10,15]</m></p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="ybb">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intersections = List("(-4,8), (20,32)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-4,20}");
                Context("Interval");
                $ineq[0] = Interval("(-inf,-4)U(20,inf)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="Rdh">Let <m>w(x)=\frac{1}{4}x^2-3x-8</m> and <m>m(x)=x+12</m>. Use graphing technology to determine the following.
                        <ol label="a." permid="glS">
                            <li permid="hFG">
                                <p permid="ElZ">What are the points of intersection for these two functions?</p>
                                <p permid="kti"><var name="$intersections" width="30" /></p>
                            </li>
                            <li permid="NMP">
                                <p permid="QAr">Solve <m>w(x)=m(x)</m>.</p>
                                <p permid="wHA"><var name="$solutions" width="30" /></p>
                            </li>
                            <li permid="tTY">
                                <p permid="cOJ">Solve <m>w(x)\gt m(x)</m>.</p>
                                <p permid="IVS"><var name="$ineq[0]" width="30" /></p>
                            </li>
                            <li permid="abh">
                                <p permid="pdb">Solve <m>w(x)\leq m(x)</m>.</p>
                                <p permid="Vkk"><var name="$ineq[1]" width="30" /></p>
                            </li>
                        </ol>
                        </p>
                    </statement>
                    <solution permid="Ryt">
                        <p permid="ubp"><ol label="a." permid="Sin">
                            <li permid="Giq"><p permid="Brt"><m>(-4,8)</m> and <m>(20,32)</m></p></li>
                            <li permid="mpz"><p permid="hyC"><m>\{-4,20\}</m></p></li>
                            <li permid="SwI"><p permid="NFL"><m>(-\infty,-4)\cup(20,\infty)</m></p></li>
                            <li permid="yDR"><p permid="tMU"><m>[-4,20]</m></p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="eik">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intersections = List("(-2,5), (0.75,5)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-2,0.75}");
                Context("Interval");
                $ineq[0] = Interval("(-2,0.75)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="xkq">Let <m>f(x)=4x^2+5x-1</m> and <m>g(x)=5</m>. Use graphing technology to determine the following.
                        <ol label="a." permid="Mtb">
                            <li permid="eLa">
                                <p permid="ZUd">What are the points of intersection for these two functions?</p>
                                <p permid="Gbm"><var name="$intersections" width="30" /></p>
                            </li>
                            <li permid="KSj">
                                <p permid="miv">Solve <m>f(x)=g(x)</m>.</p>
                                <p permid="SpE"><var name="$solutions" width="30" /></p>
                            </li>
                            <li permid="qZs">
                                <p permid="ywN">Solve <m>f(x)\lt g(x)</m>.</p>
                                <p permid="eDW"><var name="$ineq[0]" width="30" /></p>
                            </li>
                            <li permid="XgB">
                                <p permid="KLf">Solve <m>f(x)\geq g(x)</m>.</p>
                                <p permid="qSo"><var name="$ineq[1]" width="30" /></p>
                            </li>
                        </ol>
                        </p>
                    </statement>
                    <solution permid="xFC">
                        <p permid="aiy"><ol label="a." permid="ypw">
                            <li permid="DnK"><p permid="WZx"><m>(-2,5)</m> and <m>(0.75,5)</m></p></li>
                            <li permid="juT"><p permid="DgG"><m>\{-2,0.75\}</m></p></li>
                            <li permid="PCc"><p permid="jnP"><m>(-2,0.75)</m></p></li>
                            <li permid="vJl"><p permid="PuY"><m>(-\infty,-2]\cup[0.75,\infty)</m></p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="Kpt">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intersections = List("(-0.5,7), (1,7)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-0.5,1}");
                Context("Interval");
                $ineq[0] = Interval("(-0.5,1)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="drz">Let <m>p(x)=6x^2-3x+4</m> and <m>k(x)=7</m>. Use graphing technology to determine the following.
                        <ol label="a." permid="sAk">
                            <li permid="bQu">
                                <p permid="vCh">What are the points of intersection for these two functions?</p>
                                <p permid="bJq"><var name="$intersections" width="30" /></p>
                            </li>
                            <li permid="HXD">
                                <p permid="HQz">Solve <m>p(x)=k(x)</m>.</p>
                                <p permid="nXI"><var name="$solutions" width="30" /></p>
                            </li>
                            <li permid="oeM">
                                <p permid="UeR">Solve <m>p(x)\lt k(x)</m>.</p>
                                <p permid="Ama"><var name="$ineq[0]" width="30" /></p>
                            </li>
                            <li permid="UlV">
                                <p permid="gtj">Solve <m>p(x)\geq k(x)</m>.</p>
                                <p permid="MAs"><var name="$ineq[1]" width="30" /></p>
                            </li>
                        </ol>
                        </p>
                    </statement>
                    <solution permid="dML">
                        <p permid="GpH"><ol label="a." permid="ewF">
                            <li permid="Ate"><p permid="sHB"><m>(-0.5,7)</m> and <m>(1,7)</m></p></li>
                            <li permid="gAn"><p permid="YOK"><m>\{-0.5,1\}</m></p></li>
                            <li permid="MHw"><p permid="EVT"><m>(-0.5,1)</m></p></li>
                            <li permid="sOF"><p permid="ldc"><m>(-\infty,-0.5]\cup[1,\infty)</m></p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="qwC">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intersections = List("(-6,10), (-0.5,21)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-6,0.5}");
                Context("Interval");
                $ineq[0] = Interval("(-6,-0.5)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="JyI">Let <m>q(x)=-4x^2-24x+10</m> and <m>r(x)=2x+22</m>. Use graphing technology to determine the following.
                        <ol label="a." permid="YHt">
                            <li permid="YVO">
                                <p permid="Rkl">What are the points of intersection for these two functions?</p>
                                <p permid="xru"><var name="$intersections" width="30" /></p>
                            </li>
                            <li permid="FcX">
                                <p permid="dyD">Solve <m>q(x)=r(x)</m>.</p>
                                <p permid="JFM"><var name="$solutions" width="30" /></p>
                            </li>
                            <li permid="lkg">
                                <p permid="pMV">Solve <m>q(x)\gt r(x)</m>.</p>
                                <p permid="VUe"><var name="$ineq[0]" width="30" /></p>
                            </li>
                            <li permid="Rrp">
                                <p permid="Cbn">Solve <m>q(x)\leq r(x)</m>.</p>
                                <p permid="iiw"><var name="$ineq[1]" width="30" /></p>
                            </li>
                        </ol>
                        </p>
                    </statement>
                    <solution permid="JTU">
                        <p permid="mwQ"><ol label="a." permid="KDO">
                            <li permid="xyy"><p permid="OpF"><m>(-6,10)</m> and <m>(-0.5,21)</m></p></li>
                            <li permid="dFH"><p permid="uwO"><m>\{-6,-0.5\}</m></p></li>
                            <li permid="JMQ"><p permid="aDX"><m>(-6,-0.5)</m></p></li>
                            <li permid="pTZ"><p permid="GLg"><m>(-\infty,-6]\cup[-0.5,\infty)</m></p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="WDL">
                <webwork>
                    <setup>
            <pg-code>
                Context("Point");
                $intersections = List("(-1.2,-5.4), (1,-12)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-1.2,1}");
                Context("Interval");
                $ineq[0] = Interval("(-1.2,1)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="pFR">Let <m>h(x)=-10x^2-5x+3</m> and <m>j(x)=-3x-9</m>. Use graphing technology to determine the following.
                        <ol label="a." permid="EOC">
                            <li permid="Wbi">
                                <p permid="mSp">What are the points of intersection for these two functions?</p>
                                <p permid="SZy"><var name="$intersections" width="30" /></p>
                            </li>
                            <li permid="Cir">
                                <p permid="zgH">Solve <m>h(x)=j(x)</m>.</p>
                                <p permid="fnQ"><var name="$solutions" width="30" /></p>
                            </li>
                            <li permid="ipA">
                                <p permid="LuZ">Solve <m>h(x)\gt j(x)</m>.</p>
                                <p permid="rCi"><var name="$ineq[0]" width="30" /></p>
                            </li>
                            <li permid="OwJ">
                                <p permid="XJr">Solve <m>h(x)\leq j(x)</m>.</p>
                                <p permid="DQA"><var name="$ineq[1]" width="30" /></p>
                            </li>
                        </ol>
                        </p>
                    </statement>
                    <solution permid="qbd">
                        <p permid="SDZ"><ol label="a." permid="qKX">
                            <li permid="uDS"><p permid="jXJ"><m>(-1.2,-5.4)</m> and <m>(1,-12)</m></p></li>
                            <li permid="aLb"><p permid="QeS"><m>\{-1.2,1\}</m></p></li>
                            <li permid="GSk"><p permid="wmb"><m>(-1.2,1)</m></p></li>
                            <li permid="mZt"><p permid="ctk"><m>(-\infty,-1.2]\cup[1,\infty)</m></p></li>
                        </ol></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="CKU">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-3.25,2}");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="VNa">Use graphing technology to solve the equation <m>0.4x^2+0.5x-0.2=2.4</m>.</p>
                        <p permid="BUj"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="Wim">
                        <p permid="yLi"><m>\{-3.25,2\}</m></p>
                    </solution>
                </webwork>
            </exercise>


            <exercise permid="iSd">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-6,2}");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="ibs">Use graphing technology to solve the equation <m>-0.25x^2-2x+1.75=4.75</m>.</p>
                        <p permid="OiB"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="Cpv">
                        <p permid="eSr"><m>\{-6,2\}</m></p>
                    </solution>
                </webwork>
            </exercise>
            <exercise permid="OZm">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-10,20}");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="upK">Use graphing technology to solve the equation <m>(200+5x)(100-2x)=15000</m>.</p>
                        <p permid="awT"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="iwE">
                        <p permid="KZA"><m>\{-10,20\}</m></p>
                    </solution>
                </webwork>
            </exercise>
            <exercise permid="vgv">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{0,30}");
            </pg-code>
                    </setup>
                    <statement>
                        <p permid="GEc">Use graphing technology to solve the equation <m>\left(200-5x\right)\left(100+10x\right)=20000</m>. Approximate the solution(s) if necessary.</p>
                        <p permid="mLl"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="ODN">
                        <p permid="rgJ"><m>\{0,30\}</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="bnE">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-1.5,0,1.5}");
            </pg-code>
                    </setup>
                    <statement><!--3, cubic-->
                        <p permid="SSu">Use graphing technology to solve the equation <m>2x^3-5x+1=-\frac{1}{2}x+1</m>. Approximate the solution(s) if necessary.</p>
                        <p permid="yZD"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="uKW">
                        <p permid="XnS"><m>\{-1.5,0,1.5\}</m></p>
                    </solution>
                </webwork>
            </exercise>
            <exercise permid="HuN">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{2}");
            </pg-code>
                    </setup>
                    <statement><!--1, cubic-->
                        <p permid="fgM">Use graphing technology to solve the equation <m>-x^3+8x=-4x+16</m>. Approximate the solution(s) if necessary.</p>
                        <p permid="LnV"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="aSf">
                        <p permid="Dvb"><m>\{2\}</m></p>
                    </solution>
                </webwork>
            </exercise>
            <exercise permid="nBW">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-23.162,-16.838}");
            </pg-code>
                    </setup>
                    <statement><!--2-->
                        <p permid="rve">Use graphing technology to solve the equation <m>-0.05 x^2 - 2.03 x - 19.6=0.05 x^2 + 1.97 x + 19.4</m>. Approximate the solution(s) if necessary.</p>
                        <p permid="XCn"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="GZo">
                        <p permid="jCk"><m>\{x\mid x\approx -23.162, x\approx -16.838\}</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="TJf">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Compute("no solutions");
            </pg-code>
                    </setup>
                    <statement><!--0-->
                        <p permid="DJw">Use graphing technology to solve the equation <m>-0.02 x^2 + 1.97 x - 51.5=0.05\left(x-50\right)^2-0.03\left(x-50\right)</m>. Approximate the solution(s) if necessary.</p>
                        <p permid="jQF"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="ngx">
                        <p permid="PJt">No solution.</p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="zQo">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Compute("no solutions");
            </pg-code>
                    </setup>
                    <statement><!--0-->
                        <p permid="PXO">Use graphing technology to solve the equation <m>-200x^2+60x-55=-20x-40</m>. Approximate the solution(s) if necessary.</p>
                        <p permid="weX"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="TnG">
                        <p permid="vQC">No solution.</p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="fXx">
                <webwork>
                    <setup>
            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{0.094,0.706}");
            </pg-code>
                    </setup>
                    <statement><!--2-->
                        <p permid="cmg">Use graphing technology to solve the equation <m>150x^2-20x+50=100x+40</m>. Approximate the solution(s) if necessary.</p>
                        <p permid="Itp"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="zuP">
                        <p permid="bXL"><m>\{x \mid x\approx 0.094, x\approx 0.706\}</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="MeG">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,-2)U(-0.5,inf)");
            </pg-code>
                    </setup>
                    <statement><!--gt-->
                        <p permid="oAy">Use graphing technology to solve the inequality <m>2x^2+5x-3\gt -5</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="UHH"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="fBY">
                        <p permid="IeU"><m>(-\infty,-2)\cup(-0.5,\infty)</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="slP">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("(-1,5)");
            </pg-code>
                    </setup>
                    <statement><!--gt-->
                        <p permid="AOQ">Use graphing technology to solve the inequality <m>-x^2+4x-7 \gt -12</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="gVZ"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="LJh">
                        <p permid="omd"><m>(-1,5)</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="YsY">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("[0,1.1]");
            </pg-code>
                    </setup>
                    <statement><!--leq-->
                        <p permid="Ndi">Use graphing technology to solve the inequality <m>10x^2-11x+7\leq7</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="tkr"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="rQq">
                        <p permid="Utm"><m>[0,1.1]</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="EAh">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,-1]U[-0.5,inf)");
            </pg-code>
                    </setup>
                    <statement><!--leq-->
                        <p permid="ZrA">Use graphing technology to solve the inequality <m>-10x^2-15x+4\leq 9</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="FyJ"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="XXz">
                        <p permid="AAv"><m>(-\infty,-1]\cup[-0.5,\infty)</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="kHq">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("(-6.372,-0.628)");
            </pg-code>
                    </setup>
                    <statement><!--gt-->
                        <p permid="lFS">Use graphing technology to solve the inequality <m>-x^2-6x+1>x+5</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="RNb"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="EeI">
                        <p permid="gHE"><m>(-6.372,-0.628)</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="QOz">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,-2.907)U(0.573,inf)");
            </pg-code>
                    </setup>
                    <statement><!--gt-->
                        <p permid="xUk">Use graphing technology to solve the inequality <m>3x^2+5x-4>-2x+1</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="ebt"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="klR">
                        <p permid="MON"><m>(-\infty,-2.907)\cup(0.573,\infty)</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="wVI">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $a = 20;
                $b = -34 + 10;
                $c = 6 - 4;
                @r = ((-$b-sqrt(($b)**2-4*$a*$c))/(2*$a),(-$b+sqrt(($b)**2-4*$a*$c))/(2*$a));
                ($low,$high) = (min(@r),max(@r));
                $solutions = Interval("(-inf,$low]U[$high,inf)");
            </pg-code>
                    </setup>
                    <statement><!--leq-->
                        <p permid="KiC">Use graphing technology to solve the inequality <m>-10x+4\leq 20x^2-34x+6</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="qpL"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="Qta">
                        <p permid="sVW"><m>(-\infty,0.09]\cup[1.11,\infty)</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="dcR">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,inf)");
            </pg-code>
                    </setup>
                    <statement><!--lt arn-->
                        <p permid="WwU">Use graphing technology to solve the inequality <m>-15x^2-6\leq 10x-4</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="CEd"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="wAj">
                        <p permid="Zdf"><m>(-\infty,\infty)</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="Jka">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,inf)");
            </pg-code>
                    </setup>
                    <statement><!--gt arn-->
                        <p permid="iLm">Use graphing technology to solve the inequality <m>\frac{1}{2}x^2+\frac{3}{2}x \geq \frac{1}{2}x-\frac{3}{2}</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="OSv"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="cHs">
                        <p permid="Fko"><m>(-\infty,\infty)</m></p>
                    </solution>
                </webwork>
            </exercise>

            <exercise permid="prj">
                <webwork>
                    <setup>
            <pg-code>
                Context("Interval");
                $solutions = Interval("[0,15]");
            </pg-code>
                    </setup>
                    <statement><!--geq-->
                        <p permid="uZE">Use graphing technology to solve the inequality <m>\frac{3}{4}x\geq \frac{1}{4}x^2-3x</m>. State the solution set using interval notation, and approximate if necessary.</p>
                        <p permid="bgN"><var name="$solutions" width="30" /></p>
                    </statement>
                    <solution permid="IOB">
                        <p permid="lrx"><m>[0,15]</m></p>
                    </solution>
                </webwork>
            </exercise>
        </exercisegroup>
    </exercises>
</section>
