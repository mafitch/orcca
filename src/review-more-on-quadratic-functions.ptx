<?xml version="1.0" encoding="UTF-8" ?>
<!-- *********************************************************************-->
<!-- Copyright 2016-2018                                                  -->
<!-- Portland Community College                                           -->
<!--                                                                      -->
<!-- This file is part of Open Resources for Community College Algebra    -->
<!-- (ORCCA).                                                             -->
<!-- *********************************************************************-->
<section xml:id="review-more-on-quadratic-functions" permid="Qcn">
  <title>More on Quadratic Functions Chapter Review</title>
  <subsection permid="wjw">
    <title>Graphs and Vertex Form</title>
    <p permid="ePZ">
      In <xref ref="section-graphs-and-vertex-form">Section</xref>
      we covered the <xref ref="fact-vertex-form" text="title">vertex form</xref> of a quadratic function,
      how it can be used to identify the vertex,
      and how the vertex can be used to find the domain and range of a quadratic function.
      We also covered the <xref ref="fact-factored-form" text="title">factored form</xref>
      of a quadratic function and how it can help us identify horizontal intercepts.
    </p>
<!--  <example>
<title>Exploring Quadratic Functions with Graphing Technology</title>
<statement>
<p>Use technology to graph and make a table of the quadratic function <m>g</m> defined by <m>g(x)=-x^2+5x-6</m> and find each of the key points or features. <ol label="a." cols="2"> <li><p>Find the vertex.</p></li> <li><p>Find the vertical intercept.</p></li> <li><p>Find the horizontal intercept(s).</p></li> <li><p>Find <m>g(-1)</m>.</p></li> <li><p>Solve <m>g(x)=-6</m> using the graph.</p></li> <li><p>Solve <m>g(x)\le -6</m> using the graph.</p></li> <li><p>State the domain and range of the function.</p></li> </ol> </p>
</statement>
<solution>
<sidebyside widths="32% 15% 47%">
<p>The specifics of how to use any one particular technology tool vary. Whether you use an app, a physical calculator, or something else, a table and graph should look like:</p>
<tabular halign="right">
<row bottom="medium">
<cell><m>x</m></cell>
<cell><m>g(x)</m></cell>
</row>
<row>
<cell><m>-1</m></cell>
<cell><m>-12</m></cell>
</row>
<row>
<cell><m>0</m></cell>
<cell><m>-6</m></cell>
</row>
<row>
<cell><m>1</m></cell>
<cell><m>-2</m></cell>
</row>
<row>
<cell><m>2</m></cell>
<cell><m>0</m></cell>
</row>
<row>
<cell><m>2</m></cell>
<cell><m>0</m></cell>
</row>
<row>
<cell><m>3</m></cell>
<cell><m>0</m></cell>
</row>
<row>
<cell><m>4</m></cell>
<cell><m>-2</m></cell>
</row>
</tabular>
<image>
<description>the graph of the parabola g(x)=-x^2+5x-6, that opens downward, passing through the points listed in the table</description>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}[clip=false,
xmin=-2, xmax=7,
ymin=-10, ymax=2,
xtick={-1,0,...,6}, minor xtick={-2,-1,...,7},
ytick={-8,-6,...,-2}, minor ytick={-10,-9,...,2},
]
\addplot+[domain=-0.702:5.702] {-x^2+5*x-6};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
</image>
</sidebyside>
<p>Additional features of your technology tool can enhance the graph to help answer these questions. You may be able to make the graph appear like:</p>
<sidebyside widths="59%">
<image>
<description>the previous graph with all of the key points labeled as described below</description>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}[clip=false,
xmin=-2, xmax=7,
ymin=-10, ymax=2,
xtick={-1,0,...,6}, minor xtick={-2,-1,...,7},
ytick={-8,-6,...,-2}, minor ytick={-10,-9,...,2},
]
\addplot [domain=-0.702:5.702, primarycurve] {-x^2+5*x-6} node[left, pos=0.9] {$y=g(x)$};
\addplot [domain=-2:7, secondarycurve] {-6} node[above , pos=0.55] {$y=-6$};
\addplot [soliddot, color=thirdcolor] coordinates {(2.5,0.25)} node[above] {$(2.5,0.25)$};
\addplot [soliddot, color=fourthcolor] coordinates {(0,-6)} node[above left] {$(0,-6)$};
\addplot [soliddot, color=fourthcolor] coordinates {(5,-6)} node[above right] {$(5,-6)$};
\addplot [soliddot, color=fifthcolor] coordinates {(3,0)} node[right] {$(3,0)$};
\addplot [soliddot, color=fifthcolor] coordinates {(2,0)} node[left] {$(2,0)$};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
</image>
</sidebyside>
<p><ol label="a."> <li><p>The vertex is <m>(2.5,0.25)</m>.</p></li> <li><p>The vertical intercept is <m>(0,-6)</m>.</p></li> <li> <p>The horizontal intercepts are <m>(2,0)</m> and <m>(3,0)</m>.</p> </li> <li><p><m>g(-1)=-2</m>.</p></li> <li> <p>The solutions to <m>g(x)=-6</m> are the <m>x</m>-values where <m>y=6</m>. We graph the horizontal line <m>y=-6</m> and find the <m>x</m>-values where the graphs intersect. The solution set is <m>\{0,5\}</m>.</p> </li> <li> <p>The solutions are all <m>x</m>-values where the function below (or touching) the line <m>y=-6</m>. The solution set is <m>(-\infty,0]\cup[5,\infty)</m>.</p> </li> <li><p>The domain is <m>(-\infty,\infty)</m> and the range is <m>(-\infty,0.25]</m>.</p></li> </ol></p>
</solution>
</example> -->
    <example permid="kjz">
      <title>The Vertex Form of a Parabola</title>
      <statement>
        <p permid="jVr">
          Recall that the <xref ref="fact-vertex-form" text="title">vertex form</xref>
          of a quadratic function tells us the location of the vertex of a parabola.
          <ol label="a." cols="2" permid="DGR">
            <li permid="gTu">
              <p permid="PzK">
                State the vertex of the quadratic function
                <m>r(x)=-8(x+1)^2+7</m> and its domain and range.
              </p>
            </li>
            <li permid="NaD">
              <p permid="vGT">
                State the vertex of the quadratic function
                <m>u(x)=5(x-7)^2-3</m> and its domain and range.
              </p>
            </li>
            <li permid="thM">
              <p permid="bOc">
                Write the formula for a parabola with vertex <m>(-5,3)</m> and <m>a=2</m>.
              </p>
            </li>
            <li permid="ZoV">
              <p permid="HVl">
                Write the formula for a parabola with vertex <m>(1,-17)</m> and <m>a=-4</m>.
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="tDl">
        <p permid="WIK">
          <ol label="a." cols="2" permid="mIG">
            <li permid="Fwe">
              <p permid="ocu">
                The vertex of the quadratic function
                <m>r(x)=-8(x+\highlight{1})^2+\lighthigh{7}</m> is <m>(\highlight{-1},\lighthigh{7})</m>.
                The domain of <m>r</m> is <m>(-\infty,\infty)</m> and since the graph of <m>r</m> will be opening downward,
                the range of <m>r</m> is <m>(-\infty,7]</m>.
              </p>
            </li>
            <li permid="lDn">
              <p permid="UjD">
                The vertex of the quadratic function
                <m>u(x)=5(x-\highlight{7})^2\lighthigh{-3}</m> is <m>(\highlight{7},\lighthigh{-3})</m>.
                The domain of <m>u</m> is <m>(-\infty,\infty)</m> and since the graph of <m>u</m> will be opening upward,
                the range of <m>r</m> is <m>[-3,\infty)</m>.
              </p>
            </li>
            <li permid="RKw">
              <p permid="AqM">
                The formula for a parabola with vertex
                <m>(\highlight{-5},\lighthigh{3})</m> and <m>a=2</m> is <m>y=2(x+\highlight{5})^2+\lighthigh{3}</m>.
              </p>
            </li>
            <li permid="xRF">
              <p permid="gxV">
                The formula for a parabola with vertex
                <m>(\highlight{1},\lighthigh{-17})</m> and <m>a=-4</m> is <m>y=4(x-\highlight{1})^2\lighthigh{-17}</m>.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>
<!-- <example>
<title>Horizontal and Vertical Shifts</title>
<statement>
<p>Identify the horizontal and vertical shifts compared with <m>f(x)=x^2</m>. <ol label="a." cols="2"> <li><p><m>s(x)=(x+1)^2+7</m>.</p></li> <li><p><m>v(x)=(x-7)^2-3</m>.</p></li> </ol> </p>
</statement>
<solution>
<p> <ol label="a." cols="2"> <li><p> The graph of the quadratic function <m>s(x)=-8(x+1)^2+7</m> is the same as the graph of <m>f(x)=x^2</m> shifted to the left <m>1</m> unit and up <m>7</m> units.</p></li> <li><p> The graph of the quadratic function <m>v(x)=5(x-7)^2-3</m> is the same as the graph of <m>f(x)=x^2</m> shifted to the right <m>7</m> units and down <m>3</m> units.</p></li> </ol> </p>
</solution>
</example> -->
    <example permid="QqI">
      <title>The Factored Form of a Parabola</title>
      <statement>
        <p permid="QcA">
          Recall that the <xref ref="fact-factored-form" text="title">factored form</xref>
          of a quadratic function tells us the horizontal intercepts very quickly.
          <ol label="a." cols="2" permid="jOa">
            <li permid="dYO">
              <p permid="MFe">
                <m>n(x)=13(x-1)(x+6)</m>.
              </p>
            </li>
            <li permid="KfX">
              <p permid="sMn">
                <m>p(x)=-6(x-\frac{2}{3})(x+\frac{1}{2})</m>.
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="ZKu">
        <p permid="CPT">
          <ol label="a." permid="SPP">
            <li permid="qng">
              <p permid="YTw">
                The horizontal intercepts of <m>n</m> are <m>(1,0)</m> and <m>(-6,0)</m>.
              </p>
            </li>
            <li permid="Wup">
              <p permid="FaF">
                The horizontal intercepts of <m>p</m> are
                <m>(\frac{2}{3},0)</m> and <m>(-\frac{1}{2},0)</m>.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>
  </subsection>
  <subsection permid="cqF">
    <title>Completing the Square</title>
    <p permid="KXi">
      In <xref ref="section-completing-the-square">Section</xref>
      we covered how to <xref ref="fact-completing-the-square" text="title">complete the square</xref>
      to both solve quadratic equations in one variable and to put quadratic functions into <xref ref="fact-vertex-form" text="title">vertex form</xref>.
    </p>
    <example permid="wxR">
      <title>Solving Quadratic Equations by Completing the Square</title>
      <statement>
        <p permid="wjJ">
          Solve the equations by completing the square.
          <ol label="a." cols="2" permid="PVj">
            <li permid="CBy">
              <p permid="lhO">
                <m>k^2-18k+1=0</m>
              </p>
            </li>
            <li permid="iIH">
              <p permid="RoX">
                <m>4p^2-3p=2</m>
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="FRD">
        <p permid="iXc">
          <ol label="a." permid="yWY">
            <li permid="OPQ">
              <p permid="xwg">
                To complete the square in the equation <m>k^2-18k+1=0</m>,
                we first we will first move the constant term to the right side of the equation.
                Then we will use <xref ref="fact-completing-the-square">Fact</xref>
                to find <m>\left(\frac{b}{2}\right)^2</m> to add to both sides.
                <md permid="PHb">
                  <mrow>k^2-18k+1\amp=0</mrow>
                  <mrow>k^2-18k\amp=-1</mrow>
                </md>
                In our case, <m>b=-18</m>,
                so <m>\left(\frac{b}{2}\right)^2=\left(\frac{-18}{2}\right)^2=81</m>
                <md permid="vOk">
                  <mrow>k^2-18k\addright{81}\amp=-1\addright{81}</mrow>
                  <mrow>(k-9)^2\amp=80</mrow>
                </md>
                <md permid="bVt">
                  <mrow>k-9\amp=-\sqrt{80}\amp\text{ or }\amp\amp k-9\amp=\sqrt{80}</mrow>
                  <mrow>k-9\amp=-4\sqrt{5}\amp\text{ or }\amp\amp k-9\amp=4\sqrt{5}</mrow>
                  <mrow>k\amp=9-4\sqrt{5}\amp\text{ or }\amp\amp k\amp=9+4\sqrt{5}</mrow>
                </md>
                The solution set is <m>\{9+4\sqrt{5},9-4\sqrt{5}\}</m>.
              </p>
            </li>
            <li permid="uWZ">
              <p permid="dDp">
                To complete the square in the equation <m>4p^2-3p=2</m>,
                we first divide both sides by <m>4</m> since the leading coefficient is 4.
                <md permid="IcC">
                  <mrow>\divideunder{4p^2}{4}-\divideunder{3p}{4}\amp=\divideunder{2}{4}</mrow>
                  <mrow>p^2-\frac{3}{4}p\amp=\frac{1}{2}</mrow>
                  <mrow>p^2-\frac{3}{4}p\amp=\frac{1}{2}</mrow>
                </md>
                Next, we will complete the square.
                Since <m>b=-\frac{3}{4}</m>, first,
                <men xml:id="equation-completing-square-3-4ths-over-2" permid="DsJ">
                  \frac{b}{2}=\frac{-\frac{3}{4}}{2}=-\frac{3}{8}
                </men>
                and next, squaring that, we have
                <men xml:id="equation-completing-square-3-over-8" permid="jzS">
                  \left(-\frac{3}{8}\right)^2=\frac{9}{64}
                </men>.
                So we will add <m>\frac{9}{64}</m> from <xref ref="equation-completing-square-3-over-8">Equation</xref>
                to both sides of the equation:
                <md permid="ojL">
                  <mrow>p^2-\frac{3}{4}p\addright{\frac{9}{64}}\amp=\frac{1}{2}\addright{\frac{9}{64}}</mrow>
                  <mrow>p^2-\frac{3}{4}p+\frac{9}{64}\amp=\frac{32}{64}+\frac{9}{64}</mrow>
                  <mrow>p^2-\frac{3}{4}p+\frac{9}{64}\amp=\frac{41}{64}</mrow>
                </md>
                Here, remember that we always factor with the number found in the first step of completing the square,
                <xref ref="equation-completing-square-3-4ths-over-2">Equation</xref>.
                <md permid="UqU">
                  <mrow>\left(p-\frac{3}{8}\right)^2\amp=\frac{41}{64}</mrow>
                </md>
                <md permid="Ayd">
                  <mrow>p-\frac{3}{8}\amp=-\frac{\sqrt{41}}{8}\amp\text{ or }\amp\amp p-\frac{3}{8}\amp=\frac{\sqrt{41}}{8}</mrow>
                  <mrow>p\amp=\frac{3}{8}-\frac{\sqrt{41}}{8}\amp\text{ or }\amp\amp p\amp=\frac{3}{8}+\frac{\sqrt{41}}{8}</mrow>
                  <mrow>p\amp=\frac{3-\sqrt{41}}{8}\amp\text{ or }\amp\amp p\amp=\frac{3+\sqrt{41}}{8}</mrow>
                </md>
                The solution set is <m>\left\{\frac{3-\sqrt{41}}{8}, \frac{3+\sqrt{41}}{8}\right\}</m>.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>
<!--         <example>
<title>Deriving the Vertex Formula and the Quadratic Formula by Completing the Square</title>
<statement>
<p></p>
</statement>
<solution>
<p></p>
</solution>
</example> -->
    <example permid="cFa">
      <title>Putting Quadratic Functions in Vertex Form</title>
      <statement>
        <p permid="cqS">
          Write a formula in vertex form for the function <m>T</m> defined by <m>T(x)=4x^2+20x+24</m>.
        </p>
      </statement>
      <solution permid="lYM">
        <p permid="Pel">
          Before we can complete the square,
          we will factor the <m>\highlight{4}</m> out of the first two terms.
          Don't be tempted to factor the <m>4</m> out of the constant term.
          <md permid="gFm">
            <mrow>T(x)\amp=\highlight{4}\left(x^2+5x\right)+24</mrow>
          </md>
          Now we will complete the square inside the parentheses by adding and subtracting <m>\left(\frac{5}{2}\right)^2=\frac{25}{4}</m>.
          <md permid="MMv">
            <mrow>T(x)\amp=4\left(x^2+5x\addright{\frac{25}{4}}\subtractright{\frac{25}{4}}\right)+24</mrow>
          </md>
          Notice that the constant that we subtracted is inside the parentheses,
          but it will not be part of our perfect square trinomial.
          In order to bring it outside,
          we need to multiply it by <m>4</m>.
          We are distributing the <m>4</m> to that term so we can combine it with the outside term.
          <md permid="sTE">
            <mrow>T(x)\amp=4\left(\highlight{\left(\unhighlight{x^2+5x+\frac{25}{4}}\right)}-\highlight{\frac{25}{4}}\right)+24</mrow>
            <mrow>\amp=4\highlight{\left(\unhighlight{x^2+5x+\frac{25}{4}}\right)}-\multiplyleft{4}\frac{25}{4}+24</mrow>
            <mrow>\amp=4\left(x+\frac{5}{2}\right)^2-25+24</mrow>
            <mrow>\amp=4\left(x+\frac{5}{2}\right)^2-1</mrow>
          </md>
          Note that The vertex is <m>\left(-\frac{5}{2},-1\right)</m>.
        </p>
      </solution>
    </example>
    <example permid="IMj">
      <title>Graphing Quadratic Functions by Hand</title>
      <statement>
        <p permid="Iyb">
          Graph the function <m>H</m> defined by
          <m>H(x)=-x^2-8x-15</m> by determining its key features algebraically.
        </p>
      </statement>
      <solution permid="SfV">
        <p permid="vlu">
          To start, we'll note that this function opens downward because the leading coefficient,
          <m>-1</m>,
          is negative.
        </p>
        <p permid="bsD">
          Now we will complete the square to find the vertex.
          We will factor the <m>-1</m> out of the first two terms,
          and then add and subtract <m>\left(\frac{8}{2}\right)^2=4^2=\highlight{16}</m> on the right side.
          <md permid="ZaN">
            <mrow>H(x)\amp=-\left[x^2+8x\right]-15</mrow>
            <mrow>\amp=-\left[x^2+8x\addright{16}\subtractright{16}\right]-15</mrow>
            <mrow>\amp=-\left[\highlight{\left(\unhighlight{x^2+8x+16}\right)}-16\right]-15</mrow>
            <mrow>\amp=-\highlight{\left(\unhighlight{x^2+8x+16}\right)}-\left(-1\cdot16\right)-15</mrow>
            <mrow>\amp=-\left(x+4\right)^2+16-15</mrow>
            <mrow>\amp=-\left(x+4\right)^2+1</mrow>
          </md>
          The vertex is <m>(-4,1)</m> so the axis of symmetry is the line <m>x=-4</m>.
        </p>
        <p permid="HzM">
          To find the <m>y</m>-intercept,
          we'll replace <m>x</m> with <m>0</m> or read the value of <m>c</m> from the function in standard form:
          <md permid="FhW">
            <mrow>H(\substitute{0})\amp=-(\substitute{0})^2-8(\substitute{0})-15</mrow>
            <mrow>\amp=-15</mrow>
          </md>
          The <m>y</m>-intercept is <m>(0,-15)</m> and we will find its symmetric point on the graph,
          which is <m>(-8,-15)</m>.
        </p>
        <p permid="nGV">
          Next, we'll find the horizontal intercepts.
          We see this function factors so we will write the factored form to get the horizontal intercepts.
          <md permid="lpf">
            <mrow>H(x)\amp=-x^2-8x-15</mrow>
            <mrow>\amp=-\left(x^2+8x+15\right)</mrow>
            <mrow>\amp=-(x+3)(x+5)</mrow>
          </md>
          The <m>x</m>-intercepts are <m>(-3,0)</m> and <m>(-5,0)</m>.
        </p>
        <p permid="TOe">
          Now we will plot all of the key points and draw the parabola.
        </p>
        <sidebyside width="37%">
        <figure permid="zVn">
          <caption>The graph of <m>y=-x^2-8x-15</m>.</caption>
          <image width="47%" permid="oTF">
            <description>a graph of the parabola y=-x^2-8x-15 opens downward and the key features are labeled; the vertex is (-4,1);the axis of symmetry is drawn at x=-4;the y-intercept is (0,-15) and its symmetric point is (-8,-15);the x-intercepts are (-3,0) and (-5,0).</description>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=2,ymin=-20,ymax=5,
xtick={-8,-6,...,-2},
minor xtick={-10,-9,...,2},
ytick={-18,-15,...,3},
minor ytick={},
clip=false]
\addplot[soliddot, color=fifthcolor] coordinates {(-3,0)} node[above right] {$(-3,0)$};
\addplot[soliddot, color=fourthcolor] coordinates {(-8,-15)} node[below right] {$(-8,-15)$};
\addplot[soliddot, color=fifthcolor] coordinates {(-5,0)} node[above left] {$(-5,0)$};
\addplot[soliddot, color=fourthcolor] coordinates {(0,-15)} node[below left] {$(0,-15)$};
\addplot[soliddot, color=secondcolor] coordinates {(-4,1)} node[above] {$(-4,1)$};
\addplot[primarycurve,domain=-8.583:0.583] {-1*x^2-8*x-15} node[pos=0.3, left] {$y=H(x)$};
\addplot[symmetryaxis] coordinates {(-4,-20) (-4,5)};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
          </image>
        </figure>
      </sidebyside>
      </solution>
    </example>
  </subsection>
  <subsection permid="IxO">
    <title>More on Complex Solutions to Quadratic Equations</title>
    <p permid="rer">
      In <xref ref="section-more-on-complex-solutions-to-quadratic-equations">Section</xref>
      we covered the definition of a <xref ref="definition-complex-number" text="title">complex number</xref>,
      and discussed both quadratic applications and equations where complex numbers appear as solutions.
    </p>
    <example permid="oTs">
      <title>Applications with Real or Complex Solutions</title>
      <statement>
        <p permid="oFk">
          One day, Samar was bouncing a ball inside the house.
          The trajectory of his bounce followed the quadratic function <m>H(t)=-16t^2+24t</m>,
          where <m>H(t)</m> describes the height of the ball, in feet,
          at time <m>t</m> seconds after it bounced off the ground.
          If the ceilings in Samar's house were <m>10</m> feet tall,
          find out if the ball will hit the ceiling.
        </p>
      </statement>
      <solution permid="yne">
        <p permid="gcw">
          To find out if the ball will hit the ceiling,
          we need to set the formula for the function equal to <m>\substitute{10}</m> and solve.
          <md permid="Rwo">
            <mrow>\substitute{H(t)}\amp=-16t^2+24t</mrow>
            <mrow>\substitute{10}\amp=-16t^2+24t</mrow>
            <mrow>0\amp=-16t^2+24t-10</mrow>
            <intertext>This is a quadratic equation where verything is divisible by <m>2</m>. We will divide every term by <m>2</m> which can simplify the process.</intertext>
            <mrow>\divideunder{0}{2}\amp=\divideunder{-16t^2}{2}+\divideunder{24t}{2}-\divideunder{10}{2}</mrow>
            <mrow>0\amp=-8t^2+12t-5</mrow>
            <intertext>Since the equation doesn't seem to factor easily, we will use the <xref ref="fact-the-quadratic-formula" text="title">quadratic formula</xref> to solve it. Note that <m>\substitute{a=-8}</m>, <m>\substitute{b=12}</m>, and <m>\substitute{c=-5}</m>.</intertext>
            <mrow>t\amp=\frac{-b\pm\sqrt{b^2-4ac}}{2a}</mrow>
            <mrow>t\amp=\frac{-(\substitute{12})\pm\sqrt{(\substitute{12})^2-4(\substitute{-8})(\substitute{-5})}}{2(\substitute{-8})}</mrow>
            <mrow>t\amp=\frac{-12\pm\sqrt{-16}}{-16}</mrow>
            <intertext>Note that the discriminant is negative, which means that the equation has no real solutions. Just for practice, we will finish the simplification process, but we are ready to make our conclusion here.</intertext>
            <mrow>t\amp=\frac{-12\pm\sqrt{\highlight{16}\cdot\lighthigh{-1}}}{-16}</mrow>
            <mrow>t\amp=\frac{-12\pm\highlight{4}\lighthigh{i}}{-16}</mrow>
            <mrow>t\amp=\frac{-12}{-16}\pm\frac{\highlight{4}\lighthigh{i}}{-16}</mrow>
            <mrow>t\amp=\frac{3}{4}\mp\frac{\lighthigh{i}}{4}</mrow>
          </md>
          Since the solutions to the equation are complex numbers,
          the reality of the situation must be that the ball never does hit the ceiling.
          Samar's ceiling lights are safe for now.
        </p>
      </solution>
    </example>
    <example permid="VaB">
      <title>Solving Equations with Complex Solutions</title>
      <statement>
        <p permid="UMt">
          Solve for <m>x</m> in <m>3x^2-12x+36=0</m>.
        </p>
      </statement>
      <solution permid="eun">
        <p permid="MjF">
          We will use the completing-the-square method.
          To do so, we first need to divide both sides by the leading coefficient,
          <m>3</m>.
          <md permid="xDx">
            <mrow>3x^2-12x+36\amp=0</mrow>
            <mrow>3x^2-12x\amp=-36</mrow>
            <mrow>\divideunder{3x^2}{3}-\divideunder{12x}{3}\amp=\divideunder{-36}{3}</mrow>
            <mrow>x^2-4x\amp=-12</mrow>
            <intertext>Now we can add <m>\left(\frac{b}{2}\right)^2=(-2)^2=4</m> to both sides to complete the square.</intertext>
            <mrow>x^2-4x\addright{4}\amp=-12\addright{4}</mrow>
            <mrow>(x-2)^2\amp=-8</mrow>
          </md>
          <md permid="dKG">
            <mrow>x-2\amp=-\sqrt{-8}\amp\amp\text{ or }\amp x-2\amp=\sqrt{-8}</mrow>
            <mrow>x-2\amp=-\sqrt{\highlight{4}\cdot\lighthigh{-1}\cdot2}\amp\amp\text{ or }\amp x-2\amp=\sqrt{\highlight{4}\cdot\lighthigh{-1}\cdot2}</mrow>
            <mrow>x-2\amp=-\highlight{2}\lighthigh{i}\sqrt{2}\amp\amp\text{ or }\amp x-2\amp=\highlight{2}\lighthigh{i}\sqrt{2}</mrow>
            <mrow>x\amp=2-\highlight{2}\lighthigh{i}\sqrt{2}\amp\amp\text{ or }\amp x\amp=2+\highlight{2}\lighthigh{i}\sqrt{2}</mrow>
          </md>
          The solution set is <m>\{2-\highlight{2}\lighthigh{i}\sqrt{2}, 2+\highlight{2}\lighthigh{i}\sqrt{2}\}</m>.
        </p>
      </solution>
    </example>
  </subsection>
  <subsection permid="oEX">
    <title>Complex Number Operations</title>
    <p permid="XlA">
      In <xref ref="section-complex-number-operations">Section</xref>
      we covered the essential algebra of complex numbers.
    </p>
    <example permid="BhK">
      <title>Adding and Subtracting Complex Numbers</title>
      <statement>
        <p permid="ATC">
          Simplify the expression <m>(5-3i)-(1-7i)</m>.
        </p>
      </statement>
      <solution permid="KBw">
        <p permid="sqO">
          <md permid="JRP">
            <mrow>(5-3i)-(1-7i)\amp=5-3\highlight{i}-1+7\highlight{i}</mrow>
            <mrow>\amp=4+4\highlight{i}</mrow>
          </md>
        </p>
      </solution>
    </example>
    <example permid="hoT">
      <title>Multiplying Complex Numbers</title>
      <statement>
        <p permid="haL">
          Multiply <m>(3+2i)(5-6i)</m>.
        </p>
      </statement>
      <solution permid="qIF">
        <p permid="YxX">
          We will use the <acro>FOIL</acro> method to multiply the two binomials.
          <md permid="pYY">
            <mrow>(1+5i)(2-7i)\amp=15-18\highlight{i}+10\highlight{i}-12\highlight{i}^2</mrow>
            <mrow>\amp=15-8\highlight{i}-12(-1)</mrow>
            <mrow>\amp=15-8\highlight{i}+12</mrow>
            <mrow>\amp=27-8\highlight{i}</mrow>
          </md>
        </p>
      </solution>
    </example>
    <example permid="Nwc">
      <title>Dividing Complex Numbers</title>
      <statement>
        <p permid="NhU">
          Simplify the expression <m>\frac{3+5i}{5-6i}</m>.
        </p>
      </statement>
      <solution permid="WPO">
        <p permid="EFg">
          To divide complex numbers,
          we rationalize the denominator using the conjugate <m>2+4i</m>:
          <md permid="Wgh">
            <mrow>\frac{3+5i}{5-6i}\amp=\frac{(3+5i)}{(5-6i)}\multiplyright{\frac{(5+6i)}{(5+6i)}}</mrow>
            <mrow>\amp=\frac{15+18\highlight{i}+25\highlight{i}+30\highlight{i}^2}{25+30\highlight{i}-30\highlight{i}-36\highlight{i}^2}</mrow>
            <mrow>\amp=\frac{15+43\highlight{i}+30(-1)}{25-36(-1)}</mrow>
            <mrow>\amp=\frac{15+43\highlight{i}-30}{25+36}</mrow>
            <mrow>\amp=\frac{-15+43\highlight{i}}{61}</mrow>
            <mrow>\amp=-\frac{15}{61}+\frac{43\highlight{i}}{61}</mrow>
          </md>
        </p>
      </solution>
    </example>
  </subsection>
  
  <exercises permid="UMg">
    <exercisegroup cols="2" permid="ATp">
      <title>Graphs and Vertex Form</title>
      <introduction permid="TGN">
        <p permid="tpd">
          Evaluate the function for several values of <m>x</m> to make a table.
        </p>
      </introduction>
      <exercise permid="zNW">
        <webwork>
            <setup>
<pg-code>
Context()->noreduce('(-x)+y', '(-x)-y');
$f = RandomVariableName(type=>'function');
do {
do {
$b = non_zero_random(-4,4,1);
$c = non_zero_random(-3,3,1);
} until (($b)**2-4*$c > 0);
} until ((0 &lt; (-$b-sqrt(($b)**2-4*$c))/2  and  (-$b-sqrt(($b)**2-4*$c))/2 &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$c))/2  and  (-$b+sqrt(($b)**2-4*$c))/2 &lt; 4));
$formula = Compute("x^2+$b x+$c")->reduce;
@defaultAnswers = ();
for my $i (0..6) {
$x[$i] = $i-2;
$y[$i] = $formula->eval(x=>$x[$i]);
push(@defaultAnswers,$x[$i]);
push(@defaultAnswers,$y[$i]);
}
$multians = MultiAnswer(@defaultAnswers)->with(
singleResult => 1,
checker => sub {
my ( $correct, $student, $self ) = @_;
my @sxy = @{$student};
my @xs, @ys;
for my $k (0..6)
{$xs[$k] = $sxy[2*$k];
$ys[$k] = $sxy[2*$k+1];}
#
#  Loop though the student answers
##
for ($i = 0; $i &lt;= 6; $i++) {
my $ith = Value::List->NameForNumber($i+1);
#
#  Check that the student's answers are Reals
#
Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
#
#  Check that the point hasn't been given before
#
for ($j = 0, $used = 0; $j &lt; $i; $j++) {
my $jth = Value::List->NameForNumber($j+1);
if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
$used = 1; last;
}
}
#
#  If not already used, check that it satisfies the equation
#    and increase the score if so.
#
if (!$used) {
Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
}
}
return 1;
}
);
for my $i (0..6) {
Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
$rs[$i] = $formula->substitute(x=>$x[$i]);
}
</pg-code>
            </setup>
            <statement>
              <p permid="Zwm">
                Make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.
              </p>
              <sidebyside permid="ZvZ">
                <tabular halign="center" permid="euA">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>
              </sidebyside>
            </statement>
            <solution permid="CWX">
              <p permid="kMp">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>
              <p permid="QTy">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>
              <p permid="xaH">
                <md permid="Cnq">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>
              <p permid="dhQ">
                So the table may be completed as:
              </p>
              <sidebyside permid="FDi">
                <tabular halign="center" permid="KBJ">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>
              </sidebyside>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="fVf">
        <webwork>
            <setup>
<pg-code>
Context()->noreduce('(-x)+y', '(-x)-y');
$f = RandomVariableName(type=>'function');
do {
do {
$b = non_zero_random(-4,4,1);
$c = non_zero_random(-3,3,1);
} until (($b)**2-4*$c > 0);
} until ((0 &lt; (-$b-sqrt(($b)**2-4*$c))/2  and  (-$b-sqrt(($b)**2-4*$c))/2 &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$c))/2  and  (-$b+sqrt(($b)**2-4*$c))/2 &lt; 4));
$formula = Compute("x^2+$b x+$c")->reduce;
@defaultAnswers = ();
for my $i (0..6) {
$x[$i] = $i-2;
$y[$i] = $formula->eval(x=>$x[$i]);
push(@defaultAnswers,$x[$i]);
push(@defaultAnswers,$y[$i]);
}
$multians = MultiAnswer(@defaultAnswers)->with(
singleResult => 1,
checker => sub {
my ( $correct, $student, $self ) = @_;
my @sxy = @{$student};
my @xs, @ys;
for my $k (0..6)
{$xs[$k] = $sxy[2*$k];
$ys[$k] = $sxy[2*$k+1];}
#
#  Loop though the student answers
##
for ($i = 0; $i &lt;= 6; $i++) {
my $ith = Value::List->NameForNumber($i+1);
#
#  Check that the student's answers are Reals
#
Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
#
#  Check that the point hasn't been given before
#
for ($j = 0, $used = 0; $j &lt; $i; $j++) {
my $jth = Value::List->NameForNumber($j+1);
if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
$used = 1; last;
}
}
#
#  If not already used, check that it satisfies the equation
#    and increase the score if so.
#
if (!$used) {
Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
}
}
return 1;
}
);
for my $i (0..6) {
Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
$rs[$i] = $formula->substitute(x=>$x[$i]);
}
</pg-code>
            </setup>
            <statement>
              <p permid="FDv">
                Make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.
              </p>
              <sidebyside permid="lKr">
                <tabular halign="center" permid="qIS">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>
              </sidebyside>
            </statement>
            <solution permid="jeg">
              <p permid="JoZ">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>
              <p permid="pwi">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>
              <p permid="VDr">
                <md permid="iuz">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>
              <p permid="BKA">
                So the table may be completed as:
              </p>
              <sidebyside permid="RRA">
                <tabular halign="center" permid="WQb">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>
              </sidebyside>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="Mco">
        <webwork>
            <setup>
<pg-code>
Context()->noreduce('(-x)+y', '(-x)-y');
$f = RandomVariableName(type=>'function');
do {
do {
$b = non_zero_random(-4,4,1);
$c = non_zero_random(-3,3,1);
} until (($b)**2+4*$c > 0);
} until ((0 &lt; (-$b-sqrt(($b)**2+4*$c))/-2  and  (-$b-sqrt(($b)**2+4*$c))/-2 &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2+4*$c))/-2  and  (-$b+sqrt(($b)**2+4*$c))/-2 &lt; 4));
$formula = Compute("-x^2+$b x+$c")->reduce;
@defaultAnswers = ();
for my $i (0..6) {
$x[$i] = $i-2;
$y[$i] = $formula->eval(x=>$x[$i]);
push(@defaultAnswers,$x[$i]);
push(@defaultAnswers,$y[$i]);
}
$multians = MultiAnswer(@defaultAnswers)->with(
singleResult => 1,
checker => sub {
my ( $correct, $student, $self ) = @_;
my @sxy = @{$student};
my @xs, @ys;
for my $k (0..6)
{$xs[$k] = $sxy[2*$k];
$ys[$k] = $sxy[2*$k+1];}
#
#  Loop though the student answers
##
for ($i = 0; $i &lt;= 6; $i++) {
my $ith = Value::List->NameForNumber($i+1);
#
#  Check that the student's answers are Reals
#
Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
#
#  Check that the point hasn't been given before
#
for ($j = 0, $used = 0; $j &lt; $i; $j++) {
my $jth = Value::List->NameForNumber($j+1);
if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
$used = 1; last;
}
}
#
#  If not already used, check that it satisfies the equation
#    and increase the score if so.
#
if (!$used) {
Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
}
}
return 1;
}
);
for my $i (0..6) {
Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
$rs[$i] = $formula->substitute(x=>$x[$i]);
}
</pg-code>
            </setup>
            <statement>
              <p permid="lKE">
                Make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.
              </p>
              <sidebyside permid="xYJ">
                <tabular halign="center" permid="CXk">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>
              </sidebyside>
            </statement>
            <solution permid="Plp">
              <p permid="hRJ">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>
              <p permid="NYS">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>
              <p permid="ugb">
                <md permid="OBI">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>
              <p permid="ank">
                So the table may be completed as:
              </p>
              <sidebyside permid="efS">
                <tabular halign="center" permid="jet">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>
              </sidebyside>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="sjx">
        <webwork>
            <setup>
<pg-code>
Context()->noreduce('(-x)+y', '(-x)-y');
$f = RandomVariableName(type=>'function');
do {
do {
$b = non_zero_random(-4,4,1);
$c = non_zero_random(-3,3,1);
} until (($b)**2+4*$c > 0);
} until ((0 &lt; (-$b-sqrt(($b)**2+4*$c))/-2  and  (-$b-sqrt(($b)**2+4*$c))/-2 &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2+4*$c))/-2  and  (-$b+sqrt(($b)**2+4*$c))/-2 &lt; 4));
$formula = Compute("-x^2+$b x+$c")->reduce;
@defaultAnswers = ();
for my $i (0..6) {
$x[$i] = $i-2;
$y[$i] = $formula->eval(x=>$x[$i]);
push(@defaultAnswers,$x[$i]);
push(@defaultAnswers,$y[$i]);
}
$multians = MultiAnswer(@defaultAnswers)->with(
singleResult => 1,
checker => sub {
my ( $correct, $student, $self ) = @_;
my @sxy = @{$student};
my @xs, @ys;
for my $k (0..6)
{$xs[$k] = $sxy[2*$k];
$ys[$k] = $sxy[2*$k+1];}
#
#  Loop though the student answers
##
for ($i = 0; $i &lt;= 6; $i++) {
my $ith = Value::List->NameForNumber($i+1);
#
#  Check that the student's answers are Reals
#
Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
#
#  Check that the point hasn't been given before
#
for ($j = 0, $used = 0; $j &lt; $i; $j++) {
my $jth = Value::List->NameForNumber($j+1);
if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
$used = 1; last;
}
}
#
#  If not already used, check that it satisfies the equation
#    and increase the score if so.
#
if (!$used) {
Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
}
}
return 1;
}
);
for my $i (0..6) {
Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
$rs[$i] = $formula->substitute(x=>$x[$i]);
}
</pg-code>
            </setup>
            <statement>
              <p permid="RRN">
                Make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.
              </p>
              <sidebyside permid="Knb">
                <tabular halign="center" permid="PlC">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>
              </sidebyside>
            </statement>
            <solution permid="vsy">
              <p permid="Gut">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>
              <p permid="mBC">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>
              <p permid="SIL">
                <md permid="uIR">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>
              <p permid="yPU">
                So the table may be completed as:
              </p>
              <sidebyside permid="quk">
                <tabular halign="center" permid="vsL">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>
              </sidebyside>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="YqG">
        <webwork>
            <setup>
<pg-code>
Context()->noreduce('(-x)+y', '(-x)-y');
$f = RandomVariableName(type=>'function');
do {
do {
$a = random(2,3,1)*list_random(-1,1);
$b = non_zero_random(-8,8,1);
$c = non_zero_random(-6,6,1);
} until (($b)**2-4*$a*$c > 0);
} until ((0 &lt; (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4));
$formula = Compute("$a x^2+$b x+$c")->reduce;
@defaultAnswers = ();
for my $i (0..6) {
$x[$i] = $i-2;
$y[$i] = $formula->eval(x=>$x[$i]);
push(@defaultAnswers,$x[$i]);
push(@defaultAnswers,$y[$i]);
}
$multians = MultiAnswer(@defaultAnswers)->with(
singleResult => 1,
checker => sub {
my ( $correct, $student, $self ) = @_;
my @sxy = @{$student};
my @xs, @ys;
for my $k (0..6)
{$xs[$k] = $sxy[2*$k];
$ys[$k] = $sxy[2*$k+1];}
#
#  Loop though the student answers
##
for ($i = 0; $i &lt;= 6; $i++) {
my $ith = Value::List->NameForNumber($i+1);
#
#  Check that the student's answers are Reals
#
Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
#
#  Check that the point hasn't been given before
#
for ($j = 0, $used = 0; $j &lt; $i; $j++) {
my $jth = Value::List->NameForNumber($j+1);
if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
$used = 1; last;
}
}
#
#  If not already used, check that it satisfies the equation
#    and increase the score if so.
#
if (!$used) {
Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
}
}
return 1;
}
);
for my $i (0..6) {
Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
$rs[$i] = $formula->substitute(x=>$x[$i]);
}
</pg-code>
            </setup>
            <statement>
              <p permid="xYW">
                Make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.
              </p>
              <sidebyside permid="WBt">
                <tabular halign="center" permid="bzU">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>
              </sidebyside>
            </statement>
            <solution permid="bzH">
              <p permid="eXd">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>
              <p permid="Lem">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>
              <p permid="rlv">
                <md permid="aQa">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>
              <p permid="XsE">
                So the table may be completed as:
              </p>
              <sidebyside permid="CIC">
                <tabular halign="center" permid="HHd">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>
              </sidebyside>
            </solution>
        </webwork>
      </exercise>
      -->
      <exercise permid="ExP">
        <webwork>
            <setup>
<pg-code>
Context()->noreduce('(-x)+y', '(-x)-y');
$f = RandomVariableName(type=>'function');
do {
do {
$a = random(2,3,1)*list_random(-1,1);
$b = non_zero_random(-8,8,1);
$c = non_zero_random(-6,6,1);
} until (($b)**2-4*$a*$c > 0);
} until ((0 &lt; (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4));
$formula = Compute("$a x^2+$b x+$c")->reduce;
@defaultAnswers = ();
for my $i (0..6) {
$x[$i] = $i-2;
$y[$i] = $formula->eval(x=>$x[$i]);
push(@defaultAnswers,$x[$i]);
push(@defaultAnswers,$y[$i]);
}
$multians = MultiAnswer(@defaultAnswers)->with(
singleResult => 1,
checker => sub {
my ( $correct, $student, $self ) = @_;
my @sxy = @{$student};
my @xs, @ys;
for my $k (0..6)
{$xs[$k] = $sxy[2*$k];
$ys[$k] = $sxy[2*$k+1];}
#
#  Loop though the student answers
##
for ($i = 0; $i &lt;= 6; $i++) {
my $ith = Value::List->NameForNumber($i+1);
#
#  Check that the student's answers are Reals
#
Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
#
#  Check that the point hasn't been given before
#
for ($j = 0, $used = 0; $j &lt; $i; $j++) {
my $jth = Value::List->NameForNumber($j+1);
if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
$used = 1; last;
}
}
#
#  If not already used, check that it satisfies the equation
#    and increase the score if so.
#
if (!$used) {
Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
}
}
return 1;
}
);
for my $i (0..6) {
Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
$rs[$i] = $formula->substitute(x=>$x[$i]);
}
</pg-code>
            </setup>
            <statement>
              <p permid="egf">
                Make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.
              </p>
              <sidebyside permid="iPL">
                <tabular halign="center" permid="nOm">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>
              </sidebyside>
            </statement>
            <solution permid="HGQ">
              <p permid="DzN">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>
              <p permid="jGW">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>
              <p permid="POf">
                <md permid="GXj">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>
              <p permid="vVo">
                So the table may be completed as:
              </p>
              <sidebyside permid="OWU">
                <tabular halign="center" permid="TVv">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>
              </sidebyside>
            </solution>
        </webwork>
      </exercise>
      <!-- <exercise permid="kEY">
        <webwork>
            <setup>
<pg-code>
Context()->noreduce('(-x)+y', '(-x)-y');
$f = RandomVariableName(type=>'function');
do {
do {
$a = random(2,3,1)*list_random(-1,1);
$b = non_zero_random(-8,8,1);
$c = non_zero_random(-6,6,1);
} until (($b)**2-4*$a*$c > 0);
} until ((0 &lt; (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4));
$k = random(20,50,1);
$formula = Compute("$a x^2+$b x+($c+$k)")->reduce;
@defaultAnswers = ();
for my $i (0..6) {
$x[$i] = $i-2;
$y[$i] = $formula->eval(x=>$x[$i]);
push(@defaultAnswers,$x[$i]);
push(@defaultAnswers,$y[$i]);
}
$multians = MultiAnswer(@defaultAnswers)->with(
singleResult => 1,
checker => sub {
my ( $correct, $student, $self ) = @_;
my @sxy = @{$student};
my @xs, @ys;
for my $k (0..6)
{$xs[$k] = $sxy[2*$k];
$ys[$k] = $sxy[2*$k+1];}
#
#  Loop though the student answers
##
for ($i = 0; $i &lt;= 6; $i++) {
my $ith = Value::List->NameForNumber($i+1);
#
#  Check that the student's answers are Reals
#
Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
#
#  Check that the point hasn't been given before
#
for ($j = 0, $used = 0; $j &lt; $i; $j++) {
my $jth = Value::List->NameForNumber($j+1);
if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
$used = 1; last;
}
}
#
#  If not already used, check that it satisfies the equation
#    and increase the score if so.
#
if (!$used) {
Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
}
}
return 1;
}
);
for my $i (0..6) {
Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
$rs[$i] = $formula->substitute(x=>$x[$i]);
}
</pg-code>
            </setup>
            <statement>
              <p permid="Kno">
                Make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.
              </p>
              <sidebyside permid="ved">
                <tabular halign="center" permid="AcE">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>
              </sidebyside>
            </statement>
            <solution permid="nNZ">
              <p permid="ccx">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>
              <p permid="IjG">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>
              <p permid="oqP">
                <md permid="nes">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>
              <p permid="UxY">
                So the table may be completed as:
              </p>
              <sidebyside permid="blm">
                <tabular halign="center" permid="gjN">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>
              </sidebyside>
            </solution>
        </webwork>
      </exercise> -->
<!-- <exercise>
<webwork>
<setup>
<pg-code>
Context()->noreduce('(-x)+y', '(-x)-y');
$f = RandomVariableName(type=>'function');
do {
do {
$a = random(2,3,1)*list_random(-1,1);
$b = non_zero_random(-8,8,1);
$c = non_zero_random(-6,6,1);
} until (($b)**2-4*$a*$c > 0);
} until ((0 &lt; (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4));
$k = random(20,50,1);
$formula = Compute("$a x^2+$b x+($c+$k)")->reduce;
@defaultAnswers = ();
for my $i (0..6) {
$x[$i] = $i-2;
$y[$i] = $formula->eval(x=>$x[$i]);
push(@defaultAnswers,$x[$i]);
push(@defaultAnswers,$y[$i]);
}
$multians = MultiAnswer(@defaultAnswers)->with(
singleResult => 1,
checker => sub {
my ( $correct, $student, $self ) = @_;
my @sxy = @{$student};
my @xs, @ys;
for my $k (0..6)
{$xs[$k] = $sxy[2*$k];
$ys[$k] = $sxy[2*$k+1];}
#
#  Loop though the student answers
##
for ($i = 0; $i &lt;= 6; $i++) {
my $ith = Value::List->NameForNumber($i+1);
#
#  Check that the student's answers are Reals
#
Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
#
#  Check that the point hasn't been given before
#
for ($j = 0, $used = 0; $j &lt; $i; $j++) {
my $jth = Value::List->NameForNumber($j+1);
if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
$used = 1; last;
}
}
#
#  If not already used, check that it satisfies the equation
#    and increase the score if so.
#
if (!$used) {
Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
}
}
return 1;
}
);
for my $i (0..6) {
Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
$rs[$i] = $formula->substitute(x=>$x[$i]);
}
</pg-code>
</setup>
<statement>
<p>Use technology to make a table of values for the function <m><var name="$f" /></m> defined by <m><var name="$f" />(x)=<var name="$formula" /></m>.</p>
<sidebyside>
<tabular halign="center">
<row bottom="medium">
<cell><m>x</m></cell>
<cell><m><var name="$f" />(x)</m></cell>
</row>
<row>
<cell><var name="$multians" width="8" /></cell>
<cell><var name="$multians" width="8" /></cell>
</row>
<row>
<cell><var name="$multians" width="8" /></cell>
<cell><var name="$multians" width="8" /></cell>
</row>
<row>
<cell><var name="$multians" width="8" /></cell>
<cell><var name="$multians" width="8" /></cell>
</row>
<row>
<cell><var name="$multians" width="8" /></cell>
<cell><var name="$multians" width="8" /></cell>
</row>
<row>
<cell><var name="$multians" width="8" /></cell>
<cell><var name="$multians" width="8" /></cell>
</row>
<row>
<cell><var name="$multians" width="8" /></cell>
<cell><var name="$multians" width="8" /></cell>
</row>
<row>
<cell><var name="$multians" width="8" /></cell>
<cell><var name="$multians" width="8" /></cell>
</row>
</tabular>
</sidebyside>
</statement>
<solution>
<p>This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>, <m><var name="$x[1]" /></m>, <m><var name="$x[2]" /></m>, <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>, <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>. The choice to use these <m>x</m>-values is arbitrary. Since they are small numbers, they might make calculations easier. It's important to include some negative numbers.</p>
<p>One at a time, we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.</p>
<p><md> <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow> <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow> <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow> <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow> <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow> <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow> <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow> </md></p>
<p>So the table may be completed as:</p>
<sidebyside>
<tabular halign="center">
<row bottom="medium">
<cell><m>x</m></cell>
<cell><m><var name="$f" />(x)</m></cell>
</row>
<row>
<cell><m><var name="$x[0]" /></m></cell>
<cell><m><var name="$y[0]" /></m></cell>
</row>
<row>
<cell><m><var name="$x[1]" /></m></cell>
<cell><m><var name="$y[1]" /></m></cell>
</row>
<row>
<cell><m><var name="$x[2]" /></m></cell>
<cell><m><var name="$y[2]" /></m></cell>
</row>
<row>
<cell><m><var name="$x[3]" /></m></cell>
<cell><m><var name="$y[3]" /></m></cell>
</row>
<row>
<cell><m><var name="$x[4]" /></m></cell>
<cell><m><var name="$y[4]" /></m></cell>
</row>
<row>
<cell><m><var name="$x[5]" /></m></cell>
<cell><m><var name="$y[5]" /></m></cell>
</row>
<row>
<cell><m><var name="$x[6]" /></m></cell>
<cell><m><var name="$y[6]" /></m></cell>
</row>
</tabular>
</sidebyside>
</solution>
</webwork>
</exercise> -->
<!--        </exercisegroup>
<exercisegroup cols="2">
<introduction>
<p>Use technology to make a graph.</p>
</introduction> -->
<!-- <exercise>
<statement>
<p>Use technology to make a graph of <m>f</m> where <m>f(x)=x^2+3x-2</m>.</p>
</statement>
<solution>
<sidebyside widths="47%">
<image>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}
\addplot+[domain=-4.854:1.854] {x^2+3*x-2};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</sidebyside>
</solution>
</exercise>
<exercise>
<statement>
<p>Use technology to make a graph of <m>f</m> where <m>f(x)=x^2-2x-1</m>.</p>
</statement>
<solution>
<sidebyside widths="47%">
<image>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}
\addplot+[domain=-2:4] {x^2-2*x-1};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</sidebyside>
</solution>
</exercise>
<exercise>
<statement>
<p>Use technology to make a graph of <m>f</m> where <m>f(x)=-x^2+3x+2</m>.</p>
</statement>
<solution>
<sidebyside widths="47%">
<image>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}
\addplot+[domain=-1.854:4.854] {-x^2+3*x+2};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</sidebyside>
</solution>
</exercise>
<exercise>
<statement>
<p>Use technology to make a graph of <m>f</m> where <m>f(x)=-x^2+x+2</m>.</p>
</statement>
<solution>
<sidebyside widths="47%">
<image>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}
\addplot+[domain=-2.541:3.541] {-x^2+x+2};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</sidebyside>
</solution>
</exercise> -->
<!-- <exercise>
<statement>
<p>Use technology to make a graph of <m>f</m> where <m>f(x)=3x^2-6x-5</m>.</p>
</statement>
<solution>
<sidebyside widths="47%">
<image>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}[ymin=-9,
ymax=9,
ytick = {-8,-7,...,8},
minor ytick = {-8,-7,...,8},
]
\addplot+[domain=-1.38:3.38] {3*x^2-6*x-5};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</sidebyside>
</solution>
</exercise>
<exercise>
<statement>
<p>Use technology to make a graph of <m>f</m> where <m>f(x)=-3x^2-8x+3</m>.</p>
</statement>
<solution>
<sidebyside widths="47%">
<image>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}[ymin=-9,
ymax=9,
ytick = {-8,-7,...,8},
minor ytick = {-8,-7,...,8},
]
\addplot+[domain=-3.737:1.07] {-3*x^2-8*x+3};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</sidebyside>
</solution>
</exercise> -->
<!-- <exercise>
<statement>
<p>Use technology to make a graph of <m>f</m> where <m>f(x)=-3x^2+4x+49</m>.</p>
</statement>
<solution>
<sidebyside widths="47%">
<image>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}[ymin=-10,
ymax=60,
ytick = {-5,0,...,55},
minor ytick = {-9,-8,...,59},
]
\addplot+[domain=-3.818:5.151] {-3*x^2+4*x+49};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</sidebyside>
</solution>
</exercise>
<exercise>
<statement>
<p>Use technology to make a graph of <m>f</m> where <m>f(x)=2x^2-2x+41</m>.</p>
</statement>
<solution>
<sidebyside widths="47%">
<image>
<latex-image>
<![CDATA[\begin{tikzpicture}
\begin{axis}[ymin=-10,
ymax=100,
ytick = {-10,0,...,90},
minor ytick = {-5,0,...,95},
]
\addplot+[domain=-4.954:5.954] {2*x^2-2*x+41};
\end{axis}
\end{tikzpicture}]]>
</latex-image>
</image>
</sidebyside>
</solution>
</exercise> -->
<!--         </exercisegroup>
<exercisegroup cols="2">
<introduction>
<p>Use technology to find features of a quadratic function and its graph.</p>
</introduction> -->
      <exercise permid="QMh">
        <webwork>
            <setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$bound = 10;
do {
$a = non_zero_random(-2,2,1);
$b = non_zero_random(-4,4,1);
$c = random(-5,5,1);
Context()->noreduce('(-x)+y', '(-x)-y');
$formula = Formula("$a x^2 + $b x + $c")->reduce;
$discriminant = Real("($b)^2-4*$a*$c");
Context("Point");
Context()->flags->set(tolerance => 0.001);
$h = -$b/(2*$a);
$k = $formula->eval(x=>$h);
$vertex = Point($h,$k);
$yint = Point(0,$c);
if ($discriminant == 0) {$xint = List(Point($h,0));}
elsif ($discriminant > 0) {$xint = List(Point($h-sqrt($discriminant)/(2*$a),0), Point($h+sqrt($discriminant)/(2*$a),0));}
else {$xint = List(Compute("NONE"));}
Context("Numeric");
Context()->flags->set(tolerance => 0.001);
$p = non_zero_random(-8,8,1);
$fofp = $formula->eval(x=>$p);
$q = ($a > 0) ? non_zero_random(ceil($k+1),ceil($k+10),1) : non_zero_random(floor($k-10),floor($k-1),1);
Context("FiniteSolutionSets");
Context()->flags->remove("NumberCheck");
Context()->flags->set(tolerance => 0.001);
$s1 = (-$b - sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
$s2 = (-$b + sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
($s1, $s2) = (min($s1,$s2), max($s1,$s2));
$solutions = Formula("{$s1,$s2}");
Context("Interval");
Context()->flags->set(tolerance => 0.001);
$domain = Compute("(-inf,inf)");
$range = ($a > 0) ? Interval("[$k,inf)") : Interval("(-inf,$k]");
$inequality = list_random('&lt;', '>', '\leq', '\geq');
if    ($a > 0 and $inequality eq '&lt;' or $a &lt; 0 and $inequality eq '>')    {$ineqsolution = Interval("($s1,$s2)");}
elsif ($a > 0 and $inequality eq '>'    or $a &lt; 0 and $inequality eq '&lt;') {$ineqsolution = Union("(-inf,$s1)U($s2,inf)");}
elsif ($a > 0 and $inequality eq '\leq' or $a &lt; 0 and $inequality eq '\geq') {$ineqsolution = Interval("[$s1,$s2]");}
elsif ($a > 0 and $inequality eq '\geq' or $a &lt; 0 and $inequality eq '\leq') {$ineqsolution = Union("(-inf,$s1]U[$s2,inf)");}
} until (abs($h) &lt; $bound and abs($k) &lt; $bound and abs($c) &lt; $bound and abs($p) &lt; $bound and abs($fofp) &lt; $bound and abs($q) &lt; $bound and abs($s1) &lt; $bound and abs($s2) &lt; $bound and $fofp != 0);
</pg-code>
            </setup>
            <statement>
              <p permid="qux">
                Let <m><var name="$f" />(x)=<var name="$formula"/></m>.
                Find the following.
              </p>
              <instruction>
                Any decimal answers should be correct to four significant digits.
              </instruction>
              <p permid="WBG">
                <ol label="a." permid="wcs">
                  <li permid="bei">
                    <p permid="JKy">
                      The vertex is <var name="$vertex" width="20" />.
                    </p>
                  </li>
                  <li permid="Hlr">
                    <p permid="pRH">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>
                  <li permid="nsA">
                    <p permid="VYQ">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                  <li permid="TzJ">
                    <p permid="CfZ">
                      The domain of <m><var name="$f" /></m> is <var name="$domain" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>
                  <li permid="zGS">
                    <p permid="ini">
                      The range of <m><var name="$f" /></m> is <var name="$range" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>
                  <li permid="fOb">
                    <p permid="Our">
                      Calculate <m><var name="$f" />(<var name="$p" />)</m>.
                      <var name="$fofp" width="20" />.
                    </p>
                  </li>
                  <li permid="LVk">
                    <p permid="uBA">
                      Solve <m><var name="$f" />(x)=<var name="$q" /></m>.
                      <var name="$solutions" width="20" />
                    </p>
                  </li>
                  <li permid="sct">
                    <p permid="aIJ">
                      Solve <m><var name="$f" />(x)<var name="$inequality" /><var name="$q" /></m>.
                      <var name="$ineqsolution" width="20" />
                    </p>
                    <instruction>
                      Use interval notation. If you need to write a union of intervals, use <c>U</c> for the union symbol.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
        </webwork>
      </exercise>
      <exercise permid="wTq">
        <webwork>
            <setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$bound = 10;
do {
$a = non_zero_random(-2,2,1);
$b = non_zero_random(-4,4,1);
$c = random(-5,5,1);
Context()->noreduce('(-x)+y', '(-x)-y');
$formula = Formula("$a x^2 + $b x + $c")->reduce;
$discriminant = Real("($b)^2-4*$a*$c");
Context("Point");
Context()->flags->set(tolerance => 0.001);
$h = -$b/(2*$a);
$k = $formula->eval(x=>$h);
$vertex = Point($h,$k);
$yint = Point(0,$c);
if ($discriminant == 0) {$xint = List(Point($h,0));}
elsif ($discriminant > 0) {$xint = List(Point($h-sqrt($discriminant)/(2*$a),0), Point($h+sqrt($discriminant)/(2*$a),0));}
else {$xint = List(Compute("NONE"));}
Context("Numeric");
Context()->flags->set(tolerance => 0.001);
$p = non_zero_random(-8,8,1);
$fofp = $formula->eval(x=>$p);
$q = ($a > 0) ? non_zero_random(ceil($k+1),ceil($k+10),1) : non_zero_random(floor($k-10),floor($k-1),1);
Context("FiniteSolutionSets");
Context()->flags->remove("NumberCheck");
Context()->flags->set(tolerance => 0.001);
$s1 = (-$b - sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
$s2 = (-$b + sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
($s1, $s2) = (min($s1,$s2), max($s1,$s2));
$solutions = Formula("{$s1,$s2}");
Context("Interval");
Context()->flags->set(tolerance => 0.001);
$domain = Compute("(-inf,inf)");
$range = ($a > 0) ? Interval("[$k,inf)") : Interval("(-inf,$k]");
$inequality = list_random('&lt;', '>', '\leq', '\geq');
if    ($a > 0 and $inequality eq '&lt;' or $a &lt; 0 and $inequality eq '>')    {$ineqsolution = Interval("($s1,$s2)");}
elsif ($a > 0 and $inequality eq '>'    or $a &lt; 0 and $inequality eq '&lt;') {$ineqsolution = Union("(-inf,$s1)U($s2,inf)");}
elsif ($a > 0 and $inequality eq '\leq' or $a &lt; 0 and $inequality eq '\geq') {$ineqsolution = Interval("[$s1,$s2]");}
elsif ($a > 0 and $inequality eq '\geq' or $a &lt; 0 and $inequality eq '\leq') {$ineqsolution = Union("(-inf,$s1]U[$s2,inf)");}
} until (abs($h) &lt; $bound and abs($k) &lt; $bound and abs($c) &lt; $bound and abs($p) &lt; $bound and abs($fofp) &lt; $bound and abs($q) &lt; $bound and abs($s1) &lt; $bound and abs($s2) &lt; $bound and $fofp != 0);
</pg-code>
            </setup>
            <statement>
              <p permid="CIP">
                Let <m><var name="$f" />(x)=<var name="$formula"/></m>.
                Find the following.
              </p>
              <instruction>
                Any decimal answers should be correct to four significant digits.
              </instruction>
              <p permid="iPY">
                <ol label="a." permid="cjB">
                  <li permid="YjC">
                    <p permid="GPS">
                      The vertex is <var name="$vertex" width="20" />.
                    </p>
                  </li>
                  <li permid="EqL">
                    <p permid="mXb">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>
                  <li permid="kxU">
                    <p permid="Tek">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                  <li permid="QFd">
                    <p permid="zlt">
                      The domain of <m><var name="$f" /></m> is <var name="$domain" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>
                  <li permid="wMm">
                    <p permid="fsC">
                      The range of <m><var name="$f" /></m> is <var name="$range" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>
                  <li permid="cTv">
                    <p permid="LzL">
                      Calculate <m><var name="$f" />(<var name="$p" />)</m>.
                      <var name="$fofp" width="20" />.
                    </p>
                  </li>
                  <li permid="JaE">
                    <p permid="rGU">
                      Solve <m><var name="$f" />(x)=<var name="$q" /></m>.
                      <var name="$solutions" width="20" />
                    </p>
                  </li>
                  <li permid="phN">
                    <p permid="XOd">
                      Solve <m><var name="$f" />(x)<var name="$inequality" /><var name="$q" /></m>.
                      <var name="$ineqsolution" width="20" />
                    </p>
                    <instruction>
                      Use interval notation. If you need to write a union of intervals, use <c>U</c> for the union symbol.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
        </webwork>
      </exercise>
<!-- Finding the vertex -->
      <exercise permid="daz">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>
<pg-code>
$refreshCachedImages=1;
Context("Numeric");
Context()->variables->add(t => 'Real');
Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
Context()->noreduce("(-x)-y","(-x)+y");
Context()->flags->set(
tolerance => 0.005,
tolType => "absolute",
);
$initSpeed = random(50,200,10);
$a = -16;
$b = $initSpeed;
$c = random(100,300,50);
$func = Compute("$a*t**2+$b*t+$c")->reduce;
$funcX = Compute("$a*x**2+$b*x+$c")->reduce;
$xVertex = -$b/(2*$a);
$yVertex = $func->eval(t=>$xVertex);
$dis = ($b)**2-4*$a*$c;
$xInt2 = (-$b-sqrt($dis))/(2*$a);
$alt="This picture shows the graph of a parabola for the function h(t)=$func. The vertex ($xVertex,$yVertex) is labeled.";
($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);
@xticks = @$xticks_ref;
@yticks = @$yticks_ref;
$gr = init_graph($min[0],$min[1],$max[0],$max[1],
axes=>[0,0],
grid=>[$ticknum[0],$ticknum[1]],
size=>[xPixels(),yPixels()]
);
$gr->lb('reset');
for my $j (@xticks) {
if (abs($j) &lt; 10**(-10)) {next;}
$gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
}
$gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
$gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, h,'black','left','middle'));
for my $j (@yticks) {
if (Real($j) == 0) {next;}
$gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
}
add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");
$hx = ($xVertex+$xInt2)/2;
$hy = $func->eval(t=>$hx);
$gr->lb( new Label($hx-$marksep[0]/8, $hy, "h(t)",'red','right','middle'));
$gr->stamps( closed_circle($xVertex,$yVertex,'red') );
$gr->lb( new Label($xVertex, $yVertex+$marksep[1]/4, "($xVertex,$yVertex)",'red','center','bottom'));
</pg-code>
        </setup>
        <statement>
          <p permid="OXh">
            An object was launched from the top of a hill with an upward vertical velocity of <m><var name="$b"/></m> feet per second.
            The height of the object can be modeled by the function <m>h(t)=<var name="$func"/></m>,
            where <m>t</m> represents the number of seconds after the launch.
            Assume the object landed on the ground at sea level.
            Find the answer algebraically.
          </p>
          <p permid="veq">
            <var name="$xVertex" width="10" /> seconds after its launch,
            the object reached its maximum height of <var name="$yVertex" width="10" /> feet.
          </p>
        </statement>
        <solution permid="TVi">
          <p permid="AFh">
            Graph the function <m>h(t)=<var name="$func"/></m>,
            and then find the parabola's vertex,
            <m>(<var name="$xVertex"/>,<var name="$yVertex"/>)</m>.
          </p>
          <sidebyside widths="47%" permid="Hsv">
            <image pg-name="$gr" permid="VaO">
              <description><var name="$alt" /></description>
            </image>
          </sidebyside>
          <p permid="gMq">
            According to the vertex point <m>(<var name="$xVertex"/>,<var name="$yVertex"/>)</m>,
            <m><var name="$xVertex"/></m> seconds after it was launched,
            the object reached its maximum height of <m><var name="$yVertex"/></m> feet.
          </p>
        </solution>
        </webwork>
      </exercise>
<!-- Finding x-intercept -->
<!-- TODO: The solution shows 13 decimal places! -->
      <exercise permid="JhI">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>
<pg-code>
$refreshCachedImages=1;
Context("Numeric");
Context()->variables->add(t => 'Real');
Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
Context()->noreduce("(-x)-y","(-x)+y");
Context()->flags->set(
tolerance => 0.005,
tolType => "absolute",
);
$initSpeed = random(50,200,10);
$a = -16;
$b = $initSpeed;
$c = random(100,300,50);
$func = Compute("$a*t**2+$b*t+$c")->reduce;
$funcX = Compute("$a*x**2+$b*x+$c")->reduce;
$xVertex = -$b/(2*$a);
$yVertex = $func->eval(t=>$xVertex);
$dis = ($b)**2-4*$a*$c;
$xInt2 = (-$b-sqrt($dis))/(2*$a);
$alt="This picture shows the graph of a parabola for the function h(t)=$func. The x-intercept ($xInt2,0) is labeled.";
($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);
@xticks = @$xticks_ref;
@yticks = @$yticks_ref;
$gr = init_graph($min[0],$min[1],$max[0],$max[1],
axes=>[0,0],
grid=>[$ticknum[0],$ticknum[1]],
size=>[xPixels(),yPixels()]
);
$gr->lb('reset');
for my $j (@xticks) {
if (abs($j) &lt; 10**(-10)) {next;}
$gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
}
$gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
$gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, h,'black','left','middle'));
for my $j (@yticks) {
if (Real($j) == 0) {next;}
$gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
}
add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");
$hx = ($xVertex+$xInt2)/2;
$hy = $func->eval(t=>$hx);
$gr->lb( new Label($hx-$marksep[0]/8, $hy, "h(t)",'red','right','middle'));
$gr->stamps( closed_circle($xInt2,0,'red') );
$gr->lb( new Label($xInt2, $marksep[1]/4, "($xInt2,0)",'red','right','bottom'));
</pg-code>
        </setup>
        <statement>
          <p permid="blz">
            An object was launched from the top of a hill with an upward vertical velocity of <m><var name="$b"/></m> feet per second.
            The height of the object can be modeled by the function <m>h(t)=<var name="$func"/></m>,
            where <m>t</m> represents the number of seconds after the launch.
            Assume the object landed on the ground at sea level.
            Find the answer algebraically.
          </p>
          <p permid="HsI">
            <var name="$xInt2" width="10" /> seconds after its launch,
            the object fell to the ground at sea level.
          </p>
        </statement>
        <solution permid="Acr">
          <p permid="MTz">
            Graph the function <m>h(t)=<var name="$func"/></m>,
            and then find the parabola's positive <m>t</m>-intercept,
            <m>(<var name="$xInt2"/>,0)</m>.
          </p>
          <sidebyside widths="47%" permid="nzE">
            <image pg-name="$gr" permid="BhX">
              <description><var name="$alt" /></description>
            </image>
          </sidebyside>
          <p permid="taI">
            According to the <m>t</m>-intercept,
            <m>(<var name="$xInt2"/>,0)</m>,
            <m><var name="$xInt2"/></m> seconds after it was launched,
            the object fell to the ground at sea level.
          </p>
        </solution>
        </webwork>
      </exercise>
<!-- <exercise>
<webwork source="Math95/ParabolaVertex/ParabolaVertex3.pg" />
</exercise> -->
      <exercise permid="poR">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex3.pg" />
      </exercise>
      <exercise permid="Vwa">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex3.pg" />
      </exercise>
<!-- <exercise>
<webwork source="Math95/ParabolaVertex/ParabolaVertex3.pg" />
</exercise>
<exercise>
<webwork source="Math95/ParabolaVertex/ParabolaVertex4.pg" />
</exercise>
<exercise>
<webwork source="Math95/ParabolaVertex/ParabolaVertex4.pg" />
</exercise> -->
      <exercise permid="BDj">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm10.pg" />
      </exercise>
<!-- <exercise>
<webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm10.pg" />
</exercise>
<exercise>
<webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm10.pg" />
</exercise> -->
      <exercise permid="hKs">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm10.pg" />
      </exercise>
      <exercise permid="NRB">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
      </exercise>
      <exercise permid="tYK">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
      </exercise>
<!-- <exercise>
<webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
</exercise>
<exercise>
<webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
</exercise>
<exercise>
<webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
</exercise> -->
      <exercise permid="afT">
        <webwork>
            <setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = 1;
$h = non_zero_random(-9,9,1);
$k = non_zero_random(-9,9,1);
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k")->reduce;
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = Compute("[$k,inf)");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
            </setup>
            <statement>
              <p permid="nzR">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.
                <ol label="a." permid="IqK">
                  <li permid="VoW">
                    <p permid="DVm">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>
                    <p permid="kcv">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>
                  <li permid="Bwf">
                    <p permid="QjE">
                      What is the range of <m><var name="$f"/></m>?
                    </p>
                    <p permid="wqN">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="gjA">
              <p permid="ZhR">
                <ol label="a." permid="feh">
                  <li permid="hDo">
                    <p permid="cxW">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>
                  <li permid="NKx">
                    <p permid="IFf">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="Gnc">
        <webwork>
            <setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = 1;
$h = non_zero_random(-9,9,1);
$k = non_zero_random(-9,9,1);
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k")->reduce;
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = Compute("[$k,inf)");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
            </setup>
            <statement>
              <p permid="THa">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.
                <ol label="a." permid="oxT">
                  <li permid="tRG">
                    <p permid="oMo">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>
                    <p permid="UTx">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>
                  <li permid="ZYP">
                    <p permid="BaG">
                      What is the range of <m><var name="$f"/></m>?
                    </p>
                    <p permid="hhP">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="MqJ">
              <p permid="Fpa">
                <ol label="a." permid="Llq">
                  <li permid="GfY">
                    <p permid="NoY">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>
                  <li permid="mnh">
                    <p permid="twh">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>
<!--
<exercise>
<webwork>
<setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = random(0.1,9.9,0.2);
$h = non_zero_random(-9,9,1);
$k = non_zero_random(-9,9,1);
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k")->reduce;
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = Compute("[$k,inf)");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
</setup>
<statement>
<p>Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.<ol label="a."> <li> <p>What is the domain of <m><var name="$f"/></m>?</p> <p><var name="$domain" width="10"/></p> </li> <li> <p>What is the range of <m><var name="$f"/></m>?</p> <p><var name="$range" width="10"/></p> </li> </ol></p>
</statement>
<solution>
<p><ol label="a."> <li> <p>The function is quadratic, so the domain is <m><var name="$domain"/></m>. There are no invalid inputs for this function.</p> </li> <li> <p>Since the formula is in vertex form, we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>. Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>. So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>. So the range is <m><var name="$range"/></m>.</p> </li> </ol></p>
</solution>
</webwork>
</exercise>
<exercise>
<webwork>
<setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = random(0.1,9.9,0.2);
$h = non_zero_random(-9,9,1);
$k = non_zero_random(-9,9,1);
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k")->reduce;
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = Compute("[$k,inf)");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
</setup>
<statement>
<p>Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.<ol label="a."> <li> <p>What is the domain of <m><var name="$f"/></m>?</p> <p><var name="$domain" width="10"/></p> </li> <li> <p>What is the range of <m><var name="$f"/></m>?</p> <p><var name="$range" width="10"/></p> </li> </ol></p>
</statement>
<solution>
<p><ol label="a."> <li> <p>The function is quadratic, so the domain is <m><var name="$domain"/></m>. There are no invalid inputs for this function.</p> </li> <li> <p>Since the formula is in vertex form, we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>. Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>. So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>. So the range is <m><var name="$range"/></m>.</p> </li> </ol></p>
</solution>
</webwork>
</exercise>
<exercise>
<webwork>
<setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = non_zero_random(-9,9,1);
$h = non_zero_random(-9,9,1);
$k = non_zero_random(-9,9,1);
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k")->reduce;
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
</setup>
<statement>
<p>Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.<ol label="a."> <li> <p>What is the domain of <m><var name="$f"/></m>?</p> <p><var name="$domain" width="10"/></p> </li> <li> <p>What is the range of <m><var name="$f"/></m>?</p> <p><var name="$range" width="10"/></p> </li> </ol></p>
</statement>
<solution>
<p><ol label="a."> <li> <p>The function is quadratic, so the domain is <m><var name="$domain"/></m>. There are no invalid inputs for this function.</p> </li> <li> <p>Since the formula is in vertex form, we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>. Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>. So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>. So the range is <m><var name="$range"/></m>.</p> </li> </ol></p>
</solution>
</webwork>
</exercise>
<exercise>
<webwork>
<setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = non_zero_random(-9,9,1);
$h = non_zero_random(-9,9,1);
$k = non_zero_random(-9,9,1);
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k")->reduce;
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
</setup>
<statement>
<p>Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.<ol label="a."> <li> <p>What is the domain of <m><var name="$f"/></m>?</p> <p><var name="$domain" width="10"/></p> </li> <li> <p>What is the range of <m><var name="$f"/></m>?</p> <p><var name="$range" width="10"/></p> </li> </ol></p>
</statement>
<solution>
<p><ol label="a."> <li> <p>The function is quadratic, so the domain is <m><var name="$domain"/></m>. There are no invalid inputs for this function.</p> </li> <li> <p>Since the formula is in vertex form, we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>. Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>. So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>. So the range is <m><var name="$range"/></m>.</p> </li> </ol></p>
</solution>
</webwork>
</exercise>
<exercise>
<webwork>
<setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = non_zero_random(2,9,1)*random(-1,1,2);
Context("Fraction");
$h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
$k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k");
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
</setup>
<statement>
<p>Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.<ol label="a."> <li> <p>What is the domain of <m><var name="$f"/></m>?</p> <p><var name="$domain" width="10"/></p> </li> <li> <p>What is the range of <m><var name="$f"/></m>?</p> <p><var name="$range" width="10"/></p> </li> </ol></p>
</statement>
<solution>
<p><ol label="a."> <li> <p>The function is quadratic, so the domain is <m><var name="$domain"/></m>. There are no invalid inputs for this function.</p> </li> <li> <p>Since the formula is in vertex form, we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>. Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>. So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>. So the range is <m><var name="$range"/></m>.</p> </li> </ol></p>
</solution>
</webwork>
</exercise>
<exercise>
<webwork>
<setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = non_zero_random(2,9,1)*random(-1,1,2);
Context("Fraction");
$h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
$k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k");
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
</setup>
<statement>
<p>Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.<ol label="a."> <li> <p>What is the domain of <m><var name="$f"/></m>?</p> <p><var name="$domain" width="10"/></p> </li> <li> <p>What is the range of <m><var name="$f"/></m>?</p> <p><var name="$range" width="10"/></p> </li> </ol></p>
</statement>
<solution>
<p><ol label="a."> <li> <p>The function is quadratic, so the domain is <m><var name="$domain"/></m>. There are no invalid inputs for this function.</p> </li> <li> <p>Since the formula is in vertex form, we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>. Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>. So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>. So the range is <m><var name="$range"/></m>.</p> </li> </ol></p>
</solution>
</webwork>
</exercise>
<exercise>
<webwork>
<setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = non_zero_random(2,9,1)*random(-1,1,2);
Context("Fraction");
$h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
$k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k");
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
</setup>
<statement>
<p>Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.<ol label="a."> <li> <p>What is the domain of <m><var name="$f"/></m>?</p> <p><var name="$domain" width="10"/></p> </li> <li> <p>What is the range of <m><var name="$f"/></m>?</p> <p><var name="$range" width="10"/></p> </li> </ol></p>
</statement>
<solution>
<p><ol label="a."> <li> <p>The function is quadratic, so the domain is <m><var name="$domain"/></m>. There are no invalid inputs for this function.</p> </li> <li> <p>Since the formula is in vertex form, we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>. Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>. So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>. So the range is <m><var name="$range"/></m>.</p> </li> </ol></p>
</solution>
</webwork>
</exercise>
<exercise>
<webwork>
<setup>
<pg-code>
$f = RandomVariableName(type=>'function');
$a = non_zero_random(2,9,1)*random(-1,1,2);
Context("Fraction");
$h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
$k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("$a(x - $h)^2 + $k");
Context("Interval");
$domain = Compute("(-inf,inf)");
$range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
Context("Point");
$vertex = Compute("($h,$k)");
$posneg = ($a > 0) ? "positive" : "negative";
$updown = ($a > 0) ? "upward" : "downward";
$greatless = ($a > 0) ? "greater" : "less";
</pg-code>
</setup>
<statement>
<p>Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.<ol label="a."> <li> <p>What is the domain of <m><var name="$f"/></m>?</p> <p><var name="$domain" width="10"/></p> </li> <li> <p>What is the range of <m><var name="$f"/></m>?</p> <p><var name="$range" width="10"/></p> </li> </ol></p>
</statement>
<solution>
<p><ol label="a."> <li> <p>The function is quadratic, so the domain is <m><var name="$domain"/></m>. There are no invalid inputs for this function.</p> </li> <li> <p>Since the formula is in vertex form, we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>. Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>. So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>. So the range is <m><var name="$range"/></m>.</p> </li> </ol></p>
</solution>
</webwork>
</exercise> -->
<!-- <exercise width="wide">
<webwork>
<pg-macros>
<macro-file>PCCmacros.pl</macro-file>
</pg-macros>
<setup>
<pg-code>
do {
$h = non_zero_random(-9,9,1);
$k = non_zero_random(-9,9,1);
} until (abs($h) != abs($k));
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("(x - $h)^2 + $k")->reduce;
$hdir = ($h > 0) ? PopUp(['?','left','right'],2) : PopUp(['?','left','right'],1);
$horizontal = MultiAnswer(abs($h),$hdir)->with(
singleResult => 1,
checker => sub {
my ($correct, $student, $self) = @_;
my @co = @{$correct};
my @st = @{$student};
return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'left' and (lex_sort($co[1], $st[1]))[1] eq 'right');
return 0;
}
);
$vdir = ($k > 0) ? PopUp(['?','down','up'],2) : PopUp(['?','down','up'],1);
$vertical = MultiAnswer(abs($k),$vdir)->with(
singleResult => 1,
checker => sub {
my ($correct, $student, $self) = @_;
my @co = @{$correct};
my @st = @{$student};
return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'down' and (lex_sort($co[1], $st[1]))[1] eq 'up');
return 0;
}
);
$hshift = ($h > 0) ? abs($h)." units right" : abs($h)." units left";
$vshift = ($k > 0) ? abs($k)." units up" : abs($h)." units down";
</pg-code>
</setup>
<statement>
<p>Consider the graph of the equation <m>y=<var name="$formula"/></m>.</p>
<p>Compared to the graph of <m>y=x^2</m>, the vertex has been shifted <var name="$horizontal" width="5" /> units <var name="$horizontal" form="popup" /> and <var name="$vertical" width="5" /> units <var name="$vertical" form="popup" />.</p>
</statement>
<solution>
<p>Since the vertex is <m>(<var name="$h"/>,<var name="$k"/>)</m>, the graph is the graph of <m>y=x^2</m> shifted <var name="$hshift" /> and <var name="$vshift"/>.</p>
</solution>
</webwork>
</exercise>
<exercise width="wide">
<webwork>
<pg-macros>
<macro-file>PCCmacros.pl</macro-file>
</pg-macros>
<setup>
<pg-code>
do {
$h = non_zero_random(-9,9,1);
$k = non_zero_random(-9,9,1);
} until (abs($h) != abs($k));
Context()->noreduce('(-x)+y','(-x)-y');
$formula = Formula("(x - $h)^2 + $k")->reduce;
$hdir = ($h > 0) ? PopUp(['?','left','right'],2) : PopUp(['?','left','right'],1);
$horizontal = MultiAnswer(abs($h),$hdir)->with(
singleResult => 1,
checker => sub {
my ($correct, $student, $self) = @_;
my @co = @{$correct};
my @st = @{$student};
return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'left' and (lex_sort($co[1], $st[1]))[1] eq 'right');
return 0;
}
);
$vdir = ($k > 0) ? PopUp(['?','down','up'],2) : PopUp(['?','down','up'],1);
$vertical = MultiAnswer(abs($k),$vdir)->with(
singleResult => 1,
checker => sub {
my ($correct, $student, $self) = @_;
my @co = @{$correct};
my @st = @{$student};
return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'down' and (lex_sort($co[1], $st[1]))[1] eq 'up');
return 0;
}
);
$hshift = ($h > 0) ? abs($h)." units right" : abs($h)." units left";
$vshift = ($k > 0) ? abs($k)." units up" : abs($h)." units down";
</pg-code>
</setup>
<statement>
<p>Consider the graph of the equation <m>y=<var name="$formula"/></m>.</p>
<p>Compared to the graph of <m>y=x^2</m>, the vertex has been shifted <var name="$horizontal" width="5" /> units <var name="$horizontal" form="popup" /> and <var name="$vertical" width="5" /> units <var name="$vertical" form="popup" />.</p>
</statement>
<solution>
<p>Since the vertex is <m>(<var name="$h"/>,<var name="$k"/>)</m>, the graph is the graph of <m>y=x^2</m> shifted <var name="$hshift" /> and <var name="$vshift"/>.</p>
</solution>
</webwork>
</exercise> -->
      <exercise permid="mul">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticFunctionForms10.pg" />
      </exercise>
      <exercise permid="SBu">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticFunctionForms10.pg" />
      </exercise>
      <exercise permid="yID">
        <webwork>
            <setup>
<pg-code>
Context("Point");
Context()->flags->set(reduceConstants=>0);
$a = 1;
do {
$r = non_zero_random(-9,9,1);
$s = non_zero_random(-9,9,1);
} until ($r != $s);
$f = RandomVariableName(type=>"function");
$formula = Formula("$a(x - $r)(x - $s)")->reduce;
$c = $a*$r*$s;
$yint = Point(0,$c);
$xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
$fof0 = $formula->substitute(x=>Formula("0"));
</pg-code>
            </setup>
            <statement>
              <p permid="zOj">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.
                <ol label="a." permid="UFc">
                  <li permid="Suq">
                    <p permid="ZDq">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>
                  <li permid="yBz">
                    <p permid="FKz">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="sxS">
              <p permid="lwj">
                <ol label="a." permid="rsz">
                  <li permid="eII">
                    <p permid="lRI">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>
                  <li permid="KPR">
                    <p permid="RYR">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="ePM">
        <webwork>
            <setup>
<pg-code>
Context("Point");
Context()->flags->set(reduceConstants=>0);
$a = 1;
do {
$r = non_zero_random(-9,9,1);
$s = non_zero_random(-9,9,1);
} until ($r != $s);
$f = RandomVariableName(type=>"function");
$formula = Formula("$a(x - $r)(x - $s)")->reduce;
$c = $a*$r*$s;
$yint = Point(0,$c);
$xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
$fof0 = $formula->substitute(x=>Formula("0"));
</pg-code>
            </setup>
            <statement>
              <p permid="fVs">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.
                <ol label="a." permid="AMl">
                  <li permid="qXa">
                    <p permid="yga">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>
                  <li permid="Xej">
                    <p permid="enj">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="YFb">
              <p permid="RDs">
                <ol label="a." permid="XzI">
                  <li permid="Dls">
                    <p permid="Kus">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>
                  <li permid="jsB">
                    <p permid="qBB">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>
    </exercisegroup>
    <newpage/>
    <exercisegroup cols="2" permid="hay">
      <title>Completing the Square</title>
      <exercise permid="Icp">
        <webwork>
            <setup>
<pg-code>
$x = RandomVariableName(type=>'variable');
$a = 1;
do {
$sol1 = non_zero_random(-9,9,1);
$sol2 = non_zero_random(-9,9,1);
($sol1, $sol2) = (min($sol1, $sol2), max($sol1, $sol2));
} until (abs($sol1) != abs($sol2)  and  ($sol1 + $sol2) % 2 == 0);
$b = -($sol1 + $sol2);
Context()->variables->are($x=>'Real');
$left = Formula("$a^2 $x^2 + $b $x")->reduce;
$right = Formula("-$sol1*$sol2")->reduce;
Context("Fraction");
$helper = Fraction(-$sol1 - $sol2, 2);
$helpersquared = $helper**2;
$rightplushelpersquared = Fraction("$right + $helpersquared");
$margin = Fraction(sqrt($rightplushelpersquared));
Context("FiniteSolutionSets");
Context()->variables->are($x=>'Real');
$answer = Formula("{$sol1, $sol2}");
$linear = Formula("$a $x")->reduce;
($num,$den) = $helper->value;
$absnum = abs($num);
$linear = ($helper > 0) ? Formula("$linear + $helper") : Formula("$linear - $absnum/$den");
</pg-code>
            </setup>
            <statement>
              <p permid="McB">
                Solve <m><var name="$left"/>=<var name="$right"/></m> by completing the square.
              </p>
              <p permid="sjK">
                <var name="$answer" width="25" />
              </p>
            </statement>
            <solution permid="EMk">
              <p permid="xKB">
                Compute <m>\left(\frac{<var name="$b" />}{2}\right)^2=<var name="$helpersquared" /></m>.
                Then
                <md permid="TlB">
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$right" />+<var name="$helpersquared" /></mrow>
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$rightplushelpersquared" /></mrow>
                  <mrow>\left(<var name="$linear" />\right)^2\amp = <var name="$rightplushelpersquared" /></mrow>
                </md>
                <md permid="zsK">
                  <mrow><var name="$linear" /> \amp = -<var name="$margin" />\amp\amp\text{or}\amp <var name="$linear" /> \amp = <var name="$margin" /></mrow>
                  <mrow><var name="$x" /> \amp = <var name="$sol1" />\amp\amp\text{or}\amp <var name="$x" /> \amp = <var name="$sol2" /></mrow>
                </md>
                So the solution set is <m><var name="$answer" /></m>.
              </p>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="ojy">
        <webwork>
            <setup>
<pg-code>
$x = RandomVariableName(type=>'variable');
$a = 1;
do {
$sol1 = non_zero_random(-9,9,1);
$sol2 = non_zero_random(-9,9,1);
($sol1, $sol2) = (min($sol1, $sol2), max($sol1, $sol2));
} until (abs($sol1) != abs($sol2)  and  ($sol1 + $sol2) % 2 == 0);
$b = -($sol1 + $sol2);
Context()->variables->are($x=>'Real');
$left = Formula("$a^2 $x^2 + $b $x")->reduce;
$right = Formula("-$sol1*$sol2")->reduce;
Context("Fraction");
$helper = Fraction(-$sol1 - $sol2, 2);
$helpersquared = $helper**2;
$rightplushelpersquared = Fraction("$right + $helpersquared");
$margin = Fraction(sqrt($rightplushelpersquared));
Context("FiniteSolutionSets");
Context()->variables->are($x=>'Real');
$answer = Formula("{$sol1, $sol2}");
$linear = Formula("$a $x")->reduce;
($num,$den) = $helper->value;
$absnum = abs($num);
$linear = ($helper > 0) ? Formula("$linear + $helper") : Formula("$linear - $absnum/$den");
</pg-code>
            </setup>
            <statement>
              <p permid="YqT">
                Solve <m><var name="$left"/>=<var name="$right"/></m> by completing the square.
              </p>
              <p permid="Eyc">
                <var name="$answer" width="25" />
              </p>
            </statement>
            <solution permid="kTt">
              <p permid="dRK">
                Compute <m>\left(\frac{<var name="$b" />}{2}\right)^2=<var name="$helpersquared" /></m>.
                Then
                <md permid="fzT">
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$right" />+<var name="$helpersquared" /></mrow>
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$rightplushelpersquared" /></mrow>
                  <mrow>\left(<var name="$linear" />\right)^2\amp = <var name="$rightplushelpersquared" /></mrow>
                </md>
                <md permid="LHc">
                  <mrow><var name="$linear" /> \amp = -<var name="$margin" />\amp\amp\text{or}\amp <var name="$linear" /> \amp = <var name="$margin" /></mrow>
                  <mrow><var name="$x" /> \amp = <var name="$sol1" />\amp\amp\text{or}\amp <var name="$x" /> \amp = <var name="$sol2" /></mrow>
                </md>
                So the solution set is <m><var name="$answer" /></m>.
              </p>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="UqH">
        <webwork>
            <setup>
<pg-code>
$x = RandomVariableName(type=>'variable');
$a = 1;
do {
$sol1 = non_zero_random(-9,9,1);
$sol2 = non_zero_random(-9,9,1);
($sol1, $sol2) = (min($sol1, $sol2), max($sol1, $sol2));
} until (abs($sol1) != abs($sol2)  and  ($sol1 + $sol2) % 2 == 1);
$b = -($sol1 + $sol2);
$c = $sol1*$sol2;
Context()->variables->are($x=>'Real');
$left0 = Formula("$a^2 $x^2 + $b $x + $c")->reduce;
$right0 = Formula("0")->reduce;
$left = Formula("$a^2 $x^2 + $b $x")->reduce;
$right = Formula("-$sol1*$sol2")->reduce;
Context("Fraction");
$helper = Fraction(-$sol1 - $sol2, 2);
$helpersquared = $helper**2;
$rightplushelpersquared = Fraction("$right + $helpersquared");
$margin = Fraction(sqrt($rightplushelpersquared));
Context("FiniteSolutionSets");
Context()->variables->are($x=>'Real');
$answer = Formula("{$sol1, $sol2}");
$linear = Formula("$a $x")->reduce;
($num,$den) = $helper->value;
$absnum = abs($num);
$linear = ($helper > 0) ? Formula("$linear + $helper") : Formula("$linear - $absnum/$den");
</pg-code>
            </setup>
            <statement>
              <p permid="kFl">
                Solve <m><var name="$left0"/>=<var name="$right0"/></m> by completing the square.
              </p>
              <p permid="QMu">
                <var name="$answer" width="25" />
              </p>
            </statement>
            <solution permid="RaC">
              <p permid="JYT">
                Move the constant term to the other side to get <m><var name="$left" /> = <var name="$right" /></m>.
                Then compute <m>\left(\frac{<var name="$b" />}{2}\right)^2=<var name="$helpersquared" /></m>.
                Then
                <md permid="rOl">
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$right" />+<var name="$helpersquared" /></mrow>
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$rightplushelpersquared" /></mrow>
                  <mrow>\left(<var name="$linear" />\right)^2\amp = <var name="$rightplushelpersquared" /></mrow>
                </md>
                <md permid="XVu">
                  <mrow><var name="$linear" /> \amp = -<var name="$margin" />\amp\amp\text{or}\amp <var name="$linear" /> \amp = <var name="$margin" /></mrow>
                  <mrow><var name="$x" /> \amp = <var name="$sol1" />\amp\amp\text{or}\amp <var name="$x" /> \amp = <var name="$sol2" /></mrow>
                </md>
                So the solution set is <m><var name="$answer" /></m>.
              </p>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="AxQ">
        <webwork>
            <setup>
<pg-code>
$x = RandomVariableName(type=>'variable');
$a = 1;
do {
$sol1 = non_zero_random(-9,9,1);
$sol2 = non_zero_random(-9,9,1);
($sol1, $sol2) = (min($sol1, $sol2), max($sol1, $sol2));
} until (abs($sol1) != abs($sol2)  and  ($sol1 + $sol2) % 2 == 1);
$b = -($sol1 + $sol2);
$c = $sol1*$sol2;
Context()->variables->are($x=>'Real');
$left0 = Formula("$a^2 $x^2 + $b $x + $c")->reduce;
$right0 = Formula("0")->reduce;
$left = Formula("$a^2 $x^2 + $b $x")->reduce;
$right = Formula("-$sol1*$sol2")->reduce;
Context("Fraction");
$helper = Fraction(-$sol1 - $sol2, 2);
$helpersquared = $helper**2;
$rightplushelpersquared = Fraction("$right + $helpersquared");
$margin = Fraction(sqrt($rightplushelpersquared));
Context("FiniteSolutionSets");
Context()->variables->are($x=>'Real');
$answer = Formula("{$sol1, $sol2}");
$linear = Formula("$a $x")->reduce;
($num,$den) = $helper->value;
$absnum = abs($num);
$linear = ($helper > 0) ? Formula("$linear + $helper") : Formula("$linear - $absnum/$den");
</pg-code>
            </setup>
            <statement>
              <p permid="wTD">
                Solve <m><var name="$left0"/>=<var name="$right0"/></m> by completing the square.
              </p>
              <p permid="daM">
                <var name="$answer" width="25" />
              </p>
            </statement>
            <solution permid="xhL">
              <p permid="qgc">
                Move the constant term to the other side to get <m><var name="$left" /> = <var name="$right" /></m>.
                Then compute <m>\left(\frac{<var name="$b" />}{2}\right)^2=<var name="$helpersquared" /></m>.
                Then
                <md permid="EcD">
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$right" />+<var name="$helpersquared" /></mrow>
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$rightplushelpersquared" /></mrow>
                  <mrow>\left(<var name="$linear" />\right)^2\amp = <var name="$rightplushelpersquared" /></mrow>
                </md>
                <md permid="kjM">
                  <mrow><var name="$linear" /> \amp = -<var name="$margin" />\amp\amp\text{or}\amp <var name="$linear" /> \amp = <var name="$margin" /></mrow>
                  <mrow><var name="$x" /> \amp = <var name="$sol1" />\amp\amp\text{or}\amp <var name="$x" /> \amp = <var name="$sol2" /></mrow>
                </md>
                So the solution set is <m><var name="$answer" /></m>.
              </p>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="gEZ">
        <webwork>
            <setup>
<pg-code>
$x = RandomVariableName(type=>'variable');
Context("Fraction");
Context()->variables->are($x=>'Real');
do {
$m1 = random(1,9,1);
$m2 = random(1,9,1);
$b1 = non_zero_random(-9,9,1);
$b2 = non_zero_random(-9,9,1);
$a = $m1*$m2;
$b = $m1*$b2 + $m2*$b1;
$c = $b1*$b2;
$B = Fraction($b,$a);
$C = Fraction($c,$a);
($Bnum,$Bden) = $B->value;
$absBnum = abs($Bnum);
($Cnum,$Cden) = $C->value;
$absCnum = abs($Cnum);
$helper = $B/2;
$helpersquared = $helper**2;
$rightplushelpersquared = Fraction("-$C + $helpersquared");
} until (gcd($m1,$b1) == 1 and gcd($m2,$b2) == 1 and $m1 != $m2 and ($rightplushelpersquared->value)[0] &lt;= 10 and ($rightplushelpersquared->value)[1] &lt; 10);
$left0 = Formula("$a $x^2 + $b $x + $c")->reduce;
$right0 = Formula("0")->reduce;
$left1 = Formula("$a $x^2 + $b $x")->reduce;
$right1 = Formula("-$c")->reduce;
$left = ($B > 0) ? Formula("$x^2 + $B $x") : Formula("$x^2 - $absBnum/$Bden $x");
$right = ($C > 0) ? Formula("-$C") : Formula("$absCnum/$Cden");
$sol1 = Fraction(-$b1,$m1);
$sol2 = Fraction(-$b2,$m2);
($sol1, $sol2) = (min($sol1, $sol2), max($sol1, $sol2));
$margin = Fraction(sqrt(($rightplushelpersquared->value)[0]),sqrt(($rightplushelpersquared->value)[1]));
$solstep1 = -$helper - $margin;
$solstep2 = -$helper + $margin;
Context("FiniteSolutionSets");
Context()->variables->are($x=>'Real');
$answer = Formula("{$sol1, $sol2}");
($num,$den) = $helper->value;
$absnum = abs($num);
$linear = ($helper > 0) ? Formula("$x + $helper") : Formula("$x - $absnum/$den");
</pg-code>
            </setup>
            <statement>
              <p permid="JhV">
                Solve <m><var name="$left0"/>=<var name="$right0"/></m> by completing the square.
              </p>
              <p permid="ppe">
                <var name="$answer" width="25" />
              </p>
            </statement>
            <solution permid="doU">
              <p permid="Wnl">
                Move the constant term to the other side to get <m><var name="$left1" /> = <var name="$right1" /></m>.
              </p>
              <p permid="Cuu">
                Divide through by <m><var name="$a"/></m> to get <m><var name="$left" /> = <var name="$right" /></m>.
              </p>
              <p permid="iBD">
                Compute <m>\left(\frac{<var name="$B" />}{2}\right)^2=<var name="$helpersquared" /></m>.
                Then
                <md permid="QqV">
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$right" />+<var name="$helpersquared" /></mrow>
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$rightplushelpersquared" /></mrow>
                  <mrow>\left(<var name="$linear" />\right)^2\amp = <var name="$rightplushelpersquared" /></mrow>
                </md>
                <md permid="wye">
                  <mrow><var name="$linear" /> \amp = -<var name="$margin" />\amp\amp\text{or}\amp <var name="$linear" /> \amp = <var name="$margin" /></mrow>
                  <mrow><var name="$x" /> \amp = <var name="$sol1" />\amp\amp\text{or}\amp <var name="$x" /> \amp = <var name="$sol2" /></mrow>
                </md>
                So the solution set is <m><var name="$answer" /></m>.
              </p>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="MMi">
        <webwork>
            <setup>
<pg-code>
$x = RandomVariableName(type=>'variable');
Context("Fraction");
Context()->variables->are($x=>'Real');
do {
$m1 = random(1,9,1);
$m2 = random(1,9,1);
$b1 = non_zero_random(-9,9,1);
$b2 = non_zero_random(-9,9,1);
$a = $m1*$m2;
$b = $m1*$b2 + $m2*$b1;
$c = $b1*$b2;
$B = Fraction($b,$a);
$C = Fraction($c,$a);
($Bnum,$Bden) = $B->value;
$absBnum = abs($Bnum);
($Cnum,$Cden) = $C->value;
$absCnum = abs($Cnum);
$helper = $B/2;
$helpersquared = $helper**2;
$rightplushelpersquared = Fraction("-$C + $helpersquared");
} until (gcd($m1,$b1) == 1 and gcd($m2,$b2) == 1 and $m1 != $m2 and ($rightplushelpersquared->value)[0] &lt;= 10 and ($rightplushelpersquared->value)[1] &lt; 10);
$left0 = Formula("$a $x^2 + $b $x + $c")->reduce;
$right0 = Formula("0")->reduce;
$left1 = Formula("$a $x^2 + $b $x")->reduce;
$right1 = Formula("-$c")->reduce;
$left = ($B > 0) ? Formula("$x^2 + $B $x") : Formula("$x^2 - $absBnum/$Bden $x");
$right = ($C > 0) ? Formula("-$C") : Formula("$absCnum/$Cden");
$sol1 = Fraction(-$b1,$m1);
$sol2 = Fraction(-$b2,$m2);
($sol1, $sol2) = (min($sol1, $sol2), max($sol1, $sol2));
$margin = Fraction(sqrt(($rightplushelpersquared->value)[0]),sqrt(($rightplushelpersquared->value)[1]));
$solstep1 = -$helper - $margin;
$solstep2 = -$helper + $margin;
Context("FiniteSolutionSets");
Context()->variables->are($x=>'Real');
$answer = Formula("{$sol1, $sol2}");
($num,$den) = $helper->value;
$absnum = abs($num);
$linear = ($helper > 0) ? Formula("$x + $helper") : Formula("$x - $absnum/$den");
</pg-code>
            </setup>
            <statement>
              <p permid="Vwn">
                Solve <m><var name="$left0"/>=<var name="$right0"/></m> by completing the square.
              </p>
              <p permid="BDw">
                <var name="$answer" width="25" />
              </p>
            </statement>
            <solution permid="Jwd">
              <p permid="OIM">
                Move the constant term to the other side to get <m><var name="$left1" /> = <var name="$right1" /></m>.
              </p>
              <p permid="uPV">
                Divide through by <m><var name="$a"/></m> to get <m><var name="$left" /> = <var name="$right" /></m>.
              </p>
              <p permid="aXe">
                Compute <m>\left(\frac{<var name="$B" />}{2}\right)^2=<var name="$helpersquared" /></m>.
                Then
                <md permid="cFn">
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$right" />+<var name="$helpersquared" /></mrow>
                  <mrow><var name="$left" />+<var name="$helpersquared" />\amp = <var name="$rightplushelpersquared" /></mrow>
                  <mrow>\left(<var name="$linear" />\right)^2\amp = <var name="$rightplushelpersquared" /></mrow>
                </md>
                <md permid="IMw">
                  <mrow><var name="$linear" /> \amp = -<var name="$margin" />\amp\amp\text{or}\amp <var name="$linear" /> \amp = <var name="$margin" /></mrow>
                  <mrow><var name="$x" /> \amp = <var name="$sol1" />\amp\amp\text{or}\amp <var name="$x" /> \amp = <var name="$sol2" /></mrow>
                </md>
                So the solution set is <m><var name="$answer" /></m>.
              </p>
            </solution>
        </webwork>
      </exercise>
      <exercise permid="sTr">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticDomainRange40.pg" />
      </exercise>
      <exercise permid="ZaA">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticDomainRange40.pg" />
      </exercise>
      <exercise permid="FhJ">
        <statement>
          <p permid="hKF">
            Graph <m>f(x)=x^2-7x+12</m> by algebraically determining its key features.
            Then state the domain and range of the function.
          </p>
        </statement>
        <answer permid="pDm">
          <sidebyside widths="47% 47%" margins="0%" permid="KWV">
            <stack>
              <p permid="Hen">
                The parabola opens upward.
              </p>
              <p permid="nlw">
                The <m>x</m>-intercepts are <m>(3,0)</m> and <m>(4,0)</m>.
              </p>
              <p permid="TsF">
                The <m>y</m>-intercept is <m>(0,12)</m>.
              </p>
              <p permid="zzO">
                The vertex form is <m>f(x)=(x - 3.5)^2 - 0.25</m>.
              </p>
              <p permid="fGX">
                The vertex is <m>(3.5,-0.25)</m> and the axis of symmetry is <m>x=3.5</m>.
              </p>
              <p permid="LOg">
                Symmetry can be used to determine the point <m>(7,12)</m>.
              </p>
              <p permid="rVp">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>[-0.25,\infty)</m>.
              </p>
            </stack>
            <figure permid="Ycy">
              <caption></caption>
              <image permid="hpg">
                <description>the graph of the parabola f(x)=x^2-7x+12 opens upward and the key features are labeled;the vertex is (3.5,-0.25);the axis of symmetry is drawn at x=3.5;the y-intercept is (0,12) and its symmetric point is (7,12);the x-intercepts are (3,0) and (4,0);the domain is (-infinity,infinity);the range is [-0.25,infinity)</description>
<latex-image>
<![CDATA[
\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=10,ymin=-5,ymax=15,
xtick={-5,0,5},minor xtick={-10,-9,...,10},
ytick={0,5,10},minor ytick={-5,-4,...,15}]
\addplot+[domain=-0.405:7.405] {x^2-7*x+12};
\addplot[soliddot] coordinates {(3,0) (4,0) (0,12) (3.5,-0.25) (7,12)};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
              </image>
            </figure>
          </sidebyside>
        </answer>
      </exercise>
      <exercise permid="loS">
        <statement>
          <p permid="NRO">
            Graph <m>f(x)=-x^2+4x+21</m> by algebraically determining its key features.
            Then state the domain and range of the function.
          </p>
        </statement>
        <answer permid="VKv">
          <sidebyside widths="47% 47%" margins="0%" permid="ree">
            <stack>
              <p permid="EjH">
                The parabola opens downward.
              </p>
              <p permid="kqQ">
                The <m>x</m>-intercepts are <m>(-3,0)</m> and <m>(7,0)</m>.
              </p>
              <p permid="QxZ">
                The <m>y</m>-intercept is <m>(0,21)</m>.
              </p>
              <p permid="wFi">
                The vertex form is <m>f(x)=-(x - 2)^2 + 25</m>.
              </p>
              <p permid="cMr">
                The vertex is <m>(2,25)</m> and the axis of symmetry is <m>x=2</m>.
              </p>
              <p permid="ITA">
                Symmetry can be used to determine the point <m>(4,21)</m>.
              </p>
              <p permid="paJ">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>(-\infty,25]</m>.
              </p>
            </stack>
            <figure permid="VhS">
              <caption></caption>
              <image permid="Nwp">
                <description>the graph of the parabola f(x)=-x^2+4x+21 opens downward and the key features are labeled;the vertex is (2,25);the axis of symmetry is drawn at x=2;the y-intercept is (0,21) and its symmetric point is (4,21);the x-intercepts are (-3,0) and (7,0);the domain is (-infinity,infinity);the range is (-infinity,25]</description>
<latex-image>
<![CDATA[
\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=10,ymin=-10,ymax=30,
xtick={-5,0,5},minor xtick={-10,-9,...,10},
ytick={-5,0,...,25},minor ytick={-10,-9,...,30}]
\addplot+[domain=-3.916:7.916] {-x^2+4*x+21};
\addplot[soliddot] coordinates {(7,0) (-3,0) (0,21) (2,25)  (4,21)};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
              </image>
            </figure>
          </sidebyside>
        </answer>
      </exercise>
      <exercise permid="Rwb">
        <statement>
          <p permid="tYX">
            Graph <m>f(x)=x^2-8x+16</m> by algebraically determining its key features.
            Then state the domain and range of the function.
          </p>
        </statement>
        <answer permid="BRE">
          <sidebyside widths="47% 47%" margins="0%" permid="Xln">
            <stack>
              <p permid="Bpb">
                The parabola opens upward.
              </p>
              <p permid="hwk">
                The <m>x</m>-intercept is <m>(4,0)</m>.
              </p>
              <p permid="NDt">
                The <m>y</m>-intercept is <m>(0,16)</m>.
              </p>
              <p permid="tKC">
                The vertex form is <m>f(x)=(x - 4)^2 + 0</m>.
              </p>
              <p permid="ZRL">
                The vertex is <m>(4,0)</m> and the axis of symmetry is <m>x=4</m>.
              </p>
              <p permid="FYU">
                Symmetry can be used to determine the point <m>(8,16)</m>.
              </p>
              <p permid="mgd">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>[0,\infty)</m>.
              </p>
            </stack>
            <figure permid="Snm">
              <caption></caption>
              <image permid="tDy">
                <description>the graph of the parabola f(x)=x^2-8x+16 opens upward and the key features are labeled;the vertex is (4,0);the axis of symmetry is drawn at x=4;the y-intercept is (0,16) and its symmetric point is (8,16);there is only one x-intercept at (4,0);the domain is (-infinity,infinity);the range is [0,infinity)</description>
<latex-image>
<![CDATA[
\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=10,ymin=-5,ymax=20,
xtick={-5,0,5},minor xtick={-10,-9,...,10},
ytick={0,5,10,15},minor ytick={-5,-4,...,19}]
\addplot+[domain=-0.472:8.472] {x^2-8*x+16};
\addplot[soliddot] coordinates {(4,0) (0,16) (8,16)};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
              </image>
            </figure>
          </sidebyside>
        </answer>
      </exercise>
      <exercise permid="xDk">
        <statement>
          <p permid="agg">
            Graph <m>f(x)=x^2+6x+9</m> by algebraically determining its key features.
            Then state the domain and range of the function.
          </p>
        </statement>
        <answer permid="hYN">
          <sidebyside widths="47% 47%" margins="0%" permid="Dsw">
            <stack>
              <p permid="yuv">
                The parabola opens upward.
              </p>
              <p permid="eBE">
                The <m>x</m>-intercept is <m>(-3,0)</m>.
              </p>
              <p permid="KIN">
                The <m>y</m>-intercept is <m>(0,9)</m>.
              </p>
              <p permid="qPW">
                The vertex form is <m>f(x)=(x + 3)^2 + 0</m>.
              </p>
              <p permid="WXf">
                The vertex is <m>(-3,0)</m> and the axis of symmetry is <m>x=-3</m>.
              </p>
              <p permid="Deo">
                Symmetry can be used to determine the point <m>(-6,9)</m>.
              </p>
              <p permid="jlx">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>[0,\infty)</m>.
              </p>
            </stack>
            <figure permid="PsG">
              <caption></caption>
              <image permid="ZKH">
                <description>the graph of the parabola f(x)=x^2+6x+9 opens upward and the key features are labeled;the vertex is (-3,0);the axis of symmetry is drawn at x=-3;the y-intercept is (0,9) and its symmetric point is (-6,9);there is only one x-intercept at (-3,0);the domain is (-infinity,infinity);the range is [0,infinity)</description>
<latex-image>
<![CDATA[
\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=10,ymin=-5,ymax=20,
xtick={-5,0,5},minor xtick={-10,-9,...,10},
ytick={0,5,10,15},minor ytick={-5,-4,...,20}]
\addplot+[domain=-7.472:1.472] {x^2+6*x+9};
\addplot[soliddot] coordinates {(-3,0) (0,9)  (-6,9)};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
              </image>
            </figure>
          </sidebyside>
        </answer>
      </exercise>
      <exercise permid="dKt">
        <statement>
          <p permid="Gnp">
            Graph <m>f(x)=x^2+4x+7</m> by algebraically determining its key features.
            Then state the domain and range of the function.
          </p>
        </statement>
        <answer permid="OfW">
          <sidebyside widths="47% 47%" margins="0%" permid="jzF">
            <stack>
              <p permid="vzP">
                The parabola opens upward.
              </p>
              <p permid="bGY">
                There are no <m>x</m>-intercepts.
              </p>
              <p permid="HOh">
                The <m>y</m>-intercept is <m>(0,7)</m>.
              </p>
              <p permid="nVq">
                The vertex form is <m>f(x)=(x + 2)^2 + 3</m>.
              </p>
              <p permid="Ucz">
                The vertex is <m>(-2,3)</m> and the axis of symmetry is <m>x=-2</m>.
              </p>
              <p permid="AjI">
                Symmetry can be used to determine the point <m>(-4,7)</m>.
              </p>
              <p permid="gqR">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>[3,\infty)</m>.
              </p>
            </stack>
            <figure permid="Mya">
              <caption></caption>
              <image permid="FRQ">
                <description>the graph of the parabola f(x)=-x^2+4x+7 opens downward and the key features are labeled;the vertex is (0,16);the axis of symmetry is drawn at x=0;the y-intercept is (0,16) which is also the vertex so there is no symmetric point; the x-intercepts are (-4,0) and (4,0);the domain is (-infinity,infinity);the range is (-infinity,16]</description>
<latex-image>
<![CDATA[
\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=10,ymin=-10,ymax=10,
xtick={-5,0,5},minor xtick={-10,-9,...,10},
ytick={-5,0,5},minor ytick={-10,-9,...,10}]
\addplot+[domain=-4.646:0.646] {x^2+4*x+7};
\addplot[soliddot] coordinates { (0,7) (-2,3) (-4,7)};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
              </image>
            </figure>
          </sidebyside>
        </answer>
      </exercise>
      <exercise permid="JRC">
        <statement>
          <p permid="muy">
            Graph <m>f(x)=x^2-2x+6</m> by algebraically determining its key features.
            Then state the domain and range of the function.
          </p>
        </statement>
        <answer permid="unf">
          <sidebyside widths="47% 47%" margins="0%" permid="PGO">
            <stack>
              <p permid="sFj">
                The parabola opens upward.
              </p>
              <p permid="YMs">
                There are no <m>x</m>-intercepts.
              </p>
              <p permid="ETB">
                The <m>y</m>-intercept is <m>(0,6)</m>.
              </p>
              <p permid="laK">
                The vertex form is <m>f(x)=(x - 1)^2 + 5</m>.
              </p>
              <p permid="RhT">
                The vertex is <m>(1,5)</m> and the axis of symmetry is <m>x=1</m>.
              </p>
              <p permid="xpc">
                Symmetry can be used to determine the point <m>(2,6)</m>.
              </p>
              <p permid="dwl">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>[5,\infty)</m>.
              </p>
            </stack>
            <figure permid="JDu">
              <caption></caption>
              <image permid="lYZ">
                <description>the graph of the parabola f(x)=x^2-2x+6 opens upward and the key features are labeled;the vertex is (1,5);the axis of symmetry is drawn at x=1;the y-intercept is (0,6) and its symmetric point is (2,6);there are no x-intercepts;the domain is (-infinity,infinity);the range is [5,infinity)</description>
<latex-image>
<![CDATA[
\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=10,ymin=-10,ymax=10,
xtick={-5,0,5},minor xtick={-10,-9,...,10},
ytick={-5,0,5},minor ytick={-10,-9,...,10}]
\addplot+[domain=-1.236:3.236] {x^2-2*x+6};
\addplot[soliddot] coordinates {(0,6) (1,5) (2,6)};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
              </image>
            </figure>
          </sidebyside>
        </answer>
      </exercise>
      <exercise permid="pYL">
        <statement>
          <p permid="SBH">
            Graph <m>f(x)=2x^2-4x-30</m> by algebraically determining its key features.
            Then state the domain and range of the function.
          </p>
        </statement>
        <answer permid="auo">
          <sidebyside widths="47% 47%" margins="0%" permid="vNX">
            <stack>
              <p permid="pKD">
                The parabola opens upward.
              </p>
              <p permid="VRM">
                The <m>x</m>-intercepts are <m>(-3,0)</m> and <m>(5,0)</m>.
              </p>
              <p permid="BYV">
                The <m>y</m>-intercept is <m>(0,-30)</m>.
              </p>
              <p permid="ige">
                The vertex form is <m>f(x)=2(x - 1)^2 - 32</m>.
              </p>
              <p permid="Onn">
                The vertex is <m>(1,-32)</m> and the axis of symmetry is <m>x=1</m>.
              </p>
              <p permid="uuw">
                Symmetry can be used to determine the point <m>(2,-30)</m>.
              </p>
              <p permid="aBF">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>[-32,\infty)</m>.
              </p>
            </stack>
            <figure permid="GIO">
              <caption></caption>
              <image permid="Sgi">
                <description>the graph of the parabola f(x)=2x^2-4x-30 opens upward and the key features are labeled;the vertex is (1,-32);the axis of symmetry is drawn at x=1;the y-intercept is (0,-30) and its symmetric point is (2,-30);the x-intercepts are (-3,0) and (5,0);the domain is (-infinity,infinity);the range is [-32,infinity)</description>
<latex-image>
<![CDATA[
\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=10,ymin=-40,ymax=10,
xtick={-5,0,5},minor xtick={-10,-9,...,10},
ytick={-35,-20,...,5},minor ytick={-40,-39,...,10}]
\addplot+[domain=-3.583:5.583] {2*x^2-4*x-30};
\addplot[soliddot] coordinates {(-3,0) (5,0) (0,-30) (1,-32) (2,-30)};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
              </image>
            </figure>
          </sidebyside>
        </answer>
      </exercise>
      <exercise permid="WfU">
        <statement>
          <p permid="yIQ">
            Graph <m>f(x)=3x^2+21x+36</m> by algebraically determining its key features.
            Then state the domain and range of the function.
          </p>
        </statement>
        <answer permid="GBx">
          <sidebyside widths="47% 47%" margins="0%" permid="bVg">
            <stack>
              <p permid="mPX">
                The parabola opens upward.
              </p>
              <p permid="SXg">
                The <m>x</m>-intercepts are <m>(-4,0)</m> and <m>(-3,0)</m>.
              </p>
              <p permid="zep">
                The <m>y</m>-intercept is <m>(0,36)</m>.
              </p>
              <p permid="fly">
                The vertex form is <m>f(x)=3(x + 3.5)^2 - 0.75</m>.
              </p>
              <p permid="LsH">
                The vertex is <m>(-3.5,-0.75)</m> and the axis of symmetry is <m>x=-3.5</m>.
              </p>
              <p permid="rzQ">
                Symmetry can be used to determine the point <m>(-7,36)</m>.
              </p>
              <p permid="XGZ">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>[-0.75,\infty)</m>.
              </p>
            </stack>
            <figure permid="DOi">
              <caption></caption>
              <image permid="ynr">
                <description>the graph of the parabola f(x)=3x^2+21x+36 opens upward and the key features are labeled;the vertex is (-3.5,-0.75);the axis of symmetry is drawn at x=-3.5;the y-intercept is (0,36) and its symmetric point is (-7,-36);the x-intercepts are (-4,0) and (-3,0);the domain is (-infinity,infinity);the range is [-0.75,infinity)</description>
<latex-image>
<![CDATA[
\begin{tikzpicture}
\begin{axis}[xmin=-10,xmax=5,ymin=-10,ymax=40,
xtick={-5,0},minor xtick={-10,-9,...,5},
ytick={-5,0,...,35},minor ytick={-10,-9,...,40}]
\addplot+[domain=-7.186:0.186] {3*x^2+21*x+36};
\addplot[soliddot] coordinates {(-4,0) (-3,0) (0,36) (-3.5,-0.75) (-7,36)};
\end{axis}
\end{tikzpicture}
]]>
</latex-image>
              </image>
            </figure>
          </sidebyside>
        </answer>
      </exercise>
      <exercise permid="Cnd">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex5.pg" />
      </exercise>
      <exercise permid="ium">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex5.pg" />
      </exercise>
    </exercisegroup>
    <exercisegroup cols="2" permid="NhH">
      <title>More on Complex Solutions to Quadratic Equations</title>
      <exercise permid="OBv">
        <webwork source="BasicAlgebra/ComplexNumber/complexSolutions25.pg" />
      </exercise>
      <exercise permid="uIE">
        <webwork source="BasicAlgebra/ComplexNumber/complexSolutions25.pg" />
      </exercise>
      <exercise permid="aPN">
        <webwork source="BasicAlgebra/ComplexNumber/complexSolutions40.pg" />
      </exercise>
      <exercise permid="GWW">
        <webwork source="BasicAlgebra/ComplexNumber/complexSolutions40.pg" />
      </exercise>
      <exercise permid="nef">
        <webwork source="BasicAlgebra/QuadraticApplications/FlyingObjectApp30.pg" />
      </exercise>
      <exercise permid="Tlo">
        <webwork source="BasicAlgebra/QuadraticApplications/FlyingObjectApp30.pg" />
      </exercise>
    </exercisegroup>
    <exercisegroup cols="2" permid="toQ">
      <title>Complex Number Operations</title>
      <exercise permid="zsx">
        <webwork source="Math95/ComplexNumbers/ComplexNumbers4.pg" />
      </exercise>
      <exercise permid="fzG">
        <webwork source="Math95/ComplexNumbers/ComplexNumbers4.pg" />
      </exercise>
      <exercise permid="LGP">
        <webwork source="Math95/ComplexNumbers/ComplexNumbers6.pg" />
      </exercise>
      <exercise permid="rNY">
        <webwork source="Math95/ComplexNumbers/ComplexNumbers6.pg" />
      </exercise>
      <exercise permid="XVh">
        <webwork source="BasicAlgebra/ComplexNumber/complexNumberOperations110.pg" />
      </exercise>
      <exercise permid="Ecq">
        <webwork source="BasicAlgebra/ComplexNumber/complexNumberOperations110.pg" />
      </exercise>
    </exercisegroup>
  </exercises>
</section>